{
  "generated_at": "2026-09-03T15:48:14.571Z",
  "items": [
    {
      "file": "About.tsx",
      "path": "src/pages/About.tsx",
      "title": "About",
      "text": "Building bridges between talent and opportunity Our Mission {pillar.title} By The Numbers About Our Services {service.title} Meet Our Team {member.name} Our Journey Join Our Team Building bridges between talent and opportunity Users worldwide Our Mission {pillar.icon} {pillar.title} {pillar.description} By The Numbers {stat.value} {stat.label} About Our Services {service.title} {service.description} Meet Our Team {member.badge} {member.name} {member.role} {member.description} Our Journey {milestone.event} {milestone.year} Join Our Team View Open Positions />, title: 'Transparency', description: 'Complete visibility into job market data and career opportunities', }, { icon: <Zap className= />, title: 'Innovation', description: 'Cutting-edge AI and technology to revolutionize job searching', }, { icon: <Users className= />, title: 'Inclusion', description: 'Creating opportunities for talent from diverse backgrounds', }, ]; const teamMembers = [ { name: 'Owoyemi Samuel Tobi', role: 'Chief Technology Officer', badge: 'Leadership', description: 'Owoyemi leads JobBridge’s technology vision, product engineering, and platform innovation with a focus on scalable systems, reliability, and meaningful user impact.', image: `${import.meta.env.BASE_URL}images/owoyemi-samuel-tobi.jpeg`, }, ]; const services = [ { title: 'Professional Services Marketplace', description: 'Connect with vetted professionals across development, design, marketing, finance, and legal services.', }, { title: 'Business Advertising', description: 'Promote your business with featured adverts, priority placement, and category targeting on JobBridge.', }, { title: 'AI-Powered Career Tools', description: 'Use resume tools, interview preparation, and intelligent job matching to move your career forward.', }, { title: 'Recruitment & Talent Solutions', description: 'Hire top talent, post jobs, and manage recruiting directly through our unified platform.', }, ]; return ( <div className= > <Header /> <main className= > { } <AnimatedSection direction= > <section className= > <div className= > <div> <h1 className= > <span className= > Building bridges </span> <br /> between talent and opportunity </h1> <p className= > JobBridge is revolutionizing how talent discovers opportunities and how companies find their next great team members. We leverage AI, data, and human insights to create meaningful connections in the job market. </p> </div> <div className= > <div className= > <img src= className= /> <div className= > <p className= >2M+</p> <p className= >Users worldwide</p> </div> </div> </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > Our Mission </h2> <div className= > {pillars.map((pillar, idx) => ( <div key={idx} className= > <div className= >{pillar.icon}</div> <h3 className= >{pillar.title}</h3> <p className= >{pillar.description}</p> </div> ))} </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > By The Numbers </h2> <div className= > {stats.map((stat, idx) => ( <div key={idx} className= > <p className= >{stat.value}</p> <p className= >{stat.label}</p> </div> ))} </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > About Our Services </h2> <div className= > {services.map((service, idx) => ( <div key={idx} className= > <h3 className= >{service.title}</h3> <p className= >{service.description}</p> </div> ))} </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > Meet Our Team </h2> <div className= > {teamMembers.map((member, idx) => ( <div key={idx} className= > <div className= > <div className= > <img src={member.image} alt={member.name} className= onError={e => { (e.target as HTMLImageElement).src = `${import.meta.env.BASE_URL}images/team-profile.jpeg`; }} /> </div> <div className= > <p className= >{member.badge}</p> <h3 className= >{member.name}</h3> <p className= >{member.role}</p> <p className= >{member.description}</p> </div> </div> </div> ))} </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > Our Journey </h2> <div className= > { } <div className= ></div> {milestones.map((milestone, idx) => ( <div key={idx} className={`flex ${idx % 2 === 0 ? 'md:flex-row-reverse' : 'md:flex-row'} gap-8 items-center`} > <div className= > <p className= >{milestone.event}</p> </div> <div className= > <div className= ></div> </div> <div className= > <p className= >{milestone.year}</p> </div> </div> ))} </div> </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= >Join Our Team</h2> <p className= > We're building the future of job searching. Help us bridge the gap between talent and opportunity. </p> <Link to= className= > View Open Positions <ArrowRight className= const milestones = [ year: 2021, event: 'Founded' , year: 2022, event: 'Series A' , year: 2023, event: '100k Users' , year: 2024, event: 'AI Launch' , year: 2025, event: '2M Users' , year: 2026, event: 'Global Expansion' , ]; const stats = [ label: 'Founded', value: '2021' , label: 'Users', value: '2M+' , label: 'Countries', value: '50+' , label: 'Funding', value: '₦12M' , ]; const pillars = [ icon: , title: 'Transparency', description: 'Complete visibility into job market data and career opportunities', , icon: , title: 'Innovation', description: 'Cutting-edge AI and technology to revolutionize job searching', , icon: , title: 'Inclusion', description: 'Creating opportunities for talent from diverse backgrounds', , ]; const teamMembers = [ name: 'Owoyemi Samuel Tobi', role: 'Chief Technology Officer', badge: 'Leadership', description: 'Owoyemi leads JobBridge’s technology vision, product engineering, and platform innovation with a focus on scalable systems, reliability, and meaningful user impact.', image: `$ import.meta.env.BASE_URL images/owoyemi-samuel-tobi.jpeg`, , ]; const services = [ title: 'Professional Services Marketplace', description: 'Connect with vetted professionals across development, design, marketing, finance, and legal services.', , title: 'Business Advertising', description: 'Promote your business with featured adverts, priority placement, and category targeting on JobBridge.', , title: 'AI-Powered Career Tools', description: 'Use resume tools, interview preparation, and intelligent job matching to move your career forward.', , title: 'Recruitment & Talent Solutions', description: 'Hire top talent, post jobs, and manage recruiting directly through our unified platform.', , ]; return ( Building bridges between talent and opportunity JobBridge is revolutionizing how talent discovers opportunities and how companies find their next great team members. We leverage AI, data, and human insights to create meaningful connections in the job market. 2M+ Us"
    },
    {
      "file": "AIAssistant.tsx",
      "path": "src/pages/AIAssistant.tsx",
      "title": "JobBridge AI",
      "text": "JobBridge AI Quick Actions AI Resume Builder Career Assessment Interview Simulator Salary Insights JobBridge AI Your AI-powered career coach {msg.content} Suggested prompts: {prompt} Send Quick Actions AI Resume Builder Generate and optimize Career Assessment Discover your path Interview Simulator Practice with AI feedback Salary Insights Real-time salary data Reduced load time by 40% min-h-screen bg-white flex flex-col flex-1 flex flex-col lg:flex-row gap-6 p-4 lg:p-8 flex-1 flex flex-col > <div className= > <Bot className= /> <h1 className= >JobBridge AI</h1> </div> <p className= >Your AI-powered career coach</p> </div> { } <div className= > <div className= > {messages.map((msg, idx) => ( <div key={idx} className={`flex ${msg.role === 'user' ? 'justify-end' : 'justify-start'}`} > <div className={`max-w-xs lg:max-w-md px-4 py-2 rounded-lg ${ msg.role === 'user' ? 'bg-blue-700 text-white' : 'bg-white text-gray-900 border border-gray-200' }`} > <p className= >{msg.content}</p> </div> </div> ))} </div> </div> { } <div className= > <p className= >Suggested prompts:</p> <div className= > {suggestedPrompts.map((prompt) => ( <button key={prompt} onClick={() => handleSuggestedPrompt(prompt)} className= > {prompt} </button> ))} </div> </div> { } <div className= > <input type= value={inputValue} onChange={(e) => setInputValue(e.target.value)} onKeyPress={(e) => e.key === 'Enter' && handleSendMessage()} placeholder= className= /> <button onClick={handleSendMessage} className= > <Send className= /> Send </button> </div> </div> { } <div className= > <div className= > <img src={IMG.hero.ai} alt= w-full h-36 object-cover hidden lg:block mb-4 AI career coaching demo /> </div> <h2 className= >Quick Actions</h2> { } <button onClick={() => openModal('ai-resume')} className= > <div className= > <Zap className= /> <div> <h3 className= >AI Resume Builder</h3> <p className= >Generate and optimize</p> </div> </div> </button> { } <button onClick={() => openModal('info', { title: 'Career Assessment', content: 'Our AI analyzes your skills, experience, and goals to provide a personalized career roadmap with specific action steps.', }) } className= > <div className= > <Brain className= /> <div> <h3 className= >Career Assessment</h3> <p className= >Discover your path</p> </div> </div> </button> { } <button onClick={() => openModal('info', { title: 'Interview Simulator', content: 'Practice common interview questions with AI feedback on your answers, body language tips, and confidence coaching.', }) } className= > <div className= > <Lightbulb className= /> <div> <h3 className= >Interview Simulator</h3> <p className= >Practice with AI feedback</p> </div> </div> </button> { } <button onClick={() => openModal('info', { title: 'Salary Insights', content: 'Get real-time salary data for your role and location, with negotiation scripts and benchmark comparisons.', }) } className= > <div className= > <TrendingUp className= /> <div> <h3 className= >Salary Insights</h3> <p className= interface ChatMessage role: 'user' | 'bot'; content: string; function getTimeGreeting(): string const hour = new Date().getHours(); const greeting = hour ([ role: 'bot', content: `$ getTimeGreeting() ! I'm your AI career assistant. I can help with resume writing, interview prep, job search strategies, and salary negotiation. What can I help you today?`, , role: 'user', content: 'Can you help me optimize my resume for a senior frontend role?', , role: 'bot', content: 'Absolutely! To optimize your resume for a senior frontend role, focus on: 1) Quantifiable achievements (e.g., \"Reduced load time by 40%\"), 2) Modern tech stack keywords (React, TypeScript, Next.js), 3) Leadership examples, 4) Open source contributions. Would you like me to analyze your current resume?', , ]); const [inputValue, setInputValue] = useState(''); const suggestedPrompts = [ 'Review my resume', 'Interview tips', 'Salary negotiation', 'Career pivot advice', 'Top companies hiring', 'Skills gap analysis', ]; const handleSendMessage = () => if (inputValue.trim()) setMessages([...messages, role: 'user', content: inputValue ]); setInputValue(''); ; const handleSuggestedPrompt = (prompt: string) => setMessages([...messages, role: 'user', content: prompt ]); setInputValue(''); ; return ( JobBridge AI Your AI-powered career coach messages.map((msg, idx) => ( msg.content )) Suggested prompts: suggestedPrompts.map((prompt) => ( handleSuggestedPrompt(prompt) className=\"whitespace-nowrap px-4 py-2 rounded-full bg-gray-100 text-gray-700 text-sm hover:bg-blue-100 hover:text-blue-700 transition\" > prompt )) setInputValue(e.target.value) onKeyPress= (e) => e.key === 'Enter' && handleSendMessage() placeholder=\"Ask me anything about your career...\" className=\"flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-blue-700\" /> Send Quick Actions openModal('ai-resume') className=\"w-full p-4 bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg border border-blue-200 hover:shadow-md tran"
    },
    {
      "file": "AIResume.tsx",
      "path": "src/pages/AIResume.tsx",
      "title": "AI Resume Studio",
      "text": "AI Resume Studio AI Career Tools Require a Subscription Extracted Skills ({skills.length}) Target Job Details Tailored Resume Cover Letter AI-Powered Application Tools AI Resume Studio Tailor your resume and generate cover letters with AI {error && ( AI Career Tools Require a Subscription Subscribe to unlock AI resume tailoring, cover letter generation, and more. View Pricing The AI service is powered securely through JobBridge servers. Your Master Resume {resumeFileName || 'Upload CV'} (.txt, .pdf, .doc) {extracting ? 'Analyzing...' : 'Extract Skills'} Extracted Skills ({skills.length}) Target Job Details {aiSubscription.ai_status !== 'active' ? Subscribe to Unlock : loading === 'tailor' ? 'Generating...' : Tailor Resume {aiSubscription.ai_status !== 'active' ? Subscribe to Unlock : loading === 'cover' ? 'Generating...' : Cover Letter {tailoredResume && ( Tailored Resume Copy {tailoredResume} Cover Letter Copy {coverLetter} AI-Powered Application Tools Paste your resume on the left, add the job details, and let AI generate a tailored resume and cover letter optimized for that specific role. min-h-screen bg-stone-50 max-w-5xl mx-auto px-4 py-6 pb-24 flex items-center gap-3 mb-6 w-10 h-10 rounded-xl bg-gradient-to-br from-blue-600 to-purple-600 flex items-center justify-center /> </div> <div> <h1 className= >AI Resume Studio</h1> <p className= >Tailor your resume and generate cover letters with AI</p> </div> </div> {error && ( <div className= > <AlertCircle className= /> {error} </div> )} {aiSubscription.ai_status !== 'active' && ( <div className= > <div className= > <Lock className= /> </div> <h2 className= >AI Career Tools Require a Subscription</h2> <p className= >Subscribe to unlock AI resume tailoring, cover letter generation, and more.</p> <button onClick={() => navigate('/pricing#ai')} className= > View Pricing </button> <p className= > The AI service is powered securely through JobBridge servers. </p> </div> )} <div className= > { } <div className= > <div className= > <label className= > <FileText className= /> Your Master Resume </label> <label className= > <Upload className= /> <span className= >{resumeFileName || 'Upload CV'}</span> <span className= >(.txt, .pdf, .doc)</span> <input type= .txt,.pdf,.doc,.docx /> </label> <textarea value={resumeText} onChange={e => setResumeText(e.target.value)} placeholder= rows={8} className= /> <button onClick={extractSkills} disabled={extracting || !resumeText.trim()} className= > <Bot className= /> {extracting ? 'Analyzing...' : 'Extract Skills'} </button> </div> {skills.length > 0 && ( <div className= > <h3 className= > <CheckCircle className= /> Extracted Skills ({skills.length}) </h3> <div className= > {skills.map(s => ( <span key={s} className= >{s}</span> ))} </div> </div> )} <div className= > <h3 className= >Target Job Details</h3> <div className= > <input value={jobTitle} onChange={e => setJobTitle(e.target.value)} placeholder= className= /> <input value={companyName} onChange={e => setCompanyName(e.target.value)} placeholder= className= /> <textarea value={jobDesc} onChange={e => setJobDesc(e.target.value)} placeholder= rows={4} className= /> <div className= > <button onClick={handleTailorResume} disabled={loading === 'tailor' || !resumeText.trim() || !jobTitle.trim()} className= > {aiSubscription.ai_status !== 'active' ? <><Lock className= /> Subscribe to Unlock</> : loading === 'tailor' ? 'Generating...' : <><Sparkles className= /> Tailor Resume</>} </button> <button onClick={handleCoverLetter} disabled={loading === 'cover' || !resumeText.trim() || !jobTitle.trim()} className= > {aiSubscription.ai_status !== 'active' ? <><Lock className= /> Subscribe to Unlock</> : loading === 'cover' ? 'Generating...' : <><Send className= /> Cover Letter</>} </button> </div> </div> </div> </div> { } <div className= > {tailoredResume && ( <div className= > <div className= > <h3 className= > <CheckCircle className= /> Tailored Resume </h3> <div className= > <button onClick={() => { const blob = new Blob([tailoredResume], { type: 'application/msword' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'tailored-resume.doc'; a.click(); URL.revokeObjectURL(url); }} className= > <Download className= /> DOC </button> <button onClick={() => navigator.clipboard.writeText(tailoredResume)} className= > Copy </button> </div> </div> <pre className= >{tailoredResume}</pre> </div> )} {coverLetter && ( <div className= > <div className= > <h3 className= > <Send className= /> Cover Letter </h3> <div className= > <button onClick={() => { const blob = new Blob([coverLetter], { type: 'application/msword' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'cover-letter.doc'; a.click(); URL.revokeObjectURL(url); }} className= > <Download className= /> DOC </button> <button onClick={() => navigator.clipboard.writeText(coverLetter)} className= > Copy </button> </div> </div> <pre className= >{coverLetter}</pre> </div> )} {!tailoredResume && !coverLetter && ( <div className= > <div className= > <Sparkles className= /> </div> <h3 className= >AI-Powered Application Tools</h3> <p className= let pdfWorkerInitialized = false; const initPdfWorker = async () => if (pdfWorkerInitialized) return; try const pdfjsLib = (await import('pdfjs-dist')).default; pdfjsLib.GlobalWorkerOptions.workerSrc = ` pdfWorkerInitialized = true; catch console.warn('PDF.js not available, PDF parsing will be limited'); ; async function extractPdfText(file: File): Promise try await initPdfWorker(); const default: pdfjsLib = await import('pdfjs-dist'); const arrayBuffer = await file.arrayBuffer(); const pdf = await pdfjsLib.getDocument( data: arrayBuffer ).promise; let fullText = ''; for (let i = 1; i item.str) .join(' '); fullText += pageText + '\\n'; return fullText.trim(); catch (error) console.error('PDF extraction error:', error); throw new Error('Could not extract text from PDF. Try uploading a text-based PDF.'); const user, profile, aiSubscription, fetchAiSubscription = async () => , = useAuth(); const navigate = useNavigate(); const [searchParams] = useSearchParams(); const fromPayment = searchParams.get('fromPayment') === 'true'; useEffect(() => if (!user?.id) return; void fetchAiSubscription(user.id); , [user?.id, fetchAiSubscription]); useEffect(() => if (!fromPayment || !user?.id) return; fetchAiSubscription(user.id).catch(() => ); , [fromPayment, user?.id, fetchAiSubscription]); const [resumeText, setResumeText] = useState(''); const [resumeFileName, setResumeFileName] = useState(''); const [skills, setSkills] = useState ([]); const [extracting, setExtracting] = useState(false); const [jobTitle, setJobTitle] = useState(''); const [jobDesc, setJobDesc] = useState(''); const [companyName, setCompanyName] = useState(''); const [tailoredResume, setTailoredResume] = useState(''); const [coverLetter, setCoverLetter] = useState(''); const [loading, setLoading] = useState(''); const [error, setError] = useState(''); const [autoGenerated, setAutoGenerated] = useState(false); async function handleFileUpload(e: React.ChangeEvent ) const file = e.target.files?.[0]; if (!file) return"
    },
    {
      "file": "Analytics.tsx",
      "path": "src/pages/Analytics.tsx",
      "title": "Analytics",
      "text": "Variant {abMetrics.variant} Conversion Ratio Interactive Assistant Local Payment Metrics {kpi.value} Application Activity Job Market Trends Salary Benchmarks Geographic Heatmap Remote work trends Skills Demand Hero A/B Variant Variant {abMetrics.variant} Refresh All Exposure A {abMetrics.exposures.A} Exposure B {abMetrics.exposures.B} CTA clicks A {abMetrics.ctaClicks.A} CTA clicks B {abMetrics.ctaClicks.B} Conversion Ratio CTA clicks divided by exposures for each variant. {abMetrics.exposures.A ? `${((abMetrics.ctaClicks.A / abMetrics.exposures.A) * 100).toFixed(1)}%` : '0.0%'} {abMetrics.exposures.B ? `${((abMetrics.ctaClicks.B / abMetrics.exposures.B) * 100).toFixed(1)}%` : '0.0%'} Last updated {new Date(abMetrics.lastUpdated).toLocaleString()} Reset This panel stores metrics locally in browser storage for quick A/B experimentation tracking. Assistant Clicks Interactive Assistant {Object.keys(assistantMetrics.clicks).length === 0 && No assistant clicks recorded yet. Last updated: {new Date(assistantMetrics.lastUpdated).toLocaleString()} Payment Events Local Payment Metrics {Object.keys(paymentMetrics.clicks).length === 0 && No payment metrics recorded yet. Last updated: {new Date(paymentMetrics.lastUpdated).toLocaleString()} {range.charAt(0).toUpperCase() + range.slice(1)} {kpi.title} {kpi.value} {kpi.change} vs last period Application Activity {item.day} {item.count} Job Market Trends {job.title} {job.percentage}% Salary Benchmarks Avg Salary YoY Change {item.role} {item.avg} {item.min} {item.max} {item.yoy} Geographic Heatmap {city.name} {city.count} Remote work trends Skills Demand {skill.name} ({skill.demand}) min-h-screen bg-gray-50 pb-24 Track your job search performance and market trends Analytics dashboard visualization max-w-6xl mx-auto px-4 py-8 mb-6 grid grid-cols-1 lg:grid-cols-3 gap-4 bg-white rounded-xl shadow p-6 border border-gray-200 flex items-center justify-between mb-4 text-sm text-gray-500 text-xl font-bold text-gray-900 flex items-center gap-3 text-sm text-blue-700 hover:underline text-sm text-gray-600 hover:underline > <div className= > <span>Exposure A</span> <span>{abMetrics.exposures.A}</span> </div> <div className= > <span>Exposure B</span> <span>{abMetrics.exposures.B}</span> </div> <div className= /> <div className= > <span>CTA clicks A</span> <span>{abMetrics.ctaClicks.A}</span> </div> <div className= > <span>CTA clicks B</span> <span>{abMetrics.ctaClicks.B}</span> </div> </div> </div> <div className= > <h3 className= >Conversion Ratio</h3> <p className= >CTA clicks divided by exposures for each variant.</p> <div className= > <div className= > <span>Variant A</span> <span>{abMetrics.exposures.A ? `${((abMetrics.ctaClicks.A / abMetrics.exposures.A) * 100).toFixed(1)}%` : '0.0%'}</span> </div> <div className= > <span>Variant B</span> <span>{abMetrics.exposures.B ? `${((abMetrics.ctaClicks.B / abMetrics.exposures.B) * 100).toFixed(1)}%` : '0.0%'}</span> </div> </div> </div> <div className= > <div className= > <div> <p className= >Last updated</p> <p className= >{new Date(abMetrics.lastUpdated).toLocaleString()}</p> </div> <button onClick={() => { resetHeroAbMetrics(); refreshMetrics(); }} className= > Reset </button> </div> <p className= >This panel stores metrics locally in browser storage for quick A/B experimentation tracking.</p> </div> </div> { } <div className= > <div className= > <div className= > <div> <p className= >Assistant Clicks</p> <h3 className= >Interactive Assistant</h3> </div> <div> <button onClick={() => { resetAssistantMetrics(); setAssistantMetrics(getAssistantMetrics()); }} className= >Reset</button> </div> </div> <div className= > {Object.keys(assistantMetrics.clicks).length === 0 && <div className= >No assistant clicks recorded yet.</div>} {Object.entries(assistantMetrics.clicks).map(([route, count]) => ( <div key={route} className= > <span className= >{route}</span> <span className= >{count}</span> </div> ))} <div className= >Last updated: {new Date(assistantMetrics.lastUpdated).toLocaleString()}</div> </div> </div> <div className= > <div className= > <div> <p className= >Payment Events</p> <h3 className= >Local Payment Metrics</h3> </div> <div> <button onClick={() => { resetPaymentMetrics(); setPaymentMetrics(getPaymentMetrics()); }} className= >Reset</button> </div> </div> <div className= > {Object.keys(paymentMetrics.clicks).length === 0 && <div className= >No payment metrics recorded yet.</div>} {Object.entries(paymentMetrics.clicks).map(([key, count]) => ( <div key={key} className= > <span className= >{key}</span> <span className= >{count}</span> </div> ))} <div className= >Last updated: {new Date(paymentMetrics.lastUpdated).toLocaleString()}</div> </div> </div> </div> { } <div className= > {(['week', 'month', 'quarter', 'year'] as DateRange[]).map((range) => ( <button key={range} onClick={() => setDateRange(range)} className={`px-4 py-2 rounded-lg font-medium transition-colors ${ dateRange === range ? 'bg-blue-700 text-white' : 'bg-white text-gray-700 hover:bg-gray-100' }`} > {range.charAt(0).toUpperCase() + range.slice(1)} </button> ))} </div> { } <div className= > {kpis.map((kpi) => { const Icon = kpi.icon; return ( <div key={kpi.title} className= > <div className= > <div> <p className= >{kpi.title}</p> <h3 className= >{kpi.value}</h3> </div> <Icon className= size={24} /> </div> <p className= >{kpi.change} vs last period</p> </div> ); })} </div> { } <div className= > <h2 className= >Application Activity</h2> <div className= > {applicationData.map((item) => ( <div key={item.day} className= > <span className= >{item.day}</span> <div className= > <div className= style={{ width: item.width }} /> </div> <span className= > {item.count} </span> </div> ))} </div> </div> { } <div className= > <h2 className= >Job Market Trends</h2> <div className= > {trendingJobs.map((job) => ( <div key={job.title} className= > <span className= >{job.title}</span> <div className= > <div className= > <div className= style={{ width: `${job.percentage}%` }} /> </div> </div> <span className= > {job.percentage}% </span> </div> ))} </div> </div> { } <div className= > <h2 className= >Salary Benchmarks</h2> <div className= > <table className= > <thead> <tr className= > <th className= >Role</th> <th className= >Avg Salary</th> <th className= >Min</th> <th className= >Max</th> <th className= >YoY Change</th> </tr> </thead> <tbody> {salaryData.map((item) => ( <tr key={item.role} className= > <td className= >{item.role}</td> <td className= >{item.avg}</td> <td className= >{item.min}</td> <td className= >{item.max}</td> <td className= >{item.yoy}</td> </tr> ))} </tbody> </table> </div> </div> { } <div className= > <h2 className= >Geographic Heatmap</h2> <div className= > {cities.map((city) => ( <div key={city.name} className= > <span className= >{city.name}</span> <div className= > <div className= > <div className= style={{ width: city.width }} > <span className= >{city.count}</span> </div> </div> </div> </div> ))} </div> </div> { } <div className= > <h2 className= >Remote work trends</h2> <VideoPlayer src={VIDEO.remoteWork.src} poster={VIDEO.remoteWork.poster} title= /> </div> { } <div className= > <h2 className= >Skills Demand</h2> <div className= > {skills.map((skill) => ( <div key={skill.name} className={`px-4 py-2 rounded-full font-medium transition-all ${getDemandColor( skill.demand )}`} > {skill.name} <span className= type DateRange = 'week' | 'month' | 'quarter' | 'year'; const [dateRange, setDateRange] = useState ('week'); const [abMetrics, setAbMetrics] = useState(() => getHeroAbMetrics()); const [assistantMetrics, setAssistantMetrics] = useState(() => getAssistantMetrics()); const [paymentMetrics, setPaymentMetrics] = useState(() => getPaymentMetrics()); useEffect(() => setAbMetrics(getHeroAbMetrics()); setAssistantMetrics(getAssistantMetrics()); setPaymentMetrics(getPaymentMetrics()); , []); const refreshMetrics = () => setAbMetrics(getHeroAbMetrics()); const refreshAll = () => setAbMetrics(getHeroAbMetrics()); setAssistantMetrics(getAssistantMetrics()); setPaymentMetrics(getPaymentMetrics()); ; const kpis = [ title: 'Total Applications', value: '1,247', change: '+12%', icon: Briefcase, , title: 'Profile Views', value: '8,431', change: '+24%', icon: Users, , title: 'Interview Rate', value: '23%', change: '+3%', icon: TrendingUp, , title: 'Offer Rate', value: '8%', change: '+1%', icon: Award, , ]; const applicationData = [ day: 'Mon', count: 145, width: '65%' , day: 'Tue', count: 198, width: '85%' , day: 'Wed', count: 176, width: '78%' , day: 'Thu', count: 212, width: '92%' , day: 'Fri', count: 225, width: '100%' , day: 'Sat', count: 87, width: '40%' , day: 'Sun', count: 63, width: '28%' , ]; const trendingJobs = [ title: 'React Developer', percentage: 89 , title: 'Data Scientist', percentage: 76 , title: 'Product Manager', percentage: 71 , title: 'DevOps Engineer', percentage: 65 , title: 'UX Designer', percentage: 58 , ]; const salaryData = [ role: 'React Developer', avg: '₦95,000', min: '₦75,000', max: '₦120,000', yoy: '+8%' , role: 'Data Scientist', avg: '₦110,000', min: '₦85,000', max: '₦145,000', yoy: '+12%' , role: 'Product Manager', avg: '₦105,000', min: '₦80,000', max: '₦135,000', yoy: '+6%' , role: 'DevOps Engineer', avg: '₦98,000', min: '₦78,000', max: '₦125,000', yoy: '+9%' , role: 'UX Designer', avg: '₦88,000', min: '₦68,000', max: '₦115,000', yoy: '+5%' , ]; con"
    },
    {
      "file": "AuthCallback.tsx",
      "path": "src/pages/AuthCallback.tsx",
      "title": "AuthCallback",
      "text": "Confirming your email... Email Confirmed! Confirmation Issue {status === \"processing\" && ( Confirming your email... Please wait while we verify your email address. This usually takes just a moment… Email Confirmed! Your email has been verified. Redirecting you to your profile... Confirmation Issue {errorMessage} Go to Sign In Return to Home , ); const [errorMessage, setErrorMessage] = useState( ); const [progress, setProgress] = useState(0); const progressTimer = useRef<ReturnType<typeof setInterval> | null>(null); const fallbackTimer = useRef<ReturnType<typeof setTimeout> | null>(null); const isRecovery = useRef(false); useEffect(() => { if (status !== ) { if (progressTimer.current) clearInterval(progressTimer.current); return; } progressTimer.current = setInterval(() => { setProgress((prev) => { if (prev >= 85) return prev; const step = Math.max(0.5, (85 - prev) / 15); return Math.min(85, prev + step); }); }, 100); return () => { if (progressTimer.current) clearInterval(progressTimer.current); }; }, [status]); useEffect(() => { let cancelled = false; const { data: { subscription: recoverySub }, } = supabase.auth.onAuthStateChange((event) => { if (event === ) { isRecovery.current = true; } }); const handleCallback = async () => { try { const hash = window.location.hash; const searchParams = new URLSearchParams(window.location.search); const code = searchParams.get( ); if (hash && hash.includes( )) { const hashParams = new URLSearchParams(hash.substring(1)); const type = hashParams.get( ); if (type === ) { isRecovery.current = true; } if (type === ) { await new Promise((resolve) => setTimeout(resolve, 1000)); } } else if (code) { const { error } = await supabase.auth.exchangeCodeForSession(code); if (error) throw error; } if (searchParams.get( ) { isRecovery.current = true; } let session: any = null; for (let attempt = 0; attempt < 5; attempt += 1) { const { data } = await supabase.auth.getSession(); session = data.session; if (session) break; await new Promise((resolve) => setTimeout(resolve, 400)); } if (cancelled) return; if (session) { if (progressTimer.current) clearInterval(progressTimer.current); setProgress(100); setStatus( ); setTimeout(() => { if (!cancelled) { if (isRecovery.current) { window.dispatchEvent( new CustomEvent( , { detail: { message: , type: , }, }), ); navigate( , { replace: true }); } else { window.dispatchEvent( new CustomEvent( , { detail: { message: , type: , }, }), ); navigate( , { replace: true }); } } }, 1500); } else { if (progressTimer.current) clearInterval(progressTimer.current); setStatus( ); setErrorMessage( , ); } } catch (err: any) { if (cancelled) return; if (progressTimer.current) clearInterval(progressTimer.current); setStatus( ); setErrorMessage( err?.message || , ); } }; fallbackTimer.current = setTimeout(() => { if (!cancelled) { navigate( , { replace: true }); } }, MAX_WAIT_MS); handleCallback().then(() => { if (fallbackTimer.current) clearTimeout(fallbackTimer.current); }); return () => { cancelled = true; if (progressTimer.current) clearInterval(progressTimer.current); if (fallbackTimer.current) clearTimeout(fallbackTimer.current); recoverySub.unsubscribe(); }; }, [navigate]); return ( <div className= > <div className= > <div className= /> <div className= style={{ animationDelay: '1s' }} /> </div> <div className= > <div className= > <div className= > <JobBridgeLogo variant= iconSize={40} /> </div> {status === && ( <> <div className= /> <h1 className= > Confirming your email... </h1> <p className= > Please wait while we verify your email address. </p> { } <div className= > <div className= style={{ width: `${progress}%` }} /> </div> <p className= >This usually takes just a moment…</p> </> )} {status === && ( <> <div className= > <CheckCircle className= /> </div> <h1 className= > Email Confirmed! </h1> <p className= > Your email has been verified. Redirecting you to your profile... </p> { } <div className= > <div className= style={{ width: `${progress}%` }} /> </div> </> )} {status === && ( <> <div className= > <XCircle className= /> </div> <h1 className= > Confirmation Issue </h1> <p className= >{errorMessage}</p> <button onClick={() => navigate( , { replace: true })} className= > Go to Sign In </button> <button onClick={() => navigate( , { replace: true })} className= const MAX_WAIT_MS = 8000; const navigate = useNavigate(); const [status, setStatus] = useState ( \"processing\", ); const [errorMessage, setErrorMessage] = useState(\"\"); const [progress, setProgress] = useState(0); const progressTimer = useRef | null>(null); const fallbackTimer = useRef | null>(null); const isRecovery = useRef(false); useEffect(() => if (status !== \"processing\") if (progressTimer.current) clearInterval(progressTimer.current); return; progressTimer.current = setInterval(() => setProgress((prev) => if (prev >= 85) return prev; const step = Math.max(0.5, (85 - prev) / 15); return Math.min(85, prev + step); ); , 100); return () => if (progressTimer.current) clearInterval(progressTimer.current); ; , [status]); useEffect(() => let cancelled = false; const data: subscription: recoverySub , = supabase.auth.onAuthStateChange((event) => if (event === \"PASSWORD_RECOVERY\") isRecovery.current = true; ); const handleCallback = async () => try const hash = window.location.hash; const searchParams = new URLSearchParams(window.location.search); const code = searchParams.get(\"code\"); if (hash && hash.includes(\"access_token\")) const hashParams = new URLSearchParams(hash.substring(1)); const type = hashParams.get(\"type\"); if (type === \"recovery\") isRecovery.current = true; if (type === \"signup\" || type === \"recovery\" || type === \"invite\") await new Promise((resolve) => setTimeout(resolve, 1000)); else if (code) const error = await supabase.auth.exchangeCodeForSession(code); if (error) throw error; if (searchParams.get(\"type\") === \"recovery\") isRecovery.current = true; let session: any = null; for (let attempt = 0; attempt setTimeout(resolve, 400)); if (cancelled) return; if (session) if (progressTimer.current) clearInterval(progressTimer.current); setProgress(100); setStatus(\"success\"); setTimeout(() => if (!cancelled) if (isRecovery.current) window.dispatchEvent( new CustomEvent(\"jobbridge:toast\", detail: message: \"Reset link verified! Now set your new password.\", type:"
    },
    {
      "file": "Blog.tsx",
      "path": "src/pages/Blog.tsx",
      "title": "Blog",
      "text": "{featuredPost.title} {post.title} Subscribe to JobBridge Insights {featuredPost.category} {featuredPost.title} {featuredPost.excerpt} {getInitials(featuredPost.author)} {featuredPost.author} {featuredPost.date} • {featuredPost.readTime} min read Read Article {category} {post.category} {post.title} {post.excerpt} {getInitials(post.author)} {post.author} {post.date} • {post.readTime} min Read More Subscribe to JobBridge Insights Get the latest articles on careers, hiring, and the future of work delivered to your inbox. Subscribe {subscribeMsg && ( {subscribeMsg.text} Responsible for social media, Increased engagement by 150% through targeted content strategy. References available upon request. min-h-screen bg-white flex flex-col flex-1 pb-20 md:pb-0 subtitle= images={HERO_CAROUSELS.blog} imageAlt= /> { } {featuredPost && ( <AnimatedSection direction= > <section className= > <div className= > <div> <img src={featuredPost.img || 'https: </div> <div className= > <span className={`inline-block px-3 py-1 rounded-full text-sm font-semibold mb-4 ${getCategoryColor(featuredPost.category)}`}> {featuredPost.category} </span> <h2 className= > {featuredPost.title} </h2> <p className= > {featuredPost.excerpt} </p> <div className= > <div className= > <div className= > {getInitials(featuredPost.author)} </div> <div> <p className= >{featuredPost.author}</p> <p className= >{featuredPost.date} • {featuredPost.readTime} min read</p> </div> </div> <button onClick={() => navigate(`/blog/${featuredPost.id}`)} className= > Read Article </button> </div> </div> </div> </section> </AnimatedSection> )} { } <section className= > <div className= > <div className= > {categories.map(category => ( <button key={category} onClick={() => setSelectedCategory(category)} className={`px-4 py-2 rounded-full font-semibold transition-all ${ selectedCategory === category ? 'bg-blue-700 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > {category} </button> ))} </div> </div> </section> { } <AnimatedSection direction= > <section className= > <div className= > {filteredPosts.map(post => ( <Card3D key={post.id} className= strength={6} > <img src={post.img || 'https: <div className= > <span className={`inline-block px-3 py-1 rounded-full text-xs font-semibold mb-3 w-fit ${getCategoryColor(post.category)}`}> {post.category} </span> <h3 className= > {post.title} </h3> <p className= > {post.excerpt} </p> <div className= > <div className= > <div className= > {getInitials(post.author)} </div> <div> <p className= >{post.author}</p> <p className= >{post.date} • {post.readTime} min</p> </div> </div> </div> <button onClick={() => navigate(`/blog/${post.id}`)} className= > Read More </button> </div> </Card3D> ))} </div> </section> </AnimatedSection> { } <AnimatedSection direction= > <section className= > <div className= > <h2 className= > Subscribe to JobBridge Insights </h2> <p className= > Get the latest articles on careers, hiring, and the future of work delivered to your inbox. </p> <div className= > <input type= value={subscribeEmail} onChange={e => setSubscribeEmail(e.target.value)} onKeyDown={e => e.key === 'Enter' && handleSubscribe()} placeholder= className= /> <button onClick={handleSubscribe} className= type Category = 'All' | 'Career Advice' | 'AI & Tech' | 'Hiring' | 'Remote Work' | 'Salary & Benefits' | 'Leadership'; interface BlogPost id: number; title: string; excerpt: string; content: string; category: Category; author: string; date: string; readTime: number; featured?: boolean; img?: string; const Blog: React.FC = () => const openModal = useModal(); const navigate = useNavigate(); const [selectedCategory, setSelectedCategory] = useState ('All'); const [subscribeEmail, setSubscribeEmail] = useState(''); const [subscribeMsg, setSubscribeMsg] = useState (null); const handleSubscribe = async () => const email = subscribeEmail.trim().toLowerCase(); if (!email || !/\\S+@\\S+\\.\\S+/.test(email)) setSubscribeMsg( type: 'error', text: 'Please enter a valid email' ); return; try await subscribeToBlog(email); setSubscribeMsg( type: 'success', text: 'Subscribed! Check your inbox.' ); setSubscribeEmail(''); catch (error) console.error('[Blog] subscription error', error); setSubscribeMsg( type: 'error', text: 'Could not subscribe. Try again later.' ); return; try await sendEmail( type: 'subscription', email ); catch (error) console.warn('[Blog] subscription email send failed', error); setTimeout(() => setSubscribeMsg(null), 4000); ; const categories: Category[] = ['All', 'Career Advice', 'AI & Tech', 'Hiring', 'Remote Work', 'Salary & Benefits', 'Leadership']; const blogPosts: BlogPost[] = [ id: 1, title: 'The Future of AI in Hiring', excerpt: 'As artificial intelligence reshapes the recruitment landscape, companies that embrace AI-powered matching see 3x faster hiring cycles and 40% better retention rates. Learn how to prepare your team for the AI-augmented workforce.', content: 'Artificial intelligence is fundamentally transforming how companies hire. From resume screening to candidate matching, AI tools are making recruitment faster, fairer, and more effective.\\n\\nAI-powered recruitment platforms can analyze thousands of resumes in seconds, identifying the best candidates"
    },
    {
      "file": "BlogDetail.tsx",
      "path": "src/pages/BlogDetail.tsx",
      "title": "Article not found",
      "text": "Article not found {post.title} Article not found Back to Blog Back to Blog {post.category} {post.title} {post.author} {post.date} {post.readTime} min read {para} Back to all articles Responsible for social media, Increased engagement by 150% through targeted content strategy. References available upon request. min-h-screen bg-gray-50 max-w-3xl mx-auto px-4 py-20 text-center text-2xl font-bold text-gray-900 mb-4 text-blue-700 hover:underline min-h-screen bg-gray-50 w-full h-64 sm:h-80 md:h-96 overflow-hidden w-full h-full object-cover max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex items-center text-gray-600 hover:text-blue-700 mb-6 transition-colors /> Back to Blog </button> { } <span className={`inline-block px-3 py-1 rounded-full text-xs font-semibold mb-4 ${catColor}`}> {post.category} </span> { } <h1 className= > {post.title} </h1> { } <div className= > <div className= > <User size={16} /> <span className= >{post.author}</span> </div> <div className= > <Calendar size={16} /> <span>{post.date}</span> </div> <div className= > <Clock size={16} /> <span>{post.readTime} min read</span> </div> </div> { } <div className= > {paragraphs.map((para, i) => ( <p key={i} className= > {para} </p> ))} </div> { } <div className= > <button onClick={() => navigate('/blog')} className= const blogPosts = [ id: 1, title: 'The Future of AI in Hiring', excerpt: 'As artificial intelligence reshapes the recruitment landscape, companies that embrace AI-powered matching see 3x faster hiring cycles and 40% better retention rates. Learn how to prepare your team for the AI-augmented workforce.', content: 'Artificial intelligence is fundamentally transforming how companies hire. From resume screening to candidate matching, AI tools are making recruitment faster, fairer, and more effective.\\n\\nAI-powered recruitment platforms can analyze thousands of resumes in seconds, identifying the best candidates based on skills, experience, and cultural fit. Machine learning algorithms continuously improve their recommendations based on hiring outcomes, creating a virtuous cycle of better matches over time.\\n\\nCompanies that have adopted AI-driven hiring report 3x faster time-to-hire and 40% improvement in retention rates. These tools eliminate unconscious bias by focusing on objective qualifications rather than subjective impressions.\\n\\nHowever, successful implementation requires careful planning. HR teams need training to interpret AI recommendations effectively, and companies must ensure their AI tools are transparent and auditable.\\n\\nThe future of hiring is hybrid — combining the efficiency of AI with the irreplaceable human elements of empathy, intuition, and relationship-building that define great recruitment.', category: 'AI & Tech', author: 'Sarah Martinez', date: 'June 1, 2026', readTime: 8, img: 'https: , id: 2, title: '10 Resume Mistakes That Cost You Interviews', excerpt: 'Discover the most common resume mistakes that keep candidates from landing interviews. Learn what recruiters are looking for and how to optimize your resume to stand out.', content: 'Your resume is often the first impression you make on a potential employer. Yet many qualified candidates sabotage their chances with avoidable mistakes.\\n\\n1. Generic Objective Statements: Replace vague obje"
    },
    {
      "file": "Business.tsx",
      "path": "src/pages/Business.tsx",
      "title": "Business",
      "text": "{advert.title} Advertisements Showcase Featured Business Monthly Ads Weekly Ads Advert Packages Create New Advert My Adverts {advert.title} Business Tips & Insights How to Write Effective Adverts Maximizing Your Ad ROI {viewAdvert.title} Edit Advert {advert.title} {advert.featured && ( {advert.businessName} {advert.description} {advert.category} {advert.duration} {advert.views.toLocaleString()} views {advert.clicks.toLocaleString()} users clicked View advert {user?.id !== advert.ownerId && ( Advertisements Showcase Discover businesses advertising on JobBridge — updated as subscribed business owners post. {loadingPublicAdverts ? ( Loading advertisements... ) : publicAdverts.length === 0 ? ( No advertisements yet. Be the first to showcase your business! Featured Business {featuredAdverts.length} renderAdCard(advert))} Monthly Ads {monthlyAdverts.length} renderAdCard(advert))} Weekly Ads {weeklyAdverts.length} renderAdCard(advert))} {stats.totalAds} Total Adverts {stats.activeAds} Active Now {stats.totalViews.toLocaleString()} Total Views {stats.totalClicks} Total User Clicks {canCreateAdvert ? ( {subscription.advert_credits} advert credit{subscription.advert_credits !== 1 ? 's' : ''} remaining {subscription.tier && ( {subscription.tier.replace(/_/g, ' ')} plan Each advert uses 1 credit. {subscription.status === 'active' ? 'No advert credits' : 'No active plan'} Subscribe to create adverts View Plans Advert Packages {pkg.popular && ( Most Popular {pkg.name} ₦{pkg.price.toLocaleString()} {pkg.duration} Display on platform Category placement View analytics {pkg.name === 'Featured Business' && ( Homepage spotlight {canCreateAdvert ? ( Create Advert Subscribe & Create Create New Advert You have {subscription.advert_credits} advert credit{subscription.advert_credits !== 1 ? 's' : ''} remaining. Creating this will use 1 credit. Business Name * Advert Title * Description * Business Picture {(formData.imageUrl || imageFile) && ( {formData.imageUrl || imageFile ? 'Change picture' : 'Upload business picture'} {hasExistingAdvert && ( You already have an active advert. Only 1 active advert is allowed per business. Subscribe to Create Adverts My Adverts {loadingAdverts ? ( Loading your adverts... ) : adverts.length === 0 ? ( No adverts yet. Create your first advert above! {advert.title} {advert.featured && ( {advert.businessName} {advert.status.charAt(0).toUpperCase() + advert.status.slice(1)} {advert.description} {advert.category} {advert.duration} {advert.views.toLocaleString()} views {advert.clicks} users clicked View {advert.status === 'active' ? 'Pause' : advert.status === 'expired' ? 'Expired' : 'Activate'} Edit Delete Business Tips & Insights View All How to Write Effective Adverts Learn the secrets to creating ads that convert viewers into customers. Maximizing Your Ad ROI Get the most out of your advertising budget with these proven strategies. {viewAdvert.featured && ( {viewAdvert.title} {viewAdvert.businessName} {viewAdvert.category} {viewAdvert.duration} {viewAdvert.views.toLocaleString()} views {viewAdvert.clicks} users clicked {viewAdvert.description} {user?.id !== viewAdvert.ownerId && ( Chat with Business Visit Website Edit Advert Business Name * Advert Title * Description * Business Picture {(editForm.imageUrl || editImageFile) && ( {editForm.imageUrl || editImageFile ? 'Change picture' : 'Upload business picture'} Recommended size: 1220 × 434 pixels. Contact Email Category * {pkg.name} Featured placement Your advert gets priority positioning Cancel {savingEdit ? 'Saving...' : 'Save Changes'} w-full h-36 object-cover > <div className= > <div className= > <div className= > <h3 className= >{advert.title}</h3> {advert.featured && ( <span className= > <Star className= /> Featured </span> )} </div> <p className= >{advert.businessName}</p> </div> </div> <p className= >{advert.description}</p> <div className= > <span className= > <Building className= /> {advert.category} </span> <span className= > <Clock className= /> {advert.duration} </span> <span className= > <Eye className= /> {advert.views.toLocaleString()} views </span> <span className= > <TrendingUp className= /> {advert.clicks.toLocaleString()} users clicked </span> </div> <div className= > <button onClick={(e) => { e.stopPropagation(); handleViewAdvert(advert); }} className= > View advert <ChevronRight className= /> </button> {user?.id !== advert.ownerId && ( <button onClick={(e) => { e.stopPropagation(); handleChatAdvert(advert); }} className= > <MessageCircle className= /> Chat </button> )} </div> </div> </div> ); const handleChatAdvert = async (ad: Advert) => { if (!user?.id) { push({ message: 'Please sign in to message this business.', type: 'info' }); navigate(`/login?redirect=${encodeURIComponent('/business')}`); return; } if (!ad.ownerId || ad.ownerId === user.id) { push({ message: 'You cannot message your own business advert.', type: 'info' }); return; } const senderName = profile?.full_name || user.email || 'A user'; try { const conversationId = await createConversationMessage({ senderId: user.id, senderName, recipientId: ad.ownerId, recipientName: ad.businessName || 'Business', recipientEmail: ad.email || undefined, message: `Hello ${ad.businessName}! I came across your advert on JobBridge and I'd love to know more about your business.`, }); if (conversationId) { navigate(`/messages?conversationId=${encodeURIComponent(conversationId)}`); } else { navigate('/messages'); } } catch (error) { console.error('[Business] chat advert failed:', error); push({ message: '❌ Could not start a chat. Please try again.', type: 'error' }); navigate('/messages'); } }; const isFeaturedAd = (a: Advert) => a.featured || a.duration === 'Featured'; const sortedPublicAdverts = sortAdverts(publicAdverts); const featuredAdverts = sortedPublicAdverts.filter(isFeaturedAd); const monthlyAdverts = sortedPublicAdverts.filter(a => !isFeaturedAd(a) && a.duration === 'Monthly'); const weeklyAdverts = sortedPublicAdverts.filter(a => !isFeaturedAd(a) && a.duration === 'Weekly'); const stats = { totalAds: adverts.length, activeAds: adverts.filter(a => a.status === 'active').length, totalViews: adverts.reduce((sum, ad) => sum + ad.views, 0), totalClicks: adverts.reduce((sum, ad) => sum + ad.clicks, 0), }; return ( <div className= > <Header /> <PageHero title= Promote your business to thousands of JobBridge users across Nigeria Small business owner promoting their brand max-w-6xl mx-auto px-4 py-6 > <div className= > <div> <h2 className= >Advertisements Showcase</h2> <p className= > Discover businesses advertising on JobBridge — updated as subscribed business owners post. </p> </div> </div> {loadingPublicAdverts ? ( <div className= > <div className= > <Building className= /> </div> <p className= >Loading advertisements...</p> </div> ) : publicAdverts.length === 0 ? ( <div className= > <Building className= /> <p className= >No advertisements yet. Be the first to showcase your business!</p> </div> ) : ( <> { } {featuredAdverts.length > 0 && ( <div className= > <div className= > <Star className= /> <h3 className= >Featured Business</h3> <span className= >{featuredAdverts.length}</span> </div> <div className= > {featuredAdverts.map(advert => renderAdCard(advert))} </div> </div> )} { } {monthlyAdverts.length > 0 && ( <div className= > <div className= > <Clock className= /> <h3 className= >Monthly Ads</h3> <span className= >{monthlyAdverts.length}</span> </div> <div className= > {monthlyAdverts.map(advert => renderAdCard(advert))} </div> </div> )} { } {weeklyAdverts.length > 0 && ( <div className= > <div className= > <Building className= /> <h3 className= >Weekly Ads</h3> <span className= >{weeklyAdverts.length}</span> </div> <div className= > {weeklyAdverts.map(advert => renderAdCard(advert))} </div> </div> )} </> )} </AnimatedSection> { } <div className= > <div className= > <div className= >{stats.totalAds}</div> <div className= >Total Adverts</div> </div> <div className= > <div className= >{stats.activeAds}</div> <div className= >Active Now</div> </div> <div className= > <div className= >{stats.totalViews.toLocaleString()}</div> <div className= >Total Views</div> </div> <div className= > <div className= >{stats.totalClicks}</div> <div className= >Total User Clicks</div> </div> </div> { } <div className= > {canCreateAdvert ? ( <div className= > <div className= > <CheckCircle className= /> <span className= >{subscription.advert_credits} advert credit{subscription.advert_credits !== 1 ? 's' : ''} remaining</span> {subscription.tier && ( <span className= > {subscription.tier.replace(/_/g, ' ')} plan </span> )} </div> <div className= >Each advert uses 1 credit.</div> </div> ) : ( <div className= > <div className= > <CreditCard className= /> <span className= >{subscription.status === 'active' ? 'No advert credits' : 'No active plan'}</span> <span className= >Subscribe to create adverts</span> </div> <Link to= text-xs bg-amber-600 text-white px-3 py-1.5 rounded-lg font-medium hover:bg-amber-700 transition-colors inline-flex items-center > <h2 className= >Advert Packages</h2> <div className= > {adPackages.map((pkg) => ( <div key={pkg.name} className={`bg-white rounded-xl p-5 border-2 ${pkg.popular ? 'border-blue-500 relative' : 'border-gray-100'}`} > {pkg.popular && ( <div className= > Most Popular </div> )} <div className= >{pkg.name}</div> <div className= >₦{pkg.price.toLocaleString()}</div> <div className= >{pkg.duration}</div> <ul className= > <li className= > <CheckCircle className= /> Display on platform </li> <li className= > <CheckCircle className= /> Category placement </li> <li className= > <CheckCircle className= /> View analytics </li> {pkg.name === 'Featured Business' && ( <li className= > <Star className= /> Homepage spotlight </li> )} </ul> {canCreateAdvert ? ( <button onClick={() => { openCreateForm(pkg.name); }} className={`w-full py-2.5 rounded-lg font-medium text-sm transition-colors ${ pkg.popular ? 'bg-blue-700 text-white hover:bg-blue-800' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > Create Advert </button> ) : ( <Link to={`/payment?plan=${pkg.name === 'Weekly Ad' ? 'business_weekly' : pkg.name === 'Monthly Ad' ? 'business_monthly' : 'business_featured'}`} className={`block w-full py-2.5 rounded-lg font-medium text-sm text-center transition-colors ${ pkg.popular ? 'bg-blue-100 text-blue-500 border-2 border-blue-200 hover:bg-blue-200' : 'bg-gray-100 text-gray-500 border-2 border-gray-200 hover:bg-gray-200' }`} > <Lock className= /> Subscribe & Create </Link> )} </div> ))} </div> </div> { } {showCreateForm && ( <div className= > <div className= > <h2 className= >Create New Advert</h2> <p className= > <CheckCircle className= /> You have {subscription.advert_credits} advert credit{subscription.advert_credits !== 1 ? 's' : ''} remaining. Creating this will use 1 credit. </p> <form onSubmit={handleCreateAd} className= > <div> <label className= >Business Name *</label> <input type= required value={formData.businessName} onChange={(e) => setFormData({ ...formData, businessName: e.target.value })} className= placeholder= /> </div> <div> <label className= >Advert Title *</label> <input type= required value={formData.title} onChange={(e) => setFormData({ ...formData, title: e.target.value })} className= placeholder= /> </div> <div> <label className= >Description *</label> <textarea required rows={3} value={formData.description} onChange={(e) => setFormData({ ...formData, description: e.target.value })} className= placeholder= /> </div> <div> <label className= >Business Picture</label> <div className= > {(formData.imageUrl || imageFile) && ( <img src={imageFile ? URL.createObjectURL(imageFile) : formData.imageUrl} alt= className= /> )} <label className= > <ImagePlus className= /> {formData.imageUrl || imageFile ? 'Change picture' : 'Upload business picture'} <input type= accept= > {hasExistingAdvert && ( <div className= > <Lock className= /> <span>You already have an active advert. Only <strong>1 active advert</strong> is allowed per business.</span> </div> )} {canCreateAdvert ? ( <button onClick={() => { if (hasExistingAdvert) { push({ message: '❌ You already have an active advert. Pause or delete it before creating another one.', type: 'error', }); return; } openCreateForm(); }} disabled={subscription.advert_credits < 1 || hasExistingAdvert} className= > <Plus className= /> {hasExistingAdvert ? 'Active advert limit reached (1 per business)' : subscription.advert_credits > 0 ? `Create New Advert (${subscription.advert_credits} credit${subscription.advert_credits !== 1 ? 's' : ''} remaining)` : 'No credits remaining'} </button> ) : ( <Link to= className= > <Lock className= /> Subscribe to Create Adverts </Link> )} </div> { } <div className= > <h2 className= >My Adverts</h2> {loadingAdverts ? ( <div className= > <Building className= /> <p className= >Loading your adverts...</p> </div> ) : adverts.length === 0 ? ( <div className= > <Building className= /> <p className= >No adverts yet. Create your first advert above!</p> </div> ) : ( <div className= > {sortAdverts(adverts).map((advert) => ( <div key={advert.id} className={`bg-white rounded-xl overflow-hidden border ${advert.featured ? 'border-amber-200 ring-1 ring-amber-200' : 'border-gray-100'}`} > <img src={advert.imageUrl || advertImage(advert.category)} alt={advert.title} className= /> <div className= > <div className= > <div className= > <div className= > <h3 className= >{advert.title}</h3> {advert.featured && ( <span className= > <Star className= /> Featured </span> )} </div> <p className= >{advert.businessName}</p> </div> <span className={`px-2.5 py-1 rounded-full text-xs font-medium ${ advert.status === 'active' ? 'bg-emerald-100 text-emerald-700' : advert.status === 'pending' ? 'bg-amber-100 text-amber-700' : advert.status === 'paused' ? 'bg-gray-100 text-gray-600' : 'bg-red-100 text-red-600' }`} > {advert.status.charAt(0).toUpperCase() + advert.status.slice(1)} </span> </div> <p className= >{advert.description}</p> <div className= > <span className= > <Building className= /> {advert.category} </span> <span className= > <Clock className= /> {advert.duration} </span> <span className= > <Eye className= /> {advert.views.toLocaleString()} views </span> <span className= > <TrendingUp className= /> {advert.clicks} users clicked </span> </div> <div className= > <button onClick={() => handleViewAdvert(advert)} className= > <Eye className= /> View </button> <button onClick={() => handleToggleAdStatus(advert)} disabled={advert.status === 'expired'} className={`px-3 py-1.5 rounded-lg text-sm font-medium ${ advert.status === 'active' ? 'bg-gray-100 text-gray-700 hover:bg-gray-200' : advert.status === 'expired' ? 'bg-gray-50 text-gray-400 cursor-not-allowed' : 'bg-emerald-100 text-emerald-700 hover:bg-emerald-200' }`} > {advert.status === 'active' ? 'Pause' : advert.status === 'expired' ? 'Expired' : 'Activate'} </button> <button onClick={() => openEditAdvert(advert)} className= > <Edit className= /> Edit </button> <button onClick={() => handleDeleteAdvert(advert)} className= > <Trash2 className= /> Delete </button> </div> </div> </div> ))} </div> )} </div> { } <AnimatedSection direction= bg-white rounded-xl p-5 border border-gray-100 flex items-center justify-between mb-4 text-lg font-bold text-gray-900 text-sm text-blue-700 hover:underline flex items-center gap-1 /> </Link> </div> <div className= > <div className= > <img src= > <h3 className= >How to Write Effective Adverts</h3> <p className= >Learn the secrets to creating ads that convert viewers into customers.</p> <button onClick={() => openModal('info', { title: 'Effective Adverts', content: 'Key tips: Use clear headlines, include a call-to-action, showcase benefits not just features, add high-quality images, and always include contact information.' })} className= >Read more</button> </div> </div> <div className= > <img src= > <h3 className= >Maximizing Your Ad ROI</h3> <p className= >Get the most out of your advertising budget with these proven strategies.</p> <button onClick={() => openModal('info', { title: 'Ad ROI Tips', content: 'To maximize ROI: Target the right category, upgrade to featured for higher visibility, update your ads regularly, respond to inquiries quickly, and track performance metrics.' })} className= >Read more</button> </div> </div> </div> </div></AnimatedSection> </div> { } {viewAdvert && ( <div className= > <div className= > <div className= > <img src={viewAdvert.imageUrl || advertImage(viewAdvert.category)} alt={viewAdvert.title} className= /> <button onClick={() => setViewAdvert(null)} className= aria-label= > <X className= /> </button> {viewAdvert.featured && ( <span className= > <Star className= /> Featured </span> )} </div> <div className= > <h2 className= >{viewAdvert.title}</h2> <p className= >{viewAdvert.businessName}</p> <div className= > <span className= > <Building className= /> {viewAdvert.category} </span> <span className= > <Clock className= /> {viewAdvert.duration} </span> <span className= > <Eye className= /> {viewAdvert.views.toLocaleString()} views </span> <span className= > <TrendingUp className= /> {viewAdvert.clicks} users clicked </span> </div> <p className= >{viewAdvert.description}</p> <div className= > {user?.id !== viewAdvert.ownerId && ( <button onClick={() => handleChatAdvert(viewAdvert)} className= > <MessageCircle className= /> Chat with Business </button> )} {viewAdvert.phone && ( <a href={`tel:${viewAdvert.phone}`} onClick={() => handleAdvertContact(viewAdvert, () => {})} className= > <Phone className= /> Call </a> )} {viewAdvert.website && ( <a href={viewAdvert.website} target= rel= onClick={() => handleAdvertContact(viewAdvert, () => {})} className= > <Globe className= /> Visit Website </a> )} {viewAdvert.email && ( <a href={`mailto:${viewAdvert.email}`} onClick={() => handleAdvertContact(viewAdvert, () => {})} className= > <Mail className= /> Email </a> )} </div> </div> </div> </div> )} { } {editingAdvert && ( <div className= > <div className= > <div className= > <h2 className= >Edit Advert</h2> <button onClick={() => setEditingAdvert(null)} className= aria-label= > <X className= /> </button> </div> <form onSubmit={handleSaveEdit} className= > <div> <label className= >Business Name *</label> <input type= required value={editForm.businessName} onChange={(e) => setEditForm({ ...editForm, businessName: e.target.value })} className= placeholder= /> </div> <div> <label className= >Advert Title *</label> <input type= required value={editForm.title} onChange={(e) => setEditForm({ ...editForm, title: e.target.value })} className= placeholder= /> </div> <div> <label className= >Description *</label> <textarea required rows={3} value={editForm.description} onChange={(e) => setEditForm({ ...editForm, description: e.target.value })} className= placeholder= /> </div> <div> <label className= >Business Picture</label> <div className= > {(editForm.imageUrl || editImageFile) && ( <img src={editImageFile ? URL.createObjectURL(editImageFile) : editForm.imageUrl} alt= className= /> )} <label className= > <ImagePlus className= /> {editForm.imageUrl || editImageFile ? 'Change picture' : 'Upload business picture'} <input type= accept= className= onChange={(e) => { const file = e.target.files?.[0] || null; setEditImageFile(file); if (file) setEditForm({ ...editForm, imageUrl: '' }); }} /> </label> </div> <p className= >Recommended size: 1220 × 434 pixels.</p> </div> <div className= > <div> <label className= >Phone</label> <input type= value={editForm.phone} onChange={(e) => setEditForm({ ...editForm, phone: e.target.value })} className= placeholder= /> </div> <div> <label className= >Website</label> <input type= value={editForm.website} onChange={(e) => setEditForm({ ...editForm, website: e.target.value })} className= placeholder= block text-sm font-medium text-gray-700 mb-1 value={editForm.email} onChange={(e) => setEditForm({ ...editForm, email: e.target.value })} className= placeholder= /> </div> <div className= > <div> <label className= >Category *</label> <select required value={editForm.category} onChange={(e) => setEditForm({ ...editForm, category: e.target.value })} className= > <option value= >Select...</option> {categories.map(cat => ( <option key={cat} value={cat}>{cat}</option> ))} </select> </div> <div> <label className= >Package *</label> <select required value={editForm.package} onChange={(e) => setEditForm({ ...editForm, package: e.target.value })} className= > <option value= >Select...</option> {adPackages.map(pkg => ( <option key={pkg.name} value={pkg.name}>{pkg.name}</option> ))} </select> </div> </div> <div className= > <input type= id= checked={editForm.featured} onChange={(e) => setEditForm({ ...editForm, featured: e.target.checked })} className= /> <label htmlFor= > <span className= >Featured placement</span> <br /> <span className= >Your advert gets priority positioning</span> </label> </div> <div className= > <button type= onClick={() => setEditingAdvert(null)} className= > Cancel </button> <button type= disabled={savingEdit} className= fetchAdvertisementsByOwner, fetchPublicAdvertisements, createAdvertisement, updateAdvertisement, deleteAdvertisement, incrementAdvertisementViews, incrementAdvertisementClicks, decrementAdvertCredits, createConversationMessage, from '../lib/supabaseQueries'; Building, Plus, Eye, Clock, CheckCircle, CreditCard, TrendingUp, Star, ChevronRight, Edit, Trash2, Lock, Phone, Globe, Mail, X, ExternalLink, MessageCircle, ImagePlus, from 'lucide-react'; type AdvertStatus = 'pending' | 'active' | 'paused' | 'expired' | 'rejected'; interface Advert id: string; ownerId: string; businessName: string; title: string; description: string; category: string; duration: string; price: number; status: AdvertStatus; startDate: string; endDate: string; views: number; clicks: number; featured: boolean; imageUrl?: string; website?: string; phone?: string; email?: string; const initialAdverts: Advert[] = []; const adPackages = [ name: 'Weekly Ad', duration: '7 days', price: 2000, popular: false , name: 'Monthly Ad', duration: '30 days', price: 7500, popular: true , name: 'Featured Business', duration: '30 days', price: 15000, popular: false , ]; const categories = ['Restaurant', 'Fashion', 'Technology', 'Education', 'Health', 'Entertainment', 'Automotive', 'Real Estate', 'Other']; function effectiveAdStatus(status: string, expiresAt?: string | null): AdvertStatus const base = (status || 'active') as AdvertStatus; if (base !== 'expired' && expiresAt) const t = new Date(expiresAt).getTime(); if (!Number.isNaN(t) && t (a.featured || a.duration === 'Featured' ? 0 : a.duration === 'Monthly' ? 1 : 2); return [...list].sort((a, b) => const ra = rank(a); const rb = rank(b); if (ra !== rb) return ra - rb; if (a.featured !== b.featured) return a.featured ? -1 : 1; return 0; ); const openModal = useModal(); const navigate = useNavigate(); const [searchParams] = useSearchParams(); const user, profile, subscription, subscriptionLoaded, fetchSubscription = useAuth(); const push = useToasts(); const canCrea"
    },
    {
      "file": "Career.tsx",
      "path": "src/pages/Career.tsx",
      "title": "JobBridge Career Hub",
      "text": "JobBridge Career Hub {f.title} Get Beta Access & Launch Alerts Featured Database Opportunities {job.title} Ready to Start Your Search? Launch Milestones {phase.title} Coming Soon JobBridge Career Hub Your comprehensive professional ecosystem for upskilling, mentorship, and career growth. {tab.title} Stay Updated Get Beta Access & Launch Alerts Subscribe to be notified the instant these tools go live and secure your spot in our early-access coaching cohorts. {subscribed ? ( You're on the list! We'll alert you the moment the hub launches. {subscribing ? {subscribeError} Featured Database Opportunities Live job postings fetched dynamically from our Postgres server database. {jobsLoading ? ( Querying database... ) : jobs.length === 0 ? ( No active jobs found in the database. Please post a job as recruiter to see it listed here dynamically! {job.title} {job.company} {job.location} {job.type} Apply Now Ready to Start Your Search? View the entire catalog of hundreds of jobs posted across Nigerian companies. Browse All Listings Launch Milestones {phase.phase} {phase.title} {item} min-h-screen bg-[#f8f9fc] relative h-48 sm:h-56 overflow-hidden absolute inset-0 bg-gradient-to-br from-indigo-700 via-blue-600 to-teal-500 absolute inset-0 opacity-15 absolute top-8 left-[15%] w-36 h-36 bg-white/20 rounded-full blur-2xl animate-pulse absolute bottom-6 right-[20%] w-48 h-48 bg-purple-300/25 rounded-full blur-3xl style={{ backgroundImage: `radial-gradient(circle at 1px 1px, rgba(255,255,255,0.08) 1px, transparent 0)`, backgroundSize: '20px 20px' }} /> <div className= > <span className= >Coming Soon</span> <h1 className= >JobBridge Career Hub</h1> <p className= >Your comprehensive professional ecosystem for upskilling, mentorship, and career growth.</p> </div> </div> <div className= > { } <div className= > {[ { id: 'overview', title: 'Hub Overview', icon: GraduationCap, color: 'from-blue-500 to-indigo-600' }, { id: 'jobs', title: 'Featured Jobs', icon: Briefcase, color: 'from-emerald-500 to-teal-600' }, { id: 'roadmap', title: 'Timeline & Roadmap', icon: Clock, color: 'from-purple-500 to-pink-600' }, ].map(tab => { const Icon = tab.icon; const isActive = activeTab === tab.id; return ( <button key={tab.id} onClick={() => setActiveTab(tab.id as any)} className={`flex items-center gap-2 px-5 py-3 rounded-xl text-sm font-semibold transition-all duration-300 whitespace-nowrap ${ isActive ? 'bg-white text-gray-900 shadow-md border border-gray-100 scale-[1.02]' : 'bg-white/50 text-gray-500 hover:bg-white hover:text-gray-700 border border-transparent' }`} > <div className={`w-6 h-6 rounded-lg flex items-center justify-center transition-all ${ isActive ? `bg-gradient-to-br ${tab.color} text-white shadow-sm` : 'bg-gray-100 text-gray-400' }`}> <Icon className= /> </div> {tab.title} </button> ); })} </div> { } {activeTab === 'overview' && ( <div className= style={{ animation: 'pop-in 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) forwards' }}> <div className= > {UPCOMING_FEATURES.map((f, i) => { const Icon = f.icon; return ( <div key={f.title} className= > <div className={`w-11 h-11 rounded-xl bg-gradient-to-br ${f.color} flex items-center justify-center mb-4 text-white shadow-sm`}> <Icon className= /> </div> <div className= > <h3 className= >{f.title}</h3> <span className= >{f.tag}</span> </div> <p className= >{f.desc}</p> </div> ); })} </div> { } <div className= > <div className= /> <div className= > <div className= > <span className= >Stay Updated</span> <h3 className= >Get Beta Access & Launch Alerts</h3> <p className= >Subscribe to be notified the instant these tools go live and secure your spot in our early-access coaching cohorts.</p> </div> <div className= > {subscribed ? ( <div className= > <div className= > <Check className= /> </div> <div> <p className= >You're on the list!</p> <p className= >We'll alert you the moment the hub launches.</p> </div> </div> ) : ( <form onSubmit={handleSubscribe} className= > <input type= required value={emailInput} onChange={e => setEmailInput(e.target.value)} placeholder= className= /> <button type= disabled={subscribing} className= > {subscribing ? <Loader2 className= /> : <Send className= />} Notify Me </button> </form> )} {subscribeError && ( <p className= >{subscribeError}</p> )} </div> </div> </div> </div> )} { } {activeTab === 'jobs' && ( <div className= style={{ animation: 'pop-in 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) forwards' }}> <div className= > <h2 className= > <Briefcase className= /> Featured Database Opportunities </h2> <p className= >Live job postings fetched dynamically from our Postgres server database.</p> {jobsLoading ? ( <div className= > <div className= /> <p className= >Querying database...</p> </div> ) : jobs.length === 0 ? ( <div className= > No active jobs found in the database. Please post a job as recruiter to see it listed here dynamically! </div> ) : ( <div className= > {jobs.slice(0, 5).map(job => ( <div key={job.id} onClick={() => navigate('/jobs')} className= > <div> <h4 className= >{job.title}</h4> <p className= >{job.company}</p> <div className= > <span className= > <MapPin className= /> {job.location} </span> <span className= >{job.type}</span> </div> </div> <button className= > Apply Now <ArrowRight className= /> </button> </div> ))} </div> )} </div> { } <div className= > <h3 className= >Ready to Start Your Search?</h3> <p className= >View the entire catalog of hundreds of jobs posted across Nigerian companies.</p> <button onClick={() => navigate('/jobs')} className= > Browse All Listings <ArrowRight className= /> </button> </div> </div> )} { } {activeTab === 'roadmap' && ( <div className= style={{ animation: 'pop-in 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) forwards' }}> <div className= > <div className= > <Calendar className= /> <h2 className= >Launch Milestones</h2> </div> <div className= > {ROADMAP.map((phase) => ( <div key={phase.phase} className= > { } <div className= > <div className= /> </div> <div> <span className= >{phase.phase}</span> <h4 className= >{phase.title}</h4> <ul className= > {phase.items.map(item => ( <li key={item} className= > <Check className= Briefcase, Bell, ArrowRight, Check, GraduationCap, Users, Target, FileText, MessageCircle, BookOpen, Tv, Trophy, Calendar, Sparkles, Loader2, ChevronRight, Clock, Star, MapPin, Send, Mail from 'lucide-react'; const UPCOMING_FEATURES = [ icon: GraduationCap, title: 'Career Coaching', desc: 'One-on-one sessions with certified career coaches to define your path and land your dream role.', color: 'from-blue-500 to-indigo-600', tag: 'Expert-Led', , icon: Users, title: 'Mentorship Program', desc: 'Connect with experienced professionals in your field who will guide you and share industry insights.', color: 'from-emerald-500 to-teal-600', tag: 'Community', , icon: Target, title: 'Skill Assessments', desc: 'Take verified assessments to benchmark your skills and receive personalized recommendations.', color: 'from-purple-500 to-pink-600', tag: 'Verify', , icon: FileText, title: 'Professional Certifications', desc: 'Earn JobBridge-recognized certifications in customer service, coding, and sales.', color: 'from-amber-500 to-orange-600', tag: 'Certified', , icon: Tv, title: 'Workshops & Webinars', desc: 'Attend live and recorded sessions on CV writing, branding, and navigating the job market.', color: 'from-rose-500 to-red-600', tag: 'Live', , icon: MessageCircle, title: 'Networking Groups', desc: 'Join industry-specific groups to share opportunities and support with peers.', color: 'from-cyan-500 to-blue-600', tag: 'Network', , ]; const ROADMAP = [ phase: 'Q3 2026', title: 'Beta Launch', items: ['Career coaching booking', 'Learning library', 'Skill assessment pilot'], gradient: 'from-blue-500 to-indigo-600' , phase: 'Q4 2026', title: 'Expansion', items: ['Certification exams live', 'Workshop calendar', 'Peer networking groups'], gradient: 'from-emerald-500 to-teal-600' , phase: 'Q1 2027', title: 'Full Release', items: ['Automated matching', 'Gamified rewards & badges', 'Progress dashboard'], gradient: 'from-purple-500 to-pink-600' , ]; const navigate = useNavigate(); const [act"
    },
    {
      "file": "CEO.tsx",
      "path": "src/pages/CEO.tsx",
      "title": "CEO",
      "text": "We're building the infrastructure for the future of work Owoyemi Samuel Tobi Our Vision Pillars Democratizing Opportunity AI-First Approach Global Impact A note from Owoyemi Company Milestones {milestone.title} Join Our Mission Welcome from our CTO We're building the infrastructure for the future of work JobBridge is more than a platform—it's a movement to reimagine how talent and opportunity connect in a rapidly changing world. Owoyemi Samuel Tobi Chief Technology Officer Owoyemi Samuel Tobi leads JobBridge's technology vision, product engineering, and platform innovation. He focuses on scalable systems, reliability, and meaningful user impact as JobBridge connects talent with opportunity. Our Vision Pillars Democratizing Opportunity Every person deserves access to meaningful career opportunities. We're removing barriers and creating pathways for talent from all backgrounds. AI-First Approach Artificial intelligence should augment human decision-making, not replace it. We're building AI tools that enhance recruiting and career discovery for everyone. Global Impact Economic opportunity knows no borders. JobBridge is expanding globally to connect talent and employers across the world. A note from Owoyemi When I founded JobBridge, it started with a simple belief: talent is everywhere, but opportunity is not. Across communities, I saw skilled people—graduates, artisans, creatives, technicians, and professionals—struggling to find meaningful opportunities, not because they lacked ability, but because they lacked access. At the same time, businesses and employers were searching for talent, yet many qualified individuals remained invisible. The problem was never a shortage of talent. The problem was the gap between talent and opportunity. JobBridge was created to close that gap. We believe that a person's future should not be determined by who they know, where they live, or whether they have access to the right network. Everyone deserves a fair chance to showcase their skills, discover opportunities, and build a better life. Our mission is to create a platform where employers can find the right people, job seekers can access the right opportunities, and professionals can continuously develop themselves through learning and training. Technology is helping us make this vision possible. By leveraging intelligent tools and data-driven solutions, we can connect people with opportunities faster, reduce barriers to entry, and create a more transparent and inclusive job market. At JobBridge, we're not just building a job platform. We're building a bridge between potential and possibility, between ambition and achievement, and between communities and economic growth. Because when talent meets opportunity, lives change. And when lives change, communities grow. Company Milestones {milestone.year} {milestone.title} {milestone.description} Join Our Mission We're building something meaningful. If you're passionate about the future of work, we want to hear from you. Explore Open Roles Learn More About Us Owoyemi Samuel Tobi, Chief Technology Officer of JobBridge w-full h-full object-contain min-h-screen bg-white flex flex-col flex-1 pb-20 md:pb-0 relative py-16 px-4 sm:px-6 lg:px-8 overflow-hidden max-w-6xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-8 items-center h-96 rounded-lg shadow-lg overflow-hidden bg-gray-100 text-blue-700 font-semibold mb-4 uppercase tracking-wider text-4xl sm:text-5xl font-bold text-gray-900 mb-6 text-lg text-gray-600 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg p-8 border border-blue-200 flex flex-col sm:flex-row gap-8 items-start className= style={{ objectPosition: 'center 20%' }} onError={e => { (e.target as HTMLImageElement).src = ''; }} /> <div className= > <h2 className= >Owoyemi Samuel Tobi</h2> <p className= >Chief Technology Officer</p> <p className= > Owoyemi Samuel Tobi leads JobBridge's technology vision, product engineering, and platform innovation. He focuses on scalable systems, reliability, and meaningful user impact as JobBridge connects talent with opportunity. </p> </div> </div> </div> </section> { } <section className= > <h2 className= >Our Vision Pillars</h2> <div className= > <div className= > <h3 className= >Democratizing Opportunity</h3> <p className= > Every person deserves access to meaningful career opportunities. We're removing barriers and creating pathways for talent from all backgrounds. </p> </div> <div className= > <h3 className= >AI-First Approach</h3> <p className= > Artificial intelligence should augment human decision-making, not replace it. We're building AI tools that enhance recruiting and career discovery for everyone. </p> </div> <div className= > <h3 className= >Global Impact</h3> <p className= > Economic opportunity knows no borders. JobBridge is expanding globally to connect talent and employers across the world. </p> </div> </div> </section> { } <section className= > <div className= > <h2 className= >A note from Owoyemi</h2> <div className= > <p> When I founded JobBridge, it started with a simple belief: talent is everywhere, but opportunity is not. </p> <p> Across communities, I saw skilled people—graduates, artisans, creatives, technicians, and professionals—struggling to find meaningful opportunities, not because they lacked ability, but because they lacked access. At the same time, businesses and employers were searching for talent, yet many qualified individuals remained invisible. </p> <p> The problem was never a shortage of talent. The problem was the gap between talent and opportunity. </p> <p> JobBridge was created to close that gap. </p> <p> We believe that a person's future should not be determined by who they know, where they live, or whether they have access to the right network. Everyone deserves a fair chance to showcase their skills, discover opportunities, and build a better life. </p> <p> Our mission is to create a platform where employers can find the right people, job seekers can access the right opportunities, and professionals can continuously develop themselves through learning and training. </p> <p> Technology is helping us make this vision possible. By leveraging intelligent tools and data-driven solutions, we can connect people with opportunities faster, reduce barriers to entry, and create a more transparent and inclusive job market. </p> <p> At JobBridge, we're not just building a job platform. We're building a bridge between potential and possibility, between ambition and achievement, and between communities and economic growth. </p> <p> Because when talent meets opportunity, lives change. And when lives change, communities grow. </p> </div> </div> </section> { } <section className= > <h2 className= >Company Milestones</h2> <div className= > { } <div className= ></div> { } <div className= > {milestones.map((milestone, idx) => ( <div key={milestone.year} className={`relative flex ${idx % 2 === 0 ? 'md:flex-row' : 'md:flex-row-reverse'}`} > { } <div className={`w-full md:w-1/2 ${idx % 2 === 0 ? 'md:pr-12' : 'md:pl-12'}`}> <div className= > <div className= >{milestone.year}</div> <h3 className= >{milestone.title}</h3> <p className= >{milestone.description}</p> </div> </div> { } <div className= > <div className= ></div> </div> </div> ))} </div> </div> </section> { } <section className= > <div className= > <h2 className= > Join Our Mission </h2> <p className= > We're building something meaningful. If you're passionate about the future of work, we want to hear from you. </p> <div className= > <Link to= className= > Explore Open Roles <ArrowRight size={20} /> </Link> <Link to= className= interface Milestone year: number; title: string; description: string; const CEO: React.FC = () => const milestones: Milestone[] = [ year: 2021, title: 'Founded', description: 'Founded JobBridge with a simple idea: solve the disconnect between talent and opportunity.' , year: 2022, title: 'Market Research', description: 'Conducted market research and identified key challenges faced by job seekers and employers.' , year: 2023, title: 'Vision & Roadmap', description: 'Developed the JobBridge vision, business model, and long-term roadmap.' , year: 2024, title: 'Platform Design', description: 'Designed the platform concept and defined core features and services.' , year: 2025, title: 'Development', description: 'Began platform development and built the foundation for launch.' , year: 2026, title: 'AI Launch', description: 'Introduced AI-powered solutions and officially launched JobBridge to the public.' ]; const imgSrc = `$ import.meta.env.BASE_URL images/owoyemi-samuel-tobi.jpeg`; const ImageOrPlaceholder: React.FC = () => ( (e.target as HTMLImageElement).style.display = 'none'; /> ); return ( Welcome from our CTO We're building the infrastructure for the future of work JobBridge is more than a platform—it's a movement to reimagine how talent and opportunity connect in a rapidly changing world. (e.target as HTMLImageElement).src = ''; /> Owoyemi Samuel Tobi Chief Technology Officer Owoyemi Samuel Tobi leads JobBridge's technology vision, product engineering, and platform innovation. He focuses on scalable systems, reliability, and meaningful user impact as JobBridge connects talent with opportunity. Our Vision Pillars Democratizing Opportunity Every person deserves access to meaningful career opportunities. We're removing barriers and creating pathways for talent from all backgrounds. AI-First Approach Artificial intelligence should augment human decision-making, not replace it. We're building AI tools that enhance recruiting and career discovery for everyone. Global Im"
    },
    {
      "file": "Contact.tsx",
      "path": "src/pages/Contact.tsx",
      "title": "Get in Touch",
      "text": "Get in Touch Office Address Phone Email Follow Us Get in Touch {submitted ? ( Message sent! We'll get back to you as soon as possible. Name Email Subject {subj.label} Message Send Message We typically respond within 24 hours Office Address No 4, Phenol Crystal Street Koomi Rd, Saki, Oyo State Live Chat / Call jobbridgesupport@gmail.com min-h-screen bg-white flex flex-col > <div className= > { } <div> <h1 className= >Get in Touch</h1> {submitted ? ( <div className= > <p className= >Message sent!</p> <p className= > We'll get back to you as soon as possible. </p> </div> ) : ( <form onSubmit={handleSubmit} className= > { } <div> <label htmlFor= block text-sm font-medium text-gray-900 mb-2 id= name= value={formData.name} onChange={handleInputChange} required className= placeholder= /> </div> { } <div> <label htmlFor= block text-sm font-medium text-gray-900 mb-2 id= name= value={formData.email} onChange={handleInputChange} required className= placeholder= /> </div> { } <div> <label htmlFor= block text-sm font-medium text-gray-900 mb-2 name= value={formData.subject} onChange={handleInputChange} className= > {subjects.map((subj) => ( <option key={subj.value} value={subj.value}> {subj.label} </option> ))} </select> </div> { } <div> <label htmlFor= block text-sm font-medium text-gray-900 mb-2 name= value={formData.message} onChange={handleInputChange} required rows={6} className= placeholder= /> </div> { } <button type= className= > Send Message </button> </form> )} { } <p className= > We typically respond within 24 hours </p> </div> { } <div> { } <div className= > <img src= className= /> </div> { } <div className= > { } <div> <h2 className= >Office Address</h2> <div className= > <MapPin className= /> <div> <p className= >No 4, Phenol Crystal Street</p> <p className= >Koomi Rd, Saki, Oyo State</p> </div> </div> </div> { } <div> <h2 className= >Phone</h2> <div className= > <Phone className= /> <div> <a href= rel= className= > 09136171354 </a> <p className= >Live Chat / Call</p> </div> </div> </div> { } <div> <h2 className= >Email</h2> <div className= > <Mail className= /> <div> <a href= className= > jobbridgesupport@gmail.com </a> </div> </div> </div> { } <div> <h2 className= >Follow Us</h2> <div className= > <a href= rel= className= aria-label= > <Twitter className= /> </a> <a href= rel= className= aria-label= > <Instagram className= /> </a> <a href= rel= className= aria-label= > <Facebook className= type SubjectType = 'general' | 'technical' | 'partnership' | 'media' | 'other'; const [formData, setFormData] = useState( name: '', email: '', subject: 'general' as SubjectType, message: '', ); const [submitted, setSubmitted] = useState(false); const subjects: value: SubjectType; label: string [] = [ value: 'general', label: 'General' , value: 'technical', label: 'Technical Support' , value: 'partnership', label: 'Partnership' , value: 'media', label: 'Media' , value: 'other', label: 'Other' , ]; const handleInputChange = ( e: React.ChangeEvent ) => const name, value = e.target; setFormData((prev) => ( ...prev, [name]: value, )); ; const handleSubmit = async (e: React.FormEvent) => e.preventDefault(); if (!checkRateLimit('contact_form', 3, 30000)) return; if (!formData.name.trim() || !formData.message.trim()) return; if (!isValidEmail(formData.email)) return; try await supabase.from('contact_messages').insert( name: sanitize(formData.name), email: sanitize(formData.email), subject: formData.subject, message: sanitize(formData.message), ); catch setSubmitted(true); setFormData( name: '', email: '', subject: 'general', message: '' ); setTimeout(() => setSubmitted(false), 3000); ; return ( Get in Touch submitted ? ( Message sent! We'll get back to you as soon as possible. ) : ( Name Email Subject subjects.map((subj) => ( subj.label )) Message Send Message ) We typically respond within 24 hours Office Address No 4, Phenol Crystal Street Koomi Rd, Saki, Oyo State Phone 09136171354 Live Chat / Call Email jobbridgesupport@gmail.com Follow Us );"
    },
    {
      "file": "EmailLogsAdmin.tsx",
      "path": "src/pages/EmailLogsAdmin.tsx",
      "title": "EmailLogsAdmin",
      "text": "Email Admin Email Admin Email Logs Admin Email Admin Please sign in to access the email admin dashboard. Email Admin {adminError} Email Logs Admin Logs Queue Refresh Process Queue {loading && Error: {error} Error: {queueError} {log.email} {log.type} {log.subject || ''} {log.status || ''} {log.resend_id || ''} {log.created_at?.replace('T', ' ').replace('Z', '') || ''} Resend Last Error Last Attempted {item.email} {item.type || ''} {item.attempts ?? 0} {item.status || 'pending'} {item.last_error || ''} {item.last_attempted_at?.replace('T', ' ').replace('Z', '') || ''} {item.created_at?.replace('T', ' ').replace('Z', '') || ''} Retry Delete > <h2 className= >Email Admin</h2> <p className= >Please sign in to access the email admin dashboard.</p> </div> ); } if (adminError) { return ( <div className= > <h2 className= >Email Admin</h2> <p className= >{adminError}</p> </div> ); } return ( <div className= > <div className= > <h2 className= >Email Logs Admin</h2> <button type= onClick={() => setTab('logs')} className={`px-3 py-2 rounded ${tab === 'logs' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700'}`} > Logs </button> <button type= onClick={() => setTab('queue')} className={`px-3 py-2 rounded ${tab === 'queue' ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700'}`} > Queue </button> <button type= onClick={() => (tab === 'logs' ? loadLogs() : loadQueue())} className= > Refresh </button> <button type= onClick={() => handleProcessQueue()} className= > Process Queue </button> </div> {loading && <div className= >Loading…</div>} {tab === 'logs' && error && <div className= >Error: {error}</div>} {tab === 'queue' && queueError && <div className= >Error: {queueError}</div>} {tab === 'logs' ? ( <div className= > <table className= > <thead className= > <tr> <th className= >ID</th> <th className= >Email</th> <th className= >Type</th> <th className= >Subject</th> <th className= >Status</th> <th className= >Resend ID</th> <th className= >Created</th> <th className= >Actions</th> </tr> </thead> <tbody> {logs.map((log) => ( <tr key={log.id} className= > <td className= >{log.id}</td> <td className= >{log.email}</td> <td className= >{log.type}</td> <td className= >{log.subject || ''}</td> <td className= >{log.status || ''}</td> <td className= >{log.resend_id || ''}</td> <td className= >{log.created_at?.replace('T', ' ').replace('Z', '') || ''}</td> <td className= > <button onClick={() => handleResend(log)} className= > Resend </button> </td> </tr> ))} </tbody> </table> </div> ) : ( <div className= > <table className= > <thead className= > <tr> <th className= >ID</th> <th className= >Email</th> <th className= >Type</th> <th className= >Attempts</th> <th className= >Status</th> <th className= >Last Error</th> <th className= >Last Attempted</th> <th className= >Created</th> <th className= >Actions</th> </tr> </thead> <tbody> {queue.map((item) => ( <tr key={item.id} className= > <td className= >{item.id}</td> <td className= >{item.email}</td> <td className= >{item.type || ''}</td> <td className= >{item.attempts ?? 0}</td> <td className= >{item.status || 'pending'}</td> <td className= >{item.last_error || ''}</td> <td className= >{item.last_attempted_at?.replace('T', ' ').replace('Z', '') || ''}</td> <td className= >{item.created_at?.replace('T', ' ').replace('Z', '') || ''}</td> <td className= > <button onClick={() => handleProcessQueue(item.id)} className= > Retry </button> <button onClick={() => handleDeleteQueueItem(item)} className= type EmailLog = id: number; email: string; type: string; subject?: string; resend_id?: string; status?: string; meta?: any; created_at?: string; ; type EmailQueueItem = id: number; email: string; type?: string; attempts?: number; last_error?: string; status?: string; last_attempted_at?: string; created_at?: string; ; const profile, isAuthenticated = useAuth(); const [tab, setTab] = useState ('logs'); const [logs, setLogs] = useState ([]); const [queue, setQueue] = useState ([]); const [loading, setLoading] = useState(false); const [error, setError] = useState (null); const [queueError, setQueueError] = useState (null); const [adminError, setAdminError] = useState (null); const functionsBase = getSupabaseFunctionsUrl(); useEffect(() => if (!isAuthenticated) return; if (!profile || profile.role !== 'admin') setAdminError('You do not have permission to access this page.'); return; setAdminError(null); if (tab === 'logs') loadLogs(); else loadQueue(); , [isAuthenticated, profile, tab]); async function loadLogs() setLoading(true); setError(null); try const data, error = await supabase .from('email_logs') .select('*') .order('created_at', ascending: false ) .limit(200); if (error) throw error; setLogs((data || []) as EmailLog[]); catch (e: any) setError(e.message || String(e)); finally setLoading(false); async function loadQueue() setLoading(true); setQueueError(null); try const data, error = await supabase .from('email_queue') .select('*') .order('created_at', ascending: false ) .limit(200); if (error) throw error; setQueue((data || []) as EmailQueueItem[]); catch (e: any) setQueueError(e.message || String(e)); finally setLoading(false); async function handleResend(log: EmailLog) if (!functionsBase) return alert('Functions URL not configured. Set VITE_SUPABASE_FUNCTIONS_URL'); const payload: any = type: log.type, email: log.email ; if (log.meta?.name) payload.name = log.meta.name; if (log.meta?.jobTitle) payload.jobTitle = log.meta.jobTitle; if (log.meta?.company) payloa"
    },
    {
      "file": "Following.tsx",
      "path": "src/pages/Following.tsx",
      "title": "Following",
      "text": "Following Manage people and communities you follow to personalize your job feed. {item.name} {item.kind} • {item.members} Follow Explore providers Go to messages max-w-4xl mx-auto px-4 py-8 bg-white border border-gray-100 rounded-2xl p-6 mb-6 flex items-center gap-3 mb-2 w-10 h-10 rounded-xl bg-primary-fixed text-primary flex items-center justify-center /> </div> <h1 className= >Following</h1> </div> <p className= >Manage people and communities you follow to personalize your job feed.</p> </div> <div className= > {SUGGESTIONS.map((item) => ( <div key={item.id} className= > <div> <p className= >{item.name}</p> <p className= >{item.kind} • {item.members}</p> </div> <button className= > Follow </button> </div> ))} </div> <div className= > <Link to= inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-outline-variant text-sm text-gray-700 hover:bg-gray-50 /> Explore providers </Link> <Link to= inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-primary text-white text-sm font-semibold hover:bg-primary-container const SUGGESTIONS = [ id: 1, name: 'Frontend Developers NG', kind: 'Community', members: '18k members' , id: 2, name: 'Product Managers Africa', kind: 'Community', members: '9k members' , id: 3, name: 'Remote Jobs Nigeria', kind: 'Topic', members: '22k members' , ]; return ( Following Manage people and communities you follow to personalize your job feed. SUGGESTIONS.map((item) => ( item.name item.kind • item.members Follow )) Explore providers Go to messages );"
    },
    {
      "file": "Games.tsx",
      "path": "src/pages/Games.tsx",
      "title": "Games",
      "text": "Audio Settings Congratulations!!!! Stage {currentStage} Complete! Congratulations!!!! {question.question} {quizPassed ? 'Stage Passed!' : 'Not Quite'} Congratulations!!!! 0 ? streakEmoji : '💤'} {streak.current} {streakLevel} day streak · Best: {streak.best} · {streak.points.toLocaleString()} pts {completedStages.length}/10 stages {musicOn ? {tier.label} {stage.completed ? '✅' : stage.unlocked ? stage.id.toString() : '🔒'} Stage {stage.id}: {stage.name} {stage.description} {stage.completed ? ( ) : stage.unlocked ? ( Audio Settings Background Music {musicOn && ( Music Volume {musicVol}% {g.label} Sound Effects {sfxOn && ( SFX Volume Congratulations!!!! You're doing well! Keep it up! Continue Back to Stages {showResult && gameComplete && ( Stage {currentStage} Complete! Solved in {formatTime(timer)} · {moves} moves · +10 pts Continue {(card.flipped || card.matched) ? card.emoji : '?'} {gameComplete && ( Continue Congratulations!!!! You're doing well! Keep it up! Continue Back to Stages {!quizFinished ? ( Question {quizIndex + 1} of {quizQuestions.length} Score: {score} {timeLeft}s {question.question} {timedOut && ( Time's up! {hasPenalty(currentStage) ? '-1 point' : 'No points'} {showAnswer && ( {quizIndex {quizPassed ? '🎉' : '😅'} {quizPassed ? 'Stage Passed!' : 'Not Quite'} {hasPenalty(currentStage) && Wrong answers lose points {!quizPassed && ( Try Again {quizPassed ? 'Next Stage' : 'Back to Stages'} Congratulations!!!! You're doing well! Keep it up! Continue Tell me about yourself mean in a job search context?', options: ['Fixing computer networks', 'Building professional relationships for opportunities', 'Attending parties only', 'Sending spam emails'], correct: 1 }, { question: 'When should you send a thank-you email after an interview?', options: ['One month later', 'Within 24 hours', 'Never', 'Only if you got the job'], correct: 1 }, { question: 'What is a in an interview?', options: ['A question about your behavior outside work', 'A question asking how you handled past situations', 'A trick question', 'A personality test'], correct: 1 }, { question: 'What does mean?', options: ['Moving to a higher floor', 'Learning new skills to advance your career', 'Working faster', 'Changing job titles'], correct: 1 }, { question: 'What is the best way to answer in a job application?', options: ['A briefcase', 'A collection of your work samples and achievements', 'A type of resume', 'A cover letter'], correct: 1 }, { question: 'What should you do before accepting a job offer?', options: ['Accept immediately', 'Review the offer, ask questions, and consider your goals', 'Ignore the details', 'Ask friends to decide'], correct: 1 }, { question: 'What is ?', options: ['Working all the time', 'Balancing professional responsibilities with personal life', 'Working from home only', 'Taking long vacations'], correct: 1 }, { question: 'What should you do during a phone screening?', options: ['Multi-task while talking', 'Be prepared, speak clearly, and ask thoughtful questions', 'Whisper so no one hears', 'Rush through the call'], correct: 1 }, { question: 'What does mean?', options: ['Going to college forever', 'Ongoing learning to improve your career skills', 'Getting a promotion', 'Changing careers'], correct: 1 }, { question: 'How should you answer ?', options: ['Checking your social media', 'Contacting people who can vouch for your work', 'A background check', 'A credit check'], correct: 1 }, { question: 'How should you handle rejection after an interview?', options: ['Give up on job searching', 'Ask for feedback and keep applying to other roles', 'Argue with the recruiter', 'Ignore it'], correct: 1 }, { question: 'What is in a career context?', options: ['A formal class', 'Guidance from an experienced professional to help you grow', 'A type of promotion', 'A training program'], correct: 1 }, { question: 'What does mean to employers?', options: ['Having a fast internet connection', 'Being self-motivated, communicative, and organized while working remotely', 'Owning a laptop', 'Working from a coffee shop'], correct: 1 }, { question: 'What should you include in a follow-up email after an interview?', options: ['Ask if you got the job', 'Thank them, reiterate your interest, and highlight a key point from the interview', 'Complain about the process', 'Send your resume again'], correct: 1 }, { question: 'What is a ?', options: ['Being friendly', 'A teachable technical ability like coding or data analysis', 'Personality trait', 'Emotional intelligence'], correct: 1 }, { question: 'How can you identify your transferable skills?', options: ['Only look at job-specific skills', 'Review your experiences and identify skills useful across different roles', 'Ignore past experience', 'Focus on education only'], correct: 1 }, { question: 'What does mean?', options: ['Jumping between job sites', 'Changing jobs frequently within short periods', 'A type of commute', 'Working multiple jobs'], correct: 1 }, { question: 'How should you address a career change in an interview?', options: ['Avoid explaining it', 'Frame it positively by connecting past experience to the new path', 'Apologize for changing careers', 'Say you disliked your old field'], correct: 1 }, { question: 'What is the best approach to answering ?', options: ['Say you never fail', 'Be honest, describe what you learned, and how you improved', 'Blame others', 'Make up a fake failure'], correct: 1 }, { question: 'What does mean for your career?', options: ['The clothes you wear', 'How you present your professional reputation and expertise', 'Your social media followers', 'Your job title'], correct: 1 }, { question: 'How should you research a company before an interview?', options: ['Just read the job description', 'Study their website, recent news, products, and company culture', 'Look at their logo', 'Ask friends about the company'], correct: 1 }, { question: 'What is a ?', options: ['A gap between buildings', 'The difference between skills you have and skills a job requires', 'A missing tool', 'A type of interview question'], correct: 1 }, { question: 'What does mean in the workplace?', options: ['Hiring people from the same background', 'Creating an environment where all employees feel valued and respected', 'Only hiring minorities', 'A legal requirement'], correct: 1 }, { question: 'What is the best way to answer ', options: ['Give a number immediately', 'Provide a range based on market research and your experience', 'Refuse to answer', 'Ask for the maximum'], correct: 1 }, ]; const TIERS = [ { label: 'Beginner', color: 'from-emerald-500 to-green-600', stages: [1, 2, 3] }, { label: 'Intermediate', color: 'from-blue-500 to-indigo-600', stages: [4, 5, 6] }, { label: 'Advanced', color: 'from-violet-500 to-purple-600', stages: [7, 8, 9] }, { label: 'Master', color: 'from-amber-500 to-orange-600', stages: [10] }, ]; const STAGES: GameStage[] = [ { id: 1, name: 'Memory Match', description: 'Match the career pairs', type: 'memory', unlocked: true, completed: false }, { id: 2, name: 'Interview Basics', description: 'Essential interview knowledge', type: 'quiz', unlocked: false, completed: false }, { id: 3, name: 'Behavioral Mastery', description: 'Tackle behavioral questions', type: 'quiz', unlocked: false, completed: false }, { id: 4, name: 'Memory Match Pro', description: 'Match the global career tiles', type: 'memory', unlocked: false, completed: false }, { id: 5, name: 'Resume & Portfolio', description: 'Craft your professional story', type: 'quiz', unlocked: false, completed: false }, { id: 6, name: 'Salary & Negotiation', description: 'Know your worth', type: 'quiz', unlocked: false, completed: false }, { id: 7, name: 'Career Strategy', description: 'Plan your career path', type: 'quiz', unlocked: false, completed: false }, { id: 8, name: 'Networking Pro', description: 'Build meaningful connections', type: 'quiz', unlocked: false, completed: false }, { id: 9, name: 'Leadership Edge', description: 'Show leadership potential', type: 'quiz', unlocked: false, completed: false }, { id: 10, name: 'Final Challenge', description: 'The ultimate interview test', type: 'quiz', unlocked: false, completed: false }, ]; function shuffle<T>(arr: T[]): T[] { const a = [...arr]; for (let i = a.length - 1; i > 0; i--) { const j = Math.floor(Math.random() * (i + 1)); [a[i], a[j]] = [a[j], a[i]]; } return a; } function loadStreak(): StreakData { try { const raw = localStorage.getItem('jobbridge_streak'); if (raw) return JSON.parse(raw); } catch {} return DEFAULT_STREAK; } function saveStreak(data: StreakData) { localStorage.setItem('jobbridge_streak', JSON.stringify(data)); } function addStreakNotification(streak: number) { try { const raw = localStorage.getItem('jobbridge_notifications'); const notifs: any[] = raw ? JSON.parse(raw) : []; notifs.unshift({ id: 'streak-' + Date.now(), type: 'system', title: streak >= 7 ? 'Hot Streak!' : 'Streak Bonus!', content: streak >= 7 ? `You're on a ${streak}-day streak! Unstoppable! Keep playing daily to maintain your streak.` : `You've earned a ${streak}-day streak! Play daily to reach new milestones.`, time: 'Just now', isRead: false, }); localStorage.setItem('jobbridge_notifications', JSON.stringify(notifs.slice(0, 50))); } catch {} } function updateStreak(): StreakData { const streak = loadStreak(); const today = new Date().toDateString(); if (streak.lastDate === today) return streak; const yesterday = new Date(Date.now() - 86400000).toDateString(); if (streak.lastDate === '' || streak.lastDate === yesterday) { streak.current += 1; } else if (streak.lastDate !== today) { streak.current = 1; } if (streak.current > streak.best) streak.best = streak.current; streak.lastDate = today; streak.points += 10; streak.gamesPlayed += 1; saveStreak(streak); addStreakNotification(streak.current); return streak; } function loadStageProgress(): number[] { try { const raw = localStorage.getItem('jobbridge_stage_progress'); if (raw) return JSON.parse(raw); } catch {} return []; } function saveStageProgress(completed: number[]) { localStorage.setItem('jobbridge_stage_progress', JSON.stringify(completed)); } const [musicOn, setMusicOn] = useState(false); const [showSettings, setShowSettings] = useState(false); const [sfxOn, setSfxOn] = useState(() => sfxEnabled); const [musicVol, setMusicVol] = useState(() => Math.round(musicVolume * 100)); const [sfxVol, setSfxVol] = useState(() => Math.round(sfxVolume * 100)); const [genre, setGenre] = useState(() => selectedGenre); const [currentScreen, setCurrentScreen] = useState<'stages' | 'memory' | 'quiz'>('stages'); const [currentStage, setCurrentStage] = useState(1); const [completedStages, setCompletedStages] = useState<number[]>(() => loadStageProgress()); const [quizQuestions, setQuizQuestions] = useState<QuizQuestion[]>([]); const [quizIndex, setQuizIndex] = useState(0); const [score, setScore] = useState(0); const [quizFinished, setQuizFinished] = useState(false); const [quizPassed, setQuizPassed] = useState(false); const [selectedAnswer, setSelectedAnswer] = useState<number | null>(null); const [showAnswer, setShowAnswer] = useState(false); const [timeLeft, setTimeLeft] = useState(15); const [timedOut, setTimedOut] = useState(false); const [showCongrats, setShowCongrats] = useState(false); const [pendingStageId, setPendingStageId] = useState<number | null>(null); const [cards, setCards] = useState<MemoryCard[]>(() => shuffle([...CARD_PAIRS, ...CARD_PAIRS].map((pair, i) => ({ id: i, emoji: pair.emoji, label: pair.label, flipped: false, matched: false, }))) ); const [flippedIds, setFlippedIds] = useState<number[]>([]); const [moves, setMoves] = useState(0); const [matchedCount, setMatchedCount] = useState(0); const [gameComplete, setGameComplete] = useState(false); const [timer, setTimer] = useState(0); const [streak, setStreak] = useState<StreakData>(loadStreak); const [showResult, setShowResult] = useState(false); const [showStreakPopup, setShowStreakPopup] = useState(false); const [bestScore, setBestScore] = useState<number>(() => { try { return parseInt(localStorage.getItem('jobbridge_best_moves') || '999'); } catch { return 999; } }); const memoryCardSet = useRef(CARD_PAIRS); const stages = STAGES.map(s => ({ ...s, unlocked: completedStages.includes(s.id - 1) || s.id === 1, completed: completedStages.includes(s.id), })); const updateMusicVol = (v: number) => { setMusicVol(v); musicVolume = v / 100; if (musicGain) { try { musicGain.gain.setValueAtTime(musicVolume, getMusicCtx().currentTime); } catch {} } saveAudioSettings(musicVolume, sfxVolume, sfxEnabled, genre); }; const updateSfxVol = (v: number) => { setSfxVol(v); sfxVolume = v / 100; saveAudioSettings(musicVolume, sfxVolume, sfxEnabled, genre); }; const toggleSfx = () => { sfxEnabled = !sfxEnabled; setSfxOn(sfxEnabled); saveAudioSettings(musicVolume, sfxVolume, sfxEnabled, genre); }; const changeGenre = (g: string) => { selectedGenre = g; setGenre(g); saveAudioSettings(musicVolume, sfxVolume, sfxEnabled, g); if (musicOn) { stopBgMusic(); setTimeout(() => startBgMusic(), 50); } }; useEffect(() => { if (musicOn) startBgMusic(); else stopBgMusic(); return () => { stopBgMusic(); if (musicCtx) { try { musicCtx.close(); musicCtx = null; } catch {} } if (audioCtx) { try { audioCtx.close(); audioCtx = null; } catch {} } }; }, [musicOn, genre]); useEffect(() => { if (gameComplete || currentScreen !== 'memory') return; const interval = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(interval); }, [gameComplete, currentScreen]); useEffect(() => { if (flippedIds.length !== 2) return; const [first, second] = flippedIds; const card1 = cards.find(c => c.id === first)!; const card2 = cards.find(c => c.id === second)!; const isMatch = card1.label === card2.label && first !== second; setTimeout(() => { setCards(prev => prev.map(c => c.id === first || c.id === second ? { ...c, flipped: isMatch ? true : false, matched: isMatch ? true : c.matched } : c ) ); setFlippedIds([]); if (isMatch) { playMatchSound(); const newMatched = matchedCount + 1; setMatchedCount(newMatched); if (newMatched === memoryCardSet.current.length) { setGameComplete(true); playWinSound(); const newStreak = updateStreak(); setStreak(newStreak); setShowResult(true); setTimeout(() => setShowStreakPopup(true), 800); if (moves + 1 < bestScore) { setBestScore(moves + 1); try { localStorage.setItem('jobbridge_best_moves', String(moves + 1)); } catch {} } } } else { playMismatchSound(); } }, 600); }, [flippedIds, cards, moves, matchedCount, bestScore, currentStage]); const handleCardClick = useCallback((cardId: number) => { if (gameComplete) return; if (flippedIds.length >= 2) return; const card = cards.find(c => c.id === cardId); if (!card || card.flipped || card.matched) return; setCards(prev => prev.map(c => (c.id === cardId ? { ...c, flipped: true } : c))); setFlippedIds(prev => [...prev, cardId]); setMoves(m => m + 1); playFlipSound(); }, [gameComplete, flippedIds, cards]); const resetMemoryGame = () => { setCards(shuffle([...CARD_PAIRS, ...CARD_PAIRS].map((pair, i) => ({ id: i, emoji: pair.emoji, label: pair.label, flipped: false, matched: false, })))); setFlippedIds([]); setMoves(0); setMatchedCount(0); setGameComplete(false); setTimer(0); setShowResult(false); }; const startMemoryStage = (stageId: number) => { const pairs = stageId === 4 ? CARD_PAIRS_2 : CARD_PAIRS; memoryCardSet.current = pairs; setCards(shuffle([...pairs, ...pairs].map((pair, i) => ({ id: i, emoji: pair.emoji, label: pair.label, flipped: false, matched: false, })))); setFlippedIds([]); setMoves(0); setMatchedCount(0); setGameComplete(false); setTimer(0); setShowResult(false); setCurrentStage(stageId); setCurrentScreen('memory'); }; const handleStagePassed = (stageId: number) => { setPendingStageId(stageId); setShowCongrats(true); }; const confirmCongrats = () => { if (pendingStageId !== null) { const newCompleted = [...new Set([...completedStages, pendingStageId])]; setCompletedStages(newCompleted); saveStageProgress(newCompleted); } setShowCongrats(false); setPendingStageId(null); setCurrentScreen('stages'); }; const getTimeLimit = (stageId: number) => stageId <= 3 ? 15 : stageId <= 6 ? 12 : stageId <= 9 ? 10 : 8; const getPassThreshold = (stageId: number, total: number) => stageId <= 3 ? Math.ceil(total * 0.6) : stageId <= 6 ? Math.ceil(total * 0.7) : stageId <= 9 ? Math.ceil(total * 0.75) : Math.ceil(total * 0.8); const hasPenalty = (stageId: number) => stageId >= 7; const startQuizStage = (stageId: number) => { const pool = shuffle([...QUIZ_POOL]); const count = stageId <= 3 ? 5 : stageId <= 6 ? 6 : stageId <= 9 ? 8 : 10; setQuizQuestions(pool.slice(0, count)); setQuizIndex(0); setScore(0); setQuizFinished(false); setQuizPassed(false); setSelectedAnswer(null); setShowAnswer(false); setTimeLeft(getTimeLimit(stageId)); setTimedOut(false); setCurrentStage(stageId); setCurrentScreen('quiz'); }; useEffect(() => { if (currentScreen !== 'quiz' || quizFinished || showAnswer) return; const t = getTimeLimit(currentStage); setTimeLeft(t); const interval = setInterval(() => { setTimeLeft(prev => { if (prev <= 1) { clearInterval(interval); setTimedOut(true); setShowAnswer(true); setSelectedAnswer(-1); if (hasPenalty(currentStage)) setScore(s => Math.max(0, s - 1)); playWrongSound(); return 0; } return prev - 1; }); }, 1000); return () => clearInterval(interval); }, [currentScreen, quizIndex, quizFinished, showAnswer, currentStage]); const handleAnswer = (optionIndex: number) => { if (showAnswer) return; setSelectedAnswer(optionIndex); setShowAnswer(true); if (optionIndex === quizQuestions[quizIndex].correct) { setScore(s => s + 1); playCorrectSound(); } else { if (hasPenalty(currentStage)) setScore(s => Math.max(0, s - 1)); playWrongSound(); } }; const nextQuizQuestion = () => { if (quizIndex < quizQuestions.length - 1) { setQuizIndex(i => i + 1); setSelectedAnswer(null); setShowAnswer(false); setTimedOut(false); setTimeLeft(getTimeLimit(currentStage)); } else { const passThreshold = getPassThreshold(currentStage, quizQuestions.length); const passed = score >= passThreshold; setQuizFinished(true); setQuizPassed(passed); if (passed) playStageCompleteSound(); } }; const formatTime = (s: number) => `${Math.floor(s / 60).toString().padStart(2, '0')}:${(s % 60).toString().padStart(2, '0')}`; const streakLevel = streak.current >= 30 ? 'Legendary' : streak.current >= 14 ? 'Unstoppable' : streak.current >= 7 ? 'On Fire' : streak.current >= 3 ? 'Rising' : streak.current >= 1 ? 'Getting Started' : 'Not Started'; const streakEmoji = streak.current >= 30 ? '👑' : streak.current >= 14 ? '🔥🔥' : streak.current >= 7 ? '🔥' : streak.current >= 3 ? '⭐' : streak.current >= 1 ? '✨' : '💤'; if (currentScreen === 'stages') { return ( <div className= > <Header /> <PageHero compact title= Complete stages to unlock new challenges /> <div className= > { } <div className= > <div className= > <div className= > <div className={`w-12 h-12 rounded-xl flex items-center justify-center text-2xl ${ streak.current > 0 ? 'bg-gradient-to-br from-amber-400 to-orange-500 text-white' : 'bg-gray-100 text-gray-400' }`}> {streak.current > 0 ? streakEmoji : '💤'} </div> <div> <div className= > <span className= >{streak.current}</span> <span className= >{streakLevel}</span> </div> <p className= >day streak · Best: {streak.best} · {streak.points.toLocaleString()} pts</p> </div> </div> </div> <div className= > <div className= style={{ width: `${Math.min((streak.current / 30) * 100, 100)}%` }} /> </div> </div> { } <div className= > <div className= > <Brain className= /> <span>{completedStages.length}/10 stages</span> </div> <div className= > <button onClick={() => setShowSettings(true)} className= title= > <Settings className= /> </button> <button onClick={() => setMusicOn(!musicOn)} className={`flex items-center gap-2 px-4 py-2 rounded-xl text-sm font-medium transition ${ musicOn ? 'bg-blue-100 text-blue-700' : 'bg-gray-100 text-gray-500' }`}> {musicOn ? <Music className= /> : <VolumeX className= />} {musicOn ? 'Music On' : 'Music Off'} </button> </div> </div> { } <div className= > {TIERS.map(tier => { const tierStages = stages.filter(s => tier.stages.includes(s.id)); if (tierStages.length === 0) return null; return ( <div key={tier.label}> <div className={`inline-block px-3 py-1 rounded-full text-xs font-bold text-white bg-gradient-to-r ${tier.color} mb-2`}> {tier.label} </div> <div className= > {tierStages.map(stage => ( <div key={stage.id} className={`rounded-xl border p-4 transition ${ stage.completed ? 'bg-emerald-50 border-emerald-200' : stage.unlocked ? 'bg-white border-gray-200 cursor-pointer hover:shadow-md' : 'bg-gray-50 border-gray-200 opacity-60' }`} onClick={() => { if (!stage.unlocked || stage.completed) return; if (stage.type === 'memory') startMemoryStage(stage.id); else startQuizStage(stage.id); }} > <div className= > <div className= > <div className={`w-10 h-10 rounded-xl flex items-center justify-center text-lg ${ stage.completed ? 'bg-emerald-100 text-emerald-600' : stage.unlocked ? 'bg-blue-100 text-blue-600' : 'bg-gray-100 text-gray-400' }`}> {stage.completed ? '✅' : stage.unlocked ? stage.id.toString() : '🔒'} </div> <div> <p className={`font-semibold ${stage.completed ? 'text-emerald-800' : 'text-gray-900'}`}> Stage {stage.id}: {stage.name} </p> <p className= >{stage.description}</p> </div> </div> <div className= > {stage.completed ? ( <CheckCircle className= /> ) : stage.unlocked ? ( <ChevronRight className= /> ) : ( <Lock className= /> )} </div> </div> </div> ))} </div> </div> ); })} </div> </div> { } {showSettings && ( <div className= > <div className= onClick={() => setShowSettings(false)} /> <div className= > <div className= > <h3 className= >Audio Settings</h3> <button onClick={() => setShowSettings(false)} className= >&times;</button> </div> <div className= > <div className= > <div className= > <Music className={`w-5 h-5 ${musicOn ? 'text-blue-600' : 'text-gray-400'}`} /> <span className= >Background Music</span> </div> <button onClick={() => setMusicOn(!musicOn)} className={`relative w-11 h-6 rounded-full transition-colors ${musicOn ? 'bg-blue-600' : 'bg-gray-300'}`}> <div className={`absolute top-0.5 w-5 h-5 rounded-full bg-white shadow transition-transform ${musicOn ? 'translate-x-5' : 'translate-x-0.5'}`} /> </button> </div> {musicOn && ( <> <div> <label className= >Music Volume <span>{musicVol}%</span></label> <input type= value={musicVol} onChange={e => updateMusicVol(+e.target.value)} className= /> </div> <div> <label className= >Genre</label> <div className= > {GENRES.map(g => ( <button key={g.id} onClick={() => changeGenre(g.id)} className={`flex flex-col items-center gap-1 py-2 px-1 rounded-xl text-xs font-medium transition ${ genre === g.id ? 'bg-blue-100 text-blue-700 ring-2 ring-blue-400' : 'bg-gray-50 text-gray-500 hover:bg-gray-100' }`}> <span className= >{g.icon}</span> {g.label} </button> ))} </div> </div> </> )} <div className= > <div className= > <div className= > <Zap className={`w-5 h-5 ${sfxOn ? 'text-amber-500' : 'text-gray-400'}`} /> <span className= >Sound Effects</span> </div> <button onClick={toggleSfx} className={`relative w-11 h-6 rounded-full transition-colors ${sfxOn ? 'bg-blue-600' : 'bg-gray-300'}`}> <div className={`absolute top-0.5 w-5 h-5 rounded-full bg-white shadow transition-transform ${sfxOn ? 'translate-x-5' : 'translate-x-0.5'}`} /> </button> </div> {sfxOn && ( <div> <label className= >SFX Volume <span>{sfxVol}%</span></label> <input type= value={sfxVol} onChange={e => updateSfxVol(+e.target.value)} className= /> </div> )} </div> </div> </div> </div> )} { } {showCongrats && ( <div className= > <div className= onClick={confirmCongrats} /> <div className= > <div className= >🎉</div> <h2 className= >Congratulations!!!!</h2> <p className= >You're doing well! Keep it up!</p> <button onClick={confirmCongrats} className= > Continue </button> </div> </div> )} <BottomNav /> </div> ); } if (currentScreen === 'memory') { return ( <div className= > <Header /> <PageHero compact title={`Stage ${currentStage}: ${STAGES[currentStage - 1]?.name || 'Memory Match'}`} subtitle= images={HERO_CAROUSELS.games} imageAlt= /> <div className= > { } <button onClick={() => setCurrentScreen('stages')} className= > <ChevronRight className= /> Back to Stages </button> { } <div className= > {[ { label: 'Time', value: formatTime(timer), icon: Clock, color: 'from-cyan-500 to-blue-600' }, { label: 'Moves', value: moves.toString(), icon: Zap, color: 'from-violet-500 to-purple-600' }, { label: 'Matched', value: `${matchedCount}/${memoryCardSet.current.length}`, icon: CheckCircle, color: 'from-emerald-500 to-green-600' }, { label: 'Best', value: bestScore < 999 ? `${bestScore}` : '—', icon: Trophy, color: 'from-amber-500 to-orange-600' }, ].map(s => ( <div key={s.label} className={`bg-gradient-to-br ${s.color} rounded-xl p-3 text-center`}> <s.icon className= /> <p className= >{s.value}</p> <p className= >{s.label}</p> </div> ))} </div> { } <div className= > {showResult && gameComplete && ( <div className= > <div className= >🎉</div> <h3 className= >Stage {currentStage} Complete!</h3> <p className= > Solved in {formatTime(timer)} · {moves} moves · +10 pts </p> <button onClick={() => handleStagePassed(currentStage)} className= > Continue </button> </div> )} <div className= > {cards.map(card => ( <button key={card.id} onClick={() => handleCardClick(card.id)} disabled={card.flipped || card.matched || gameComplete} className={`aspect-square rounded-xl text-2xl sm:text-3xl flex items-center justify-center transition-all duration-300 cursor-pointer ${ card.matched ? 'bg-emerald-50 border border-emerald-300 scale-95 opacity-70' : card.flipped ? 'bg-white border-2 border-blue-400 shadow-md scale-100' : 'bg-gray-50 border border-gray-200 hover:bg-gray-100 hover:border-gray-300 hover:shadow-md text-gray-400' }`} > {(card.flipped || card.matched) ? card.emoji : '?'} </button> ))} </div> </div> <div className= > <button onClick={resetMemoryGame} className= > <RotateCcw className= /> Restart </button> {gameComplete && ( <button onClick={() => handleStagePassed(currentStage)} className= > Continue </button> )} </div> </div> { } {showCongrats && ( <div className= > <div className= onClick={confirmCongrats} /> <div className= > <div className= >🎉</div> <h2 className= >Congratulations!!!!</h2> <p className= >You're doing well! Keep it up!</p> <button onClick={confirmCongrats} className= > Continue </button> </div> </div> )} <BottomNav /> </div> ); } if (currentScreen === 'quiz') { const question = quizQuestions[quizIndex]; const passThreshold = Math.ceil(quizQuestions.length * 0.6); const progress = ((quizIndex) / quizQuestions.length) * 100; return ( <div className= > <Header /> <PageHero compact title={`Stage ${currentStage}: ${STAGES[currentStage - 1].name}`} subtitle= images={HERO_CAROUSELS.games} imageAlt= /> <div className= > <button onClick={() => setCurrentScreen('stages')} className= > <ChevronRight className= /> Back to Stages </button> {!quizFinished ? ( <> { } <div className= > <div className= > <span className= >Question {quizIndex + 1} of {quizQuestions.length}</span> <span className= >Score: {score}</span> </div> <div className= > <div className= style={{ width: `${progress}%` }} /> </div> { } <div className= > <Clock className={`w-4 h-4 ${timeLeft <= 3 ? 'text-red-500' : 'text-gray-400'}`} /> <div className= > <div className={`h-full rounded-full transition-all duration-1000 ${ timeLeft <= 3 ? 'bg-red-500' : timeLeft <= 6 ? 'bg-amber-500' : 'bg-blue-500' }`} style={{ width: `${(timeLeft / getTimeLimit(currentStage)) * 100}%` }} /> </div> <span className={`text-xs font-bold ${timeLeft <= 3 ? 'text-red-500' : 'text-gray-500'}`}> {timeLeft}s </span> </div> </div> { } <div className= > <h3 className= >{question.question}</h3> {timedOut && ( <div className= > <span className= >Time's up! {hasPenalty(currentStage) ? '-1 point' : 'No points'}</span> </div> )} <div className= > {question.options.map((opt, i) => { let btnClass = 'border-gray-200 hover:bg-gray-50 text-gray-900'; if (showAnswer) { if (i === question.correct) btnClass = 'border-emerald-400 bg-emerald-50 text-emerald-800'; else if (i === selectedAnswer) btnClass = 'border-red-400 bg-red-50 text-red-800'; else btnClass = 'border-gray-200 opacity-60'; } else if (selectedAnswer === i) { btnClass = 'border-blue-400 bg-blue-50'; } return ( <button key={i} onClick={() => handleAnswer(i)} disabled={showAnswer} className={`w-full text-left p-4 rounded-xl border transition ${btnClass}`}> <span className= >{opt}</span> </button> ); })} </div> {showAnswer && ( <button onClick={nextQuizQuestion} className= > {quizIndex < quizQuestions.length - 1 ? 'Next Question' : 'See Results'} </button> )} </div> </> ) : ( <div className= > <div className= >{quizPassed ? '🎉' : '😅'}</div> <h3 className= > {quizPassed ? 'Stage Passed!' : 'Not Quite'} </h3> <p className= > You scored {score}/{quizQuestions.length} {quizPassed ? ' — well done!' : ` — need ${passThreshold}/${quizQuestions.length} to pass.`} </p> {hasPenalty(currentStage) && <p className= >Wrong answers lose points</p>} <div className= > <div className={`h-full rounded-full transition-all duration-500 ${ quizPassed ? 'bg-emerald-500' : 'bg-red-400' }`} style={{ width: `${(score / quizQuestions.length) * 100}%` }} /> </div> <div className= > {!quizPassed && ( <button onClick={() => startQuizStage(currentStage)} className= > Try Again </button> )} <button onClick={() => quizPassed ? handleStagePassed(currentStage) : setCurrentScreen('stages')} className={`flex-1 py-3 rounded-xl font-semibold transition ${ quizPassed ? 'bg-gradient-to-r from-emerald-500 to-green-600 text-white hover:from-emerald-600 hover:to-green-700' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`}> {quizPassed ? 'Next Stage' : 'Back to Stages'} </button> </div> </div> )} </div> { } {showCongrats && ( <div className= > <div className= onClick={confirmCongrats} /> <div className= > <div className= >🎉</div> <h2 className= >Congratulations!!!!</h2> <p className= >You're doing well! Keep it up!</p> <button onClick={confirmCongrats} className= interface StreakData current: number; best: number; lastDate: string; points: number; gamesPlayed: number; interface MemoryCard id: number; emoji: string; label: string; flipped: boolean; matched: boolean; interface QuizQuestion question: string; options: string[]; correct: number; interface GameStage id: number; name: string; description: string; type: 'memory' | 'quiz'; unlocked: boolean; completed: boolean; const DEFAULT_STREAK: StreakData = current: 0, best: 0, lastDate: '', points: 0, gamesPlayed: 0 ; let musicVolume = 0.06; let sfxVolume = 0.15; let sfxEnabled = true; let selectedGenre = 'jazz'; function loadAudioSettings() try const raw = localStorage.getItem('jobbridge_audio_settings'); if (raw) const s = JSON.parse(raw); if (s.musicVolume !== undefined) musicVolume = s.musicVolume; if (s.sfxVolume !== undefined) sfxVolume = s.sfxVolume; if (s.sfxEnabled !== undefined) sfxEnabled = s.sfxEnabled; if (s.genre !== undefined) selectedGenre = s.genre; catch function saveAudioSettings(mv: number, sv: number, se: boolean, genre?: string) try localStorage.setItem('jobbridge_audio_settings', JSON.stringify( musicVolume: mv, sfxVolume: sv, sfxEnabled: se, genre: genre || selectedGenre )); catch loadAudioSettings(); interface GenreDef id: string; label: string; icon: string; oscType: OscillatorType; chords: number[][]; bassNotes: number[]; bpm: number; volume: number; style: 'chord' | 'arpeggio' | 'broken'; const GENRES: GenreDef[] = [ id: 'jazz', label: 'Jazz', icon: '🎷', oscType: 'triangle', chords: [ [261.63, 329.63, 392.00, 493.88], [220.00, 329.63, 392.00, 440.00], [293.66, 349.23, 440.00, 523.25], [196.00, 293.66, 392.00, 466.16], ], bassNotes: [130.81, 110.00, 146.83, 98.00], bpm: 60, volume: 0.025, style: 'chord', , id: 'lofi', label: 'Lo-fi', icon: '☕', oscType: 'sine', chords: [ [261.63, 329.63, 392.00], [293.66, 369.99, 440.00], [329.63, 392.00, 493.88], [220.00, 329.63, 392.00], ], bassNotes: [130.81, 146.83, 164.81, 110.00], bpm: 55, volume: 0.03, style:"
    },
    {
      "file": "Home.tsx",
      "path": "src/pages/Home.tsx",
      "title": "Home",
      "text": "Your career bridge to what's next {ad.title} Why professionals choose JobBridge {title} Featured Jobs {job.title} Professionals who found their path {name} Trusted by top talent & teams Ready to find your next opportunity? 24,500+ jobs actively hiring now Your career bridge to what's AI-powered job matching, verified employers, and real-time talent search — all in one professional network. Find Jobs Hire Talent AI Resume Search Jobs {(ad.package === 'featured' || ad.is_featured) && ( Featured {ad.title} {ad.location} {ad.description && ( {ad.description} {ad.package === 'featured' ? 'Premium Plan' : ad.package === 'monthly' ? 'Monthly' : 'Weekly'} View advert Why professionals choose JobBridge Built for the modern workforce Featured Jobs Latest opportunities from top employers View all Loading featured jobs... ) : jobsError || homeJobs.length === 0 ? ( {jobsError || 'No jobs available at the moment'} Check back soon for new opportunities Browse all jobs {initials} {badge && ( {job.title} {job.company} · {job.location} {salaryDisplay} View job Professionals who found their path Join thousands of professionals who landed their dream roles {img ? ( n[0]).slice(0, 2).join('')} Hired via JobBridge Trusted by top talent & teams {role} at {company} Ready to find your next opportunity? Join over 2 million professionals already using JobBridge to advance their careers. Go to Payment View Premium Plans © 2026 JobBridge. All rights reserved. className={`absolute inset-0 w-full h-full object-cover transition-opacity duration-700 ${ i === idx ? 'opacity-100' : 'opacity-0' }`} /> ))} </div> ); } const { openModal } = useModal(); const navigate = useNavigate(); const [homeJobs, setHomeJobs] = useState<any[]>([]); const [jobsLoading, setJobsLoading] = useState(true); const [jobsError, setJobsError] = useState(''); useEffect(() => { let cancelled = false; (async () => { try { setJobsLoading(true); const jobs = await fetchJobs(); if (cancelled) return; setHomeJobs(jobs.slice(0, 4)); setJobsError(''); } catch (e) { console.warn('[Home] featured jobs failed to load:', e); if (!cancelled) setJobsError('Unable to load featured jobs'); } finally { if (!cancelled) setJobsLoading(false); } })(); return () => { cancelled = true; }; }, []); const [spotlightAds, setSpotlightAds] = useState<Advertisement[]>([]); const [spotlightLoading, setSpotlightLoading] = useState(true); useEffect(() => { let cancelled = false; (async () => { try { const ads = await fetchPublicAdvertisements(); if (cancelled) return; const featuredAds = ads.filter( (ad) => (ad.is_featured || ad.package === 'featured') && ad.payment_status === 'paid', ); const sorted = [...featuredAds].sort((a, b) => { const fd = (b.is_featured ? 1 : 0) - (a.is_featured ? 1 : 0); if (fd !== 0) return fd; return new Date(b.created_at || 0).getTime() - new Date(a.created_at || 0).getTime(); }); setSpotlightAds(sorted); } catch (e) { console.warn('[Home] spotlight ads failed to load:', e); } finally { if (!cancelled) setSpotlightLoading(false); } })(); return () => { cancelled = true; }; }, []); const handleSpotlightView = (ad: Advertisement) => { incrementAdvertisementViews(ad.id); incrementAdvertisementClicks(ad.id); }; return ( <div className= > <Header /> { } <section className= style={{ perspective: '1000px' }}> <FloatingDecorations className= /> { } <div className= > <video className= autoPlay muted loop playsInline disablePictureInPicture poster= https: { } <img src= Professional workspace absolute inset-0 w-full h-full object-cover opacity-30 absolute inset-0 bg-gradient-to-br from-blue-900/75 via-blue-800/50 to-blue-900/75 pointer-events-none z-10 absolute inset-0 opacity-10 pointer-events-none z-10 absolute top-10 left-10 w-72 h-72 rounded-full bg-white blur-3xl absolute bottom-0 right-0 w-96 h-96 rounded-full bg-blue-300 blur-3xl relative max-w-7xl mx-auto px-4 sm:px-6 py-20 lg:py-28 z-20 grid lg:grid-cols-2 gap-12 items-center inline-flex items-center gap-2 bg-white/10 border border-white/20 rounded-full px-4 py-1.5 text-sm text-blue-100 mb-6 animate-float w-2 h-2 rounded-full bg-emerald-400 animate-pulse text-4xl sm:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6 preserve-3d >next</span> </h1> <p className= style={{ transform: 'translateZ(20px)' }}> AI-powered job matching, verified employers, and real-time talent search — all in one professional network. </p> <div className= style={{ transform: 'translateZ(30px)' }}> <Link to= inline-flex items-center justify-center gap-2 bg-white text-blue-700 font-semibold px-6 py-3 rounded-xl hover:bg-blue-50 transition-all hover:shadow-xl shadow-lg /> Find Jobs </Link> <Link to= inline-flex items-center justify-center gap-2 bg-blue-600 text-white font-semibold px-6 py-3 rounded-xl hover:bg-blue-500 transition-all hover:shadow-xl border border-blue-500 /> Hire Talent </Link> <Link to= inline-flex items-center justify-center gap-2 bg-transparent text-white font-semibold px-6 py-3 rounded-xl hover:bg-white/10 transition-all hover:shadow-xl border border-white/30 /> AI Resume </Link> </div> </div> <div className= > <HeroSlides /> </div> </div> </div> </section> { } <section className= > <div className= > <div className= > <div className= > <Search className= /> <input className= Job title, keyword, or company flex-1 flex items-center gap-3 bg-gray-50 rounded-xl px-4 py-3 w-4 h-4 text-gray-400 shrink-0 flex-1 bg-transparent text-sm text-gray-700 outline-none placeholder-gray-400 /> </div> <Link to= flex items-center justify-center gap-2 bg-blue-700 text-white font-semibold px-6 py-3 rounded-xl hover:bg-blue-800 transition-colors whitespace-nowrap /> </Link> </div> </div> </section> { } {!spotlightLoading && spotlightAds.length > 0 && ( <section className= > <div className= > {spotlightAds.slice(0, 6).map((ad) => ( <Card3D key={ad.id} className= strength={6} > <Link to= onClick={() => handleSpotlightView(ad)} className= > <div className= > <img src={ad.image_url || advertImage(ad.category)} alt={ad.title} className= loading= /> {(ad.package === 'featured' || ad.is_featured) && ( <span className= > <Star className= /> Featured </span> )} </div> <div className= > <div className= > <h3 className= >{ad.title}</h3> </div> <p className= > <Building className= /> {ad.business_name || 'Business'} {ad.location && ( <span className= > <MapPin className= /> {ad.location} </span> )} </p> {ad.description && ( <p className= >{ad.description}</p> )} <div className= > <span className= > {ad.package === 'featured' ? 'Premium Plan' : ad.package === 'monthly' ? 'Monthly' : 'Weekly'} </span> <span className= > View advert <ExternalLink className= /> </span> </div> </div> </Link> </Card3D> ))} </div> </section> )} { } <AnimatedSection className= > <div className= > {stats.map(({ label, value, icon: Icon, color }) => ( <Card3D key={label} className= strength={5}> <div className={`w-11 h-11 rounded-xl flex items-center justify-center ${color}`}> <Icon className= /> </div> <div> <div className= >{value}</div> <div className= >{label}</div> </div> </Card3D> ))} </div> </AnimatedSection> { } <AnimatedSection className= > <div className= > <h2 className= >Why professionals choose JobBridge</h2> <p className= >Built for the modern workforce</p> </div> <div className= > {[ { icon: Zap, title: 'AI-Powered Matching', desc: 'Smart algorithms match you with the perfect opportunities based on skills, experience, and preferences.', imgs: [pexel(7176027, 400, 250), pexel(8386440, 400, 250), pexel(5466785, 400, 250)] }, { icon: Shield, title: 'Verified Employers', desc: 'Every company on our platform is verified and vetted to ensure legitimate opportunities only.', imgs: [pexel(3952020, 400, 250), pexel(5668855, 400, 250), pexel(3771111, 400, 250)] }, { icon: Globe, title: 'Remote & Global', desc: 'Find opportunities worldwide — remote, hybrid, or on-site across every industry and timezone.', imgs: [pexel(7575322, 400, 250), pexel(3194519, 400, 250), pexel(927022, 400, 250)] }, ].map(({ icon: Icon, title, desc, imgs }) => ( <Card3D key={title} className= strength={6}> <CarouselImg images={imgs} className= /> <div className= > <div className= > <Icon className= /> </div> <h3 className= >{title}</h3> <p className= >{desc}</p> </div> </Card3D> ))} </div> </AnimatedSection> { } <AnimatedSection className= delay={100}> <div className= > <div> <h2 className= >Featured Jobs</h2> <p className= >Latest opportunities from top employers</p> </div> <Link to= text-sm font-medium text-blue-700 hover:text-blue-800 flex items-center gap-1 /> </Link> </div> { } {jobsLoading ? ( <div className= > <div className= > <Loader2 className= /> <p className= >Loading featured jobs...</p> </div> </div> ) : jobsError || homeJobs.length === 0 ? ( <div className= > <Briefcase className= /> <p className= >{jobsError || 'No jobs available at the moment'}</p> <p className= >Check back soon for new opportunities</p> <Link to= className= > Browse all jobs <ChevronRight className= /> </Link> </div> ) : ( <div className= bg-white rounded-2xl p-5 border border-gray-100 shadow-sm group flex items-start justify-between mb-3 w-10 h-10 rounded-xl bg-blue-600 flex items-center justify-center text-white text-xs font-bold font-semibold text-gray-900 group-hover:text-blue-700 transition-colors line-clamp-2 text-sm text-gray-500 mt-0.5 line-clamp-1 flex items-center justify-between mt-4 text-sm font-medium text-gray-700 line-clamp-1 text-xs bg-blue-700 text-white px-3 py-1.5 rounded-lg font-medium hover:bg-blue-800 transition-colors shrink-0 > <div className= > <div className= > <h2 className= >Professionals who found their path</h2> <p className= >Join thousands of professionals who landed their dream roles</p> </div> <div className= > {profileCards.map(({ name, role, img, color }) => ( <Card3D key={name} className= strength={8}> {img ? ( <img src={img} alt={name} className= /> ) : ( <div className={`w-full h-64 ${color} flex items-center justify-center`}> <span className= >{name.split(' ').map(n => n[0]).slice(0, 2).join('')}</span> </div> )} <div className= > <h3 className= >{name}</h3> <p className= >{role}</p> <div className= > <Star className= /> <span className= >Hired via JobBridge</span> </div> </div> </Card3D> ))} </div> </div> </AnimatedSection> { } <AnimatedSection className= delay={150}> <div className= > <div className= > <h2 className= >Trusted by top talent & teams</h2> </div> <div className= > {testimonials.map(({ name, role, company, quote, avatar }) => ( <Card3D key={name} className= strength={4}> <div className= > {[...Array(5)].map((_, i) => ( <Star key={i} className= /> ))} </div> <p className= </p> <div className= > <img src={avatar} alt={name} className= /> <div> <div className= >{name}</div> <div className= >{role} at {company}</div> </div> </div> </Card3D> ))} </div> </div> </AnimatedSection> { } <AnimatedSection className= delay={200}> <div className= > <div className= > <div className= /> <div className= /> </div> <div className= style={{ transform: 'translateZ(20px)' }}> <h2 className= >Ready to find your next opportunity?</h2> <p className= >Join over 2 million professionals already using JobBridge to advance their careers.</p> <div className= > <Link to= bg-white text-blue-700 font-semibold px-6 py-3 rounded-xl hover:bg-blue-50 transition-all hover:shadow-xl className= > View Premium Plans </Link> </div> </div> </div> </AnimatedSection> { } <footer className= > <div className= > <div className= > <div className= > <JobBridgeLogo variant= iconSize={24} /> </div> <div className= > <Link to= hover:text-gray-900 transition-colors hover:text-gray-900 transition-colors hover:text-gray-900 transition-colors hover:text-gray-900 transition-colors hover:text-gray-900 transition-colors text-xs text-gray-400 const stats = [ label: 'Active Jobs', value: '24,500+', icon: Briefcase, color: 'bg-blue-50 text-blue-700' , label: 'Companies', value: '3,200+', icon: Users, color: 'bg-emerald-50 text-emerald-700' , label: 'Placements', value: '18,900+', icon: Star, color: 'bg-amber-50 text-amber-700' , label: 'Success Rate', value: '94%', icon: TrendingUp, color: 'bg-rose-50 text-rose-700' , ]; const testimonials = [ name: 'Sarah K.', role: 'Software Engineer', company: 'Google', quote: 'Found my dream job in 3 weeks. The AI matching is incredible — every suggestion felt tailor-made.', avatar: pexel(774909, 100, 100) , name: 'Marcus T.', role: 'Recruiter', company: 'Stripe', quote: 'We filled 12 senior positions in a month. The talent quality here is unmatched.', avatar: pexel(220453, 100, 100) , name: 'Priya M.', role: 'Designer', company: 'Figma', quote: 'The platform understood my portfolio and connected me with exactly the right companies.', avatar: pexel(415829, 100, 100) , ]; const profileCards = [ name: 'Uthman Busari Olamide', role: 'Social Media Manager', img: '/images/social-media-manager.jpeg', color: 'bg-blue-600' , name: 'Akinnirun Timilehin', role: 'Project Manager', img: '/images/project-manager.jpeg', color: 'bg-emerald-600' , name: 'Makinde David Ayomidotun', role: 'Digital Marketing Manager', img: '/images/team-profile.jpeg', color: 'bg-purple-600' , ]; function CarouselImg( images, className : images: string[]; className?: string ) const [idx, setIdx] = useState(0); useEffect(() => if (images.length setIdx(i => (i + 1) % images.length), 3500); return () => clearInterval(t); , [images.length]); return ( images.map((src, i) => ( )) ); const openModal = useModal(); const navigate = useNavigate(); const [homeJobs, setHomeJobs] = useState ([]); const [jobsLoading, setJobsLoading] = useState(true); const [jobsError, setJobsError] = useState(''); useEffect(() => let cancelled = false; (async () => try setJobsLoading(true); const jobs = await fetchJobs(); if (cancelled"
    },
    {
      "file": "JobPreferences.tsx",
      "path": "src/pages/JobPreferences.tsx",
      "title": "Job Preferences",
      "text": "Job Preferences Job Preferences Set what jobs you want to be matched with. Preferred location Min salary (₦) Back to profile Find matching jobs max-w-3xl mx-auto px-4 py-8 bg-white border border-gray-100 rounded-2xl p-6 flex items-center gap-3 mb-2 w-10 h-10 rounded-xl bg-primary-fixed text-primary flex items-center justify-center /> </div> <h1 className= >Job Preferences</h1> </div> <p className= >Set what jobs you want to be matched with.</p> <div className= > <label className= > <span className= >Work type</span> <select value={prefs.workType} onChange={(e) => setPrefs((p) => ({ ...p, workType: e.target.value }))} className= > <option>Remote</option> <option>On-site</option> <option>Hybrid</option> </select> </label> <label className= > <span className= >Preferred location</span> <input value={prefs.location} onChange={(e) => setPrefs((p) => ({ ...p, location: e.target.value }))} className= /> </label> <label className= > <span className= >Min salary (₦)</span> <input value={prefs.salary} onChange={(e) => setPrefs((p) => ({ ...p, salary: e.target.value }))} className= /> </label> </div> <div className= > <Link to= px-4 py-2 rounded-xl border border-outline-variant text-sm text-gray-700 hover:bg-gray-50 inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-primary text-white text-sm font-semibold hover:bg-primary-container const [prefs, setPrefs] = useState( workType: 'Remote', location: 'Lagos', salary: '500000', ); return ( Job Preferences Set what jobs you want to be matched with. Work type setPrefs((p) => ( ...p, workType: e.target.value )) className=\"w-full border border-gray-200 rounded-lg px-3 py-2\" > Remote On-site Hybrid Preferred location setPrefs((p) => ( ...p, location: e.target.value )) className=\"w-full border border-gray-200 rounded-lg px-3 py-2\" /> Min salary (₦) setPrefs((p) => ( ...p, salary: e.target.value )) className=\"w-full border border-gray-200 rounded-lg px-3 py-2\" /> Back to profile Find matching jobs );"
    },
    {
      "file": "Jobs.tsx",
      "path": "src/pages/Jobs.tsx",
      "title": "{selectedJob.title}",
      "text": "{job.title} {selectedJob.title} Pay Job Type Requirements ({requirements.length}) Benefits ({benefits.length}) Full Job Description {line} Application Submitted! Apply for {selectedJob.title} Apply for {selectedJob.title} — {applyMethod === 'upload' ? 'Upload CV' : 'Manual'} Select a job to view details Find Jobs Internship Entry level {(typeFilter || categoryFilter || search || locationFilter) && ( Clear all {filteredJobs.length} {loadingJobs ? ( Loading jobs... ) : filteredJobs.length === 0 ? ( No jobs match your search Try adjusting your filters {job.title} {job.company} {job.location || 'Location not provided'} {job.type} {job.salary_range && ( {job.salary_range} +{jobBenefits.length - 3} more {timeAgo(job.created_at)} {job.is_featured ? ( ) : null} {selectedJob ? ( Back to jobs {selectedJob.title} {selectedJob.category} {selectedJob.location || 'Location not provided'} {timeAgo(selectedJob.created_at)} {selectedJob.applications_count || 0} applicants {appliedLocal.includes(selectedJob.id) || applied ? 'Applied' : 'Apply Now'} {selectedJob.salary_range} {selectedJob.type} Requirements ({requirements.length}) {requirement} Benefits ({benefits.length}) {showBenefits ? {showBenefits && ( {b} Full Job Description {line.replace(/^[•\\-]\\s*/, '')} {!showApplication ? ( {applied || appliedLocal.includes(selectedJob.id) ? 'Applied ✓' : 'Apply Now'} {selectedJob.applications_count || 0} people have already applied ) : applied ? ( Application Submitted! Good luck! You'll hear from the recruiter if you're shortlisted. ) : !applyMethod ? ( Apply for {selectedJob.title} Choose how you'd like to apply: Upload my CV pdf, doc, docx & rtf — max 10MB Fill in details manually Professional headline, experience & cover letter Apply for {selectedJob.title} — {applyMethod === 'upload' ? 'Upload CV' : 'Manual'} {appError && {appError} Upload your CV * {cvName ? ( Tap to upload your CV pdf, doc, docx & rtf files no bigger than 10MB Cover letter (optional) {submitting ? ( Submitting... Submit Application Professional headline / title * Years of Experience * Cover letter (optional) {submitting ? ( Submitting... Submit Application Select a job to view details Click on any job from the list to see full details including pay, benefits, and requirements flex flex-col min-h-[100dvh] bg-gray-100 flex-1 flex flex-col pb-[calc(4rem+env(safe-area-inset-bottom))] md:pb-0 subtitle= images={HERO_CAROUSELS.jobs} imageAlt= /> { } <div className= > <div className= > <div className= > <div className= > <Search className= /> <input type= placeholder= value={search} onChange={e => setSearch(e.target.value)} className= /> </div> <div className= > <MapPin className= /> <input type= placeholder= value={locationFilter} onChange={e => setLocationFilter(e.target.value)} className= /> </div> <button className= > Find Jobs </button> </div> { } <div className= > <select value={typeFilter} onChange={e => setTypeFilter(e.target.value)} className= > <option value= >Job Type</option> <option>Full-time</option> <option>Part-time</option> <option>Contract</option> <option>Internship</option> <option>Entry level</option> <option>Freelance</option> </select> <select value={categoryFilter} onChange={e => setCategoryFilter(e.target.value)} className= > <option value= >Category</option> {categories.map(c => <option key={c} value={c}>{c}</option>)} </select> {(typeFilter || categoryFilter || search || locationFilter) && ( <button onClick={() => { setTypeFilter(''); setCategoryFilter(''); setSearch(''); setLocationFilter(''); }} className= > Clear all </button> )} </div> </div> </div> { } <div className= > <div className= > <p className= > <span className= >{filteredJobs.length}</span> jobs found {search && <span> for </span>} </p> </div> </div> { } <div className= > { } <div className={`${selectedJob ? 'hidden lg:flex' : 'flex'} flex-col w-full lg:w-[420px] xl:w-[460px] flex-shrink-0 border-r border-gray-200 bg-white lg:overflow-y-auto lg:min-h-0`}> {loadingJobs ? ( <div className= > <Loader2 className= /> <p className= >Loading jobs...</p> </div> ) : filteredJobs.length === 0 ? ( <div className= > <Search className= /> <p className= >No jobs match your search</p> <p className= >Try adjusting your filters</p> </div> ) : ( filteredJobs.map(job => { const isActive = selectedJob?.id === job.id; const isSaved = savedJobs.includes(job.id); const jobBenefits = parseBenefits(job.benefits); return ( <Card3D key={job.id} onClick={() => selectJob(job)} className={`px-4 sm:px-5 py-5 border-b border-gray-100 cursor-pointer transition-colors ${ isActive ? 'bg-blue-50 border-l-4 border-l-blue-700' : 'hover:bg-gray-50 border-l-4 border-l-transparent' }`} strength={6} > <div className= > <CompanyLogo company={job.company} fallbackClassName={getCompanyColor(job.company)} /> <div className= > <h3 className={`font-bold text-base leading-snug break-words ${isActive ? 'text-blue-700' : 'text-gray-900'}`}>{job.title}</h3> <p className= >{job.company}</p> <div className= > <span className= w-4 h-4 flex-shrink-0 text-gray-500 mt-0.5 >{job.location || 'Location not provided'}</span></span> <span className= ><Briefcase className= />{job.type}</span> </div> {job.salary_range && ( <p className= >{job.salary_range}</p> )} {jobBenefits.length > 0 && ( <div className= > {jobBenefits.slice(0, 3).map((b: string) => ( <span key={b} className= >{b}</span> ))} {jobBenefits.length > 3 && ( <span className= >+{jobBenefits.length - 3} more</span> )} </div> )} <div className= > <span className= > <Clock className= />{timeAgo(job.created_at)} </span> <div className= > {job.is_featured ? ( <span className= >Featured</span> ) : null} <button onClick={(e) => toggleSave(job.id, e)} className={`p-1 rounded-full transition ${isSaved ? 'text-blue-600' : 'text-gray-300 hover:text-gray-500'}`} title={isSaved ? 'Remove from saved' : 'Save job'} > <Bookmark className= fill={isSaved ? 'currentColor' : 'none'} /> </button> </div> </div> </div> </div> </Card3D> ); }) )} </div> { } <div ref={detailRef} className={`${selectedJob ? 'flex' : 'hidden lg:flex'} flex-col flex-1 bg-white overflow-y-auto min-h-0`}> {selectedJob ? ( <> { } <button onClick={() => { setMobileDetail(false); setSelectedJob(null); setShowApplication(false); }} className= > <ArrowLeft className= /> Back to jobs </button> { } <div className= > <div className= > <CompanyLogo company={selectedJob.company} className= fallbackClassName={`${getCompanyColor(selectedJob.company)} shadow-sm`} /> <div className= > <h1 className= >{selectedJob.title}</h1> <p className= > <Building className= /> {selectedJob.company} {selectedJob.category && ( <span className= >{selectedJob.category}</span> )} </p> <div className= > <span className= w-4 h-4 flex-shrink-0 mt-0.5 >{selectedJob.location || 'Location not provided'}</span></span> <span className= />{timeAgo(selectedJob.created_at)}</span> <span className= />{selectedJob.applications_count || 0} applicants</span> </div> </div> </div> { } <div className= > <button onClick={() => handleApply(selectedJob)} disabled={appliedLocal.includes(selectedJob.id) || applied} className={`flex-1 font-semibold py-3 rounded-lg transition text-sm flex items-center justify-center gap-2 ${ appliedLocal.includes(selectedJob.id) || applied ? 'bg-green-100 text-green-700 cursor-default' : 'bg-blue-700 hover:bg-blue-800 text-white' }`} > <Briefcase className= /> {appliedLocal.includes(selectedJob.id) || applied ? 'Applied' : 'Apply Now'} </button> <button onClick={() => toggleSave(selectedJob.id)} className={`p-3 rounded-lg border transition ${savedJobs.includes(selectedJob.id) ? 'border-blue-300 text-blue-600 bg-blue-50' : 'border-gray-300 text-gray-500 hover:bg-gray-50'}`} title={savedJobs.includes(selectedJob.id) ? 'Saved' : 'Save this job'} > <Bookmark className= fill={savedJobs.includes(selectedJob.id) ? 'currentColor' : 'none'} /> </button> <button onClick={() => { try { const url = `${window.location.origin}/jobs?id=${selectedJob.id}`; const text = `${selectedJob.title} at ${selectedJob.company}`; if (navigator.share) { navigator.share({ title: text, text, url }).catch(() => {}); } else { window.open(`https: } } catch(e) {} }} className= > <Share2 className= /> </button> </div> </div> { } {selectedJob.salary_range && ( <div className= > <h3 className= >Pay</h3> <div className= > <CheckCircle className= /> <span className= >{selectedJob.salary_range}</span> </div> </div> )} { } <div className= > <h3 className= >Job Type</h3> <div className= > <CheckCircle className= /> <span className= >{selectedJob.type}</span> </div> </div> { } {requirements.length > 0 && ( <div className= > <h3 className= >Requirements ({requirements.length})</h3> <ul className= > {requirements.map((requirement: string) => ( <li key={requirement} className= > <CheckCircle className= /> <span>{requirement}</span> </li> ))} </ul> </div> )} { } {benefits.length > 0 && ( <div className= > <button onClick={() => setShowBenefits(!showBenefits)} className= > <h3 className= > Benefits ({benefits.length}) </h3> {showBenefits ? <ChevronUp className= /> : <ChevronDown className= />} </button> {showBenefits && ( <div className= > {benefits.map((b: string) => ( <span key={b} className= > <CheckCircle className= /> {b} </span> ))} </div> )} </div> )} { } <div className= > <h3 className= >Full Job Description</h3> <div className= />; if (line.endsWith(':') || line.startsWith('About ') || line.startsWith('Role Overview') || line.startsWith('Responsibilities') || line.startsWith('Requirements')) { return <h4 key={i} className= >{line}</h4>; } if (line.startsWith('•') || line.startsWith('- ')) { return ( <div key={i} className= > <span className= /> <span className= text-sm leading-relaxed border-t border-gray-100 bg-gray-50 > <button onClick={() => handleApply(selectedJob)} disabled={appliedLocal.includes(selectedJob.id) || applied} className={`w-full font-semibold py-3 rounded-lg transition text-sm flex items-center justify-center gap-2 ${ appliedLocal.includes(selectedJob.id) || applied ? 'bg-green-100 text-green-700 cursor-default' : 'bg-blue-700 hover:bg-blue-800 text-white' }`} > <Briefcase className= /> {applied || appliedLocal.includes(selectedJob.id) ? 'Applied ✓' : 'Apply Now'} </button> <p className= > {selectedJob.applications_count || 0} people have already applied </p> </div> ) : applied ? ( <div className= > <div className= > <CheckCircle className= /> </div> <h3 className= >Application Submitted!</h3> <p className= >Good luck! You'll hear from the recruiter if you're shortlisted.</p> </div> ) : !applyMethod ? ( <div className= > <div className= > <h3 className= > <Briefcase className= /> Apply for {selectedJob.title} </h3> <button onClick={() => { setShowApplication(false); setApplyMethod(null); }} className= >Cancel</button> </div> <p className= >Choose how you'd like to apply:</p> <button onClick={() => setApplyMethod('upload')} className= > <FileText className= /> <p className= >Upload my CV</p> <p className= >pdf, doc, docx & rtf — max 10MB</p> </button> <div className= > <div className= /> <span className= >OR</span> <div className= /> </div> <button onClick={() => setApplyMethod('manual')} className= > <Edit3 className= /> <p className= >Fill in details manually</p> <p className= >Professional headline, experience & cover letter</p> </button> </div> ) : ( <div className= > <div className= > <h3 className= > <Briefcase className= /> Apply for {selectedJob.title} <span className= >— {applyMethod === 'upload' ? 'Upload CV' : 'Manual'}</span> </h3> <button onClick={() => setApplyMethod(null)} className= >Back</button> </div> {appError && <div className= >{appError}</div>} {applyMethod === 'upload' && ( <div className= > <div> <label className= >Upload your CV *</label> <label className= > <input type= .pdf,.doc,.docx,.rtf /> {cvName ? ( <div className= > <CheckCircle className= /> <span className= >{cvName}</span> </div> ) : ( <div> <Upload className= /> <p className= >Tap to upload your CV</p> <p className= >pdf, doc, docx & rtf files no bigger than 10MB</p> </div> )} </label> </div> <div> <label className= >Cover letter (optional)</label> <textarea value={appForm.cover_letter} onChange={e => updateAppForm('cover_letter', e.target.value)} rows={3} placeholder= className= /> </div> <button onClick={() => handleSubmitApplication(selectedJob.id)} disabled={submitting} className= > {submitting ? ( <><span className= /> Submitting...</> ) : ( <><Send className= /> Submit Application</> )} </button> </div> )} {applyMethod === 'manual' && ( <div className= > <div> <label className= >Professional headline / title *</label> <input type= value={appForm.professional_headline} onChange={e => updateAppForm('professional_headline', e.target.value)} placeholder= className= /> </div> <div> <label className= >Years of Experience *</label> <select value={appForm.years_of_experience} onChange={e => updateAppForm('years_of_experience', e.target.value)} className= > <option value= >Select...</option> {['Entry (0-1)', '1-2', '3-5', '5-7', '7-10', '10+'].map(y => <option key={y} value={y}>{y}</option>)} </select> </div> <div> <label className= >Cover letter (optional)</label> <textarea value={appForm.cover_letter} onChange={e => updateAppForm('cover_letter', e.target.value)} rows={3} placeholder= className= /> </div> <button onClick={() => handleSubmitApplication(selectedJob.id)} disabled={submitting} className= > {submitting ? ( <><span className= /> Submitting...</> ) : ( <><Send className= /> Submit Application</> )} </button> </div> )} </div> )} </div> </> ) : ( <div className= > <div className= > <div className= > <Briefcase className= /> </div> <h3 className= >Select a job to view details</h3> <p className= const COMPANY_COLORS = [ 'bg-blue-600', 'bg-emerald-600', 'bg-purple-600', 'bg-rose-600', 'bg-amber-600', 'bg-cyan-600', 'bg-indigo-600', 'bg-pink-600', 'bg-teal-600', 'bg-orange-600', ]; function getCompanyColor(name: string) let hash = 0; for (let i = 0; i w[0]).join('').slice(0, 2).toUpperCase(); function timeAgo(dateStr: string) const diff = Date.now() - new Date(dateStr).getTime(); const days = Math.floor(diff / (1000 * 60 * 60 * 24)); if (days === 0) return 'Today'; if (days === 1) return '1 day ago'; if (days 1 ? 's' : '' ago`; return `$ Math.floor(days / 30) month$ Math.floor(days / 30) > 1 ? 's' : '' ago`; function parseBenefits(benefits: string | string[] | null): string[] if (!benefits) return []; if (Array.isArray(benefits)) return benefits; try return JSON.parse(benefits); catch return []; function parseList(value: string | string[] | null): string[] if (!value) return []; if (Array.isArray(value)) return value.filter(Boolean); try const parsed = JSON.parse(value); if (Array.isArray(parsed)) return parsed.filter(Boolean); catch return value.split(',').map(item => item.trim()).filter(Boolean); const Jobs = () => const user, profile = useAuth(); const savedJobs, toggleSaveJob, markApplied, appliedJobs = useAuth(); const openProtectedModal = useAuthRequired(); const handleApply = (job: any) => openProtectedModal( action: 'apply-job', modalData: job_id: job.id, title: job.title, company: job.company , ); ; const [showApplication, setShowApplication] = useState(false); const [applyMethod, setApplyMethod] = useState (null); const [appForm, setAppForm] = useState( professional_headline: '', years_of_experience: '', cover_letter: '', ); const [cvFile, setCvFile] = useState (null); const [cvName, setCvName] = useState(''); const [submitting, setSubmitting] = useState(false); const [appError, setAppError] = useState(''); const [applied, setApplied] = useState(false); const updateAppForm = (field: string, value: any) => setAppForm(prev => ( ...prev, [field]: value"
    },
    {
      "file": "Login.tsx",
      "path": "src/pages/Login.tsx",
      "title": "Welcome back",
      "text": "Welcome back Reset Password Check Your Inbox Reset Email Sent! What to do next: {view === 'signin' && ( Welcome back Sign in to your JobBridge account Reset Password We'll send you a secure reset link Check Your Inbox A reset link has been sent {bg} {logoHeader} Reset Email Sent! We sent a password reset link to {forgotEmail} What to do next: Open the reset email we sent you Reset password in the email Enter and confirm your new password Didn't receive it? Check your spam folder. The link expires in 1 hour. Back to Sign In {bg} {logoHeader} ← Back to Sign In Enter the email address you signed up with and we'll send you a secure link to reset your password. Email Address {forgotEmailError && ( {forgotEmailError} {forgotLoading ? ( Sending Reset Link... Send Reset Link {bg} {logoHeader} Email Address {emailError && ( {emailError} Password Forgot password? {showPassword ? {passwordError && ( {passwordError} Keep me signed in on this device {formError} {loading ? ( Signing In... Sign In Don't have an account?{' '} Sign up free Encrypted & Secure Nigeria's #1 Job Network absolute inset-0 overflow-hidden pointer-events-none absolute top-20 left-20 w-96 h-96 bg-blue-500/20 rounded-full blur-3xl animate-pulse absolute bottom-20 right-20 w-72 h-72 bg-indigo-400/20 rounded-full blur-3xl animate-pulse absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[min(600px,90vw)] h-[min(600px,90vw)] bg-blue-600/5 rounded-full blur-3xl https: alt= absolute inset-0 w-full h-full object-cover opacity-5 > <div className= > <JobBridgeLogo variant= iconSize={40} /> </div> {view === 'signin' && ( <> <h1 className= >Welcome back</h1> <p className= >Sign in to your JobBridge account</p> </> )} {view === 'forgot' && ( <> <h1 className= >Reset Password</h1> <p className= >We'll send you a secure reset link</p> </> )} {view === 'forgot-sent' && ( <> <h1 className= >Check Your Inbox</h1> <p className= >A reset link has been sent</p> </> )} </div> ); if (view === 'forgot-sent') { return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <div className= > <CheckCircle className= /> </div> <h2 className= >Reset Email Sent!</h2> <p className= >We sent a password reset link to</p> <p className= >{forgotEmail}</p> <div className= > <h3 className= >What to do next:</h3> <ol className= > <li className= >1.</span><span>Open the reset email we sent you</span></li> <li className= >2.</span><span>Click <strong>Reset password</strong> in the email</span></li> <li className= >3.</span><span>Enter and confirm your new password</span></li> </ol> </div> <p className= > Didn't receive it? Check your spam folder. The link expires in 1 hour. </p> <button onClick={() => { setView('signin'); setForgotEmail(''); }} className= > Back to Sign In </button> </div> </div> </div> ); } if (view === 'forgot') { return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <button onClick={() => { setView('signin'); setForgotEmailError(''); }} className= > ← Back to Sign In </button> <p className= > Enter the email address you signed up with and we'll send you a secure link to reset your password. </p> <form onSubmit={handleForgotSubmit} className= noValidate> <div> <label className= > Email Address </label> <div className= > <Mail className= /> <input id= type= value={forgotEmail} onChange={(e) => { setForgotEmail(e.target.value); setForgotEmailError(''); }} className={`w-full pl-12 pr-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ forgotEmailError ? 'border-red-400 bg-red-50' : 'border-gray-300' }`} placeholder= autoFocus /> </div> {forgotEmailError && ( <p className= > <AlertCircle className= /> {forgotEmailError} </p> )} </div> <button type= disabled={forgotLoading} className= > {forgotLoading ? ( <> <Loader2 className= /> Sending Reset Link... </> ) : ( <> Send Reset Link <ArrowRight className= /> </> )} </button> </form> </div> </div> </div> ); } return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <form onSubmit={handleSubmit} className= noValidate> { } <div> <label htmlFor= block text-sm font-medium text-gray-700 mb-1.5 > <Mail className= /> <input id= type= value={email} onChange={(e) => setEmail(e.target.value)} className={`w-full pl-12 pr-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ emailError ? 'border-red-400 bg-red-50' : 'border-gray-300' }`} placeholder= autoComplete= /> </div> {emailError && ( <p className= > <AlertCircle className= /> {emailError} </p> )} </div> { } <div> <div className= > <label htmlFor= block text-sm font-medium text-gray-700 onClick={() => { setView('forgot'); setForgotEmail(email); }} className= > Forgot password? </button> </div> <div className= > <Lock className= /> <input id= type={showPassword ? 'text' : 'password'} required value={password} onChange={(e) => setPassword(e.target.value)} className={`w-full pl-12 pr-12 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ passwordError ? 'border-red-400 bg-red-50' : 'border-gray-300' }`} placeholder= autoComplete= /> <button type= onClick={() => setShowPassword(!showPassword)} className= aria-label={showPassword ? 'Hide password' : 'Show password'} > {showPassword ? <EyeOff className= /> : <Eye className= />} </button> </div> {passwordError && ( <p className= > <AlertCircle className= /> {passwordError} </p> )} </div> { } <div className= > <input type= id= checked={rememberMe} onChange={(e) => setRememberMe(e.target.checked)} className= /> <label htmlFor= text-sm text-gray-600 cursor-pointer select-none flex items-start gap-3 p-3.5 bg-red-50 border border-red-200 rounded-xl text-sm text-red-700 animate-fade-in w-4 h-4 shrink-0 mt-0.5 disabled={loading} className= > {loading ? ( <> <Loader2 className= /> Signing In... </> ) : ( <> Sign In <ArrowRight className= /> </> )} </button> </form> <p className= > Don't have an account?{' '} <Link to= font-semibold text-blue-700 hover:text-blue-800 transition-colors flex items-center justify-center gap-4 mt-6 text-blue-300/70 text-xs flex items-center gap-1 type View = 'signin' | 'forgot' | 'forgot-sent'; const signIn, resetPassword = useAuth(); const navigate = useNavigate(); const location = useLocation(); const redirectTo = new URLSearchParams(location.search).get('redirect') || '/'; const [view, setView] = useState ('signin'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [rememberMe, setRememberMe] = useState(true); const [loading, setLoading] = useState(false); const [showPassword, setShowPassword] = useState(false); const [emailError, setEmailError] = useState(''); const [passwordError, setPasswordError] = useState(''); const [formError, setFormError] = useState(''); const [forgotEmail, setForgotEmail] = useState(''); const [forgotEmailError, setForgotEmailError] = useState(''); const [forgotLoading, setForgotLoading] = useState(false); useEffect(() => if (email) setEmailError(''); , [email]); useEffect(() => if (password) setPasswordError(''); , [password]); const validateSignInFields = () => let valid = true; const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/; if (!email.trim()) setEmailError('Email address is required.'); valid = false; else if (!emailRegex.test(email.trim())) setEmailError('Please enter a valid email address.'); valid = false; if (!password) setPasswordError('Password is required.'); valid = false; return valid; ; const handleSubmit = async (e: React.FormEvent) => e.preventDefault(); setFormError(''); if (!validateSignInFields()) return; if (!checkRateLimit('signin', 5, 60000)) setFormError('Too many sign-in attempts. Please wait a minute and try again.'); return; setLoading(true); const error = await signIn(email.trim(), password, rememberMe); setLoading(false); if (error) const msg = error.message || 'Sign in failed. Please check your credentials and try again.'; setFormError(msg); if (msg.toLowerCase().includes('confirm') || msg.toLowerCase().includes('email')) setEmailError(msg); setFormError(''); return; window.dispatchEvent(new CustomEvent('jo"
    },
    {
      "file": "Messages.tsx",
      "path": "src/pages/Messages.tsx",
      "title": "Messages",
      "text": "Messages JobBridge Messages {!isAuthenticated ? ( Sign in to see your messages Sign in ) : loading ? ( ) : filtered.length === 0 ? ( No conversations yet When you contact a service provider, your chat will appear here. {conv.online && ( {conv.company} {conv.timestamp} {conv.locked && {conv.unread} {!selectedId ? ( JobBridge Messages {selectedConversation && ( {selectedConversation?.company} {selectedConversation?.locked ? 'Conversation ended' : 'Private chat'} {selectedConversation?.locked && ( This conversation is no longer active. No messages yet Say hello to start the conversation. item.type === 'day' ? ( {item.day} {item.msg!.text} {item.msg!.temp && ( flex h-[calc(100dvh-64px)] px-4 pt-4 pb-2 border-b border-gray-100 flex items-center justify-between mb-3 text-lg font-bold text-gray-900 flex items-center gap-1 text-xs text-blue-600 /> Online </span> </div> <div className= > <Search className= /> <input type= placeholder= value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} className= /> </div> </div> <div className= > {!isAuthenticated ? ( <div className= > <img src={IMG.empty.noMessages} alt= className= /> <p className= >Sign in to see your messages</p> <a href= text-blue-600 text-sm font-medium hover:underline > <div className= > {[1, 2, 3].map((i) => ( <div key={i} className= /> ))} </div> </div> ) : filtered.length === 0 ? ( <div className= > <img src={IMG.empty.noMessages} alt= className= /> <p className= >No conversations yet</p> <p className= > When you contact a service provider, your chat will appear here. </p> </div> ) : ( filtered.map((conv) => ( <button key={conv.id} onClick={() => openConversation(conv.id)} className={`w-full flex items-start gap-3 px-4 py-3 border-b border-gray-50 text-left transition-colors ${ selectedId === conv.id ? 'bg-blue-50' : 'hover:bg-gray-50' }`} > <div className= > <CompanyLogo company={conv.company} className= fallbackClassName={conv.color} /> {conv.online && ( <span className= /> )} </div> <div className= > <div className= > <span className={`text-sm truncate ${ conv.unread > 0 ? 'font-semibold text-gray-900' : 'font-medium text-gray-700' }`} > {conv.company} </span> <span className= >{conv.timestamp}</span> </div> <div className= > <p className={`text-xs truncate ${ conv.unread > 0 ? 'text-gray-700 font-medium' : 'text-gray-500' }`} > {conv.locked && <Lock className= />} {conv.lastMessage} </p> {conv.unread > 0 && ( <span className= > {conv.unread} </span> )} </div> </div> </button> )) )} </div> </div> { } <div className={`flex-1 flex-col bg-slate-50 ${selectedId ? 'flex' : 'hidden sm:flex'}`}> {!selectedId ? ( <div className= > <div className= > <Send className= /> </div> <h3 className= >JobBridge Messages</h3> <p className= > Select a conversation to view and reply. Each conversation is private to you and the other person. </p> </div> ) : ( <> { } <div className= > <div className= > <button onClick={handleBackToList} className= aria-label= > <ArrowLeft className= /> </button> {selectedConversation && ( <CompanyLogo company={selectedConversation.company} className= fallbackClassName={selectedConversation.color} /> )} <div className= > <p className= > {selectedConversation?.company} </p> <p className= > {selectedConversation?.locked ? 'Conversation ended' : 'Private chat'} </p> </div> </div> </div> { } <div className= > {selectedConversation?.locked && ( <div className= > <Lock className= /> This conversation is no longer active. </div> )} {currentMessages.length === 0 && !sending ? ( <div className= > <div className= > <p className= > No messages yet </p> <p className= > Say hello to start the conversation. </p> </div> </div> ) : ( <div className= > {renderedMessages.map((item) => item.type === 'day' ? ( <div key={item.key} className= > <span className= > {item.day} </span> </div> ) : ( <div key={item.key} className={`flex ${item.msg!.sender === 'me' ? 'justify-end' : 'justify-start'}`} > <div className={`max-w-[75%] sm:max-w-sm rounded-2xl px-3.5 py-2 text-sm shadow-sm ${ item.msg!.sender === 'me' ? 'bg-blue-600 text-white rounded-br-sm' : 'bg-white text-gray-800 rounded-bl-sm' }`} > <p className= >{item.msg!.text}</p> <div className={`flex items-center justify-end gap-1 mt-1 text-[11px] ${ item.msg!.sender === 'me' ? 'text-gray-500' : 'text-gray-400' }`} > {item.msg!.temp && ( <> <Clock className= /> <span>Sending…</span> </> )} {!item.msg!.temp && item.msg!.time} {item.msg!.sender === 'me' && !item.msg!.temp && ( item.msg!.read ? ( <CheckCheck className= /> ) : ( <Check className= /> ) )} </div> </div> </div> ), )} <div ref={messagesEndRef} /> </div> )} </div> { } {!selectedConversation?.locked && ( <div className= > <div className= > <input type= value={newMessage} onChange={(e) => setNewMessage(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && handleSend()} placeholder= className= /> <button onClick={handleSend} disabled={!newMessage.trim() || sending} className= aria-label= > <Send className= fetchConversations, fetchConversationById, fetchConversationMessages, createConversationMessage, markConversationRead, from '../lib/supabaseQueries'; interface Conversation id: string; company: string; logo_initial: string; color: string; lastMessage: string; timestamp: string; unread: number; locked?: boolean; online?: boolean; recipientId: string; recipientName: string; recipientEmail?: string; interface MessageItem id: string; sender: 'me' | 'them'; text: string; time: string; isoTime?: string; read?: boolean; temp?: boolean; function formatListTime(iso?: string): string if (!iso) return ''; const d = new Date(iso); if (Number.isNaN(d.getTime())) return ''; const now = new Date(); const startToday = new Date(now.getFullYear(), now.getMonth(), now.getDate()).getTime(); const startMsg = new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime(); const diffDays = Math.round((startToday - startMsg) / 86400000); if (diffDays (queryConversationId || null); const [searchTerm, setSearchTerm] = useState(''); const [newMessage, setNewMessage] = useState(''); const [conversations, setConversations] = useState ([]); const [messages, setMessages] = useState >( ); const [loading, setLoading] = useState(true); const [sending, setSending] = useState(false); const messagesEndRef = useRef (null); const messagesRef = useRef >( ); useEffect(() => messagesRef.current = messages; , [messages]); useEffect(() => if (!selectedId) return; if (queryConversationId === selectedId) return; navigate(`/messages?conversationId=$ encodeURIComponent(selectedId) `, replace: true ); , [selectedId, queryConversationId, navigate]); useEffect(() => if (queryConversationId && selectedId !== queryConversationId) setSelectedId(queryConversationId); , [queryConversationId, selectedId]); const reloadConversations = useCallback(async () => if (!user?.id) return; try const convs = await fetchConversations(user.id); setConversations(convs.map((c) => mapConversation(c, user.id))); catch (err) console.error("
    },
    {
      "file": "MyJobs.tsx",
      "path": "src/pages/MyJobs.tsx",
      "title": "MyJobs",
      "text": "{emptyMessages[activeTab].title} {job.title} {label} {count} {searchTerm && ( ) : filteredItems.length === 0 ? ( {emptyMessages[activeTab].title} {emptyMessages[activeTab].desc} {activeTab === 'saved' && ( Not seeing a job?{' '} Browse all jobs Find Jobs {job.title} {job.company} {job.location} {job.type} {job.salary_range && ( {job.salary_range} {job.category} +{benefits.length - 4} more Applied on {new Date((job as any).applied_at).toLocaleDateString()} {activeTab === 'saved' && ( {(job as any).application_status || 'Applied'} {(job as any).application_status !== 'rejected' && (job as any).application_status !== 'hired' && ( Withdraw View → Having an issue with My Jobs?{' '} Tell us more My Jobs & Applications Track your saved jobs, manage your applications, and monitor your interview progress Person organizing job applications max-w-4xl mx-auto px-4 py-8 flex items-center gap-1 border-b border-gray-200 mb-6 overflow-x-auto /> {label} <span className={`ml-1 text-xs px-2 py-0.5 rounded-full font-medium ${ activeTab === key ? 'bg-blue-100 text-blue-700' : 'bg-gray-100 text-gray-600' }`}> {count} </span> </button> ))} </div> { } {currentItems.length > 0 && ( <div className= > <Search className= /> <input type= placeholder={`Search ${activeTab} jobs...`} value={searchTerm} onChange={e => setSearchTerm(e.target.value)} className= /> {searchTerm && ( <button onClick={() => setSearchTerm('')} className= > <X className= /> </button> )} </div> )} { } {loading ? ( <div className= > <div className= /> </div> ) : filteredItems.length === 0 ? ( <div className= > <img src={IMG.empty.noSaved} alt= w-full max-w-xs rounded-xl mb-5 object-cover text-xl font-semibold text-gray-800 mb-2 text-gray-500 mb-6 max-w-sm > <p className= > Not seeing a job?{' '} <Link to= text-blue-600 hover:underline className= > Find Jobs <ChevronRight className= /> </Link> </div> ) : ( <div className= > {filteredItems.map(job => { const benefits = parseBenefits(job.benefits); return ( <div key={job.id} className= > <div className= > <div className= > { } <div className= > <CompanyLogo company={job.company} className= /> <div> <h3 className= >{job.title}</h3> <p className= >{job.company}</p> </div> </div> { } <div className= > <span className= />{job.location}</span> <span className= />{job.type}</span> {job.salary_range && ( <span className= >₦</span>{job.salary_range}</span> )} {job.category && ( <span className= >{job.category}</span> )} </div> { } {benefits.length > 0 && ( <div className= > {benefits.slice(0, 4).map((b, i) => ( <span key={i} className= >{b}</span> ))} {benefits.length > 4 && ( <span className= >+{benefits.length - 4} more</span> )} </div> )} { } {activeTab === 'applied' && (job as any).applied_at && ( <p className= >Applied on {new Date((job as any).applied_at).toLocaleDateString()}</p> )} </div> { } <div className= > {activeTab === 'saved' && ( <button onClick={() => toggleSaveJob(job.id)} className= title= > <Bookmark className= /> </button> )} {activeTab === 'applied' && ( <> <span className={`text-xs px-2.5 py-1 rounded-full font-medium ${ (job as any).application_status === 'shortlisted' ? 'bg-green-50 text-green-700' : (job as any).application_status === 'reviewed' ? 'bg-blue-50 text-blue-700' : (job as any).application_status === 'rejected' ? 'bg-red-50 text-red-700' : (job as any).application_status === 'hired' ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }`}> {(job as any).application_status || 'Applied'} </span> {(job as any).application_status !== 'rejected' && (job as any).application_status !== 'hired' && ( <button onClick={() => { if (confirm('Are you sure you want to withdraw your application?')) { withdrawApplication((job as any).id).then(() => { setMyApplications(prev => prev.filter(a => a.job_id !== job.id)); }); } }} className= > Withdraw </button> )} </> )} <Link to= className= > View → </Link> </div> </div> </div> ); })} </div> )} { } {!loading && ( <div className= > <p className= > Having an issue with My Jobs?{' '} <Link to= text-blue-600 hover:underline type Tab = 'saved' | 'applied' | 'interviews' | 'archived'; interface JobItem id: string; title: string; company: string; location: string; type: string; salary_range?: string; benefits?: string; category?: string; description?: string; created_at?: string; interface ApplicationItem id: string; job_id: string; professional_headline: string; status: string; created_at: string; job: JobItem | null; const savedJobs, appliedJobs, toggleSaveJob, isAuthenticated, user = useAuth(); const [activeTab, setActiveTab] = useState ('saved'); const [allJobs, setAllJobs] = useState ([]); const [myApplications, setMyApplications] = useState ([]); const [loading, setLoading] = useState(true); const [searchTerm, setSearchTerm] = useState(''); useEffect(() => (async () => setLoading(true); try const [jobsData, appsData] = await Promise.all([ fetchJobs(), isAuthenticated ? fetchUserApplications(user?.id).catch(() => []) : Promise.resolve([]), ]); setAllJobs(jobsData); setMyApplications(appsData); catch setLoading(false); )(); , [isAuthenticated, user?.id]); const savedJobItems = allJobs.filter(j => savedJobs.includes(j.id)); const appliedJobItems = myApplications.map(app => ( ...app.job, application_status: app.status, applied_at: app.created_at, as any)).filter(Boolean); const interviewItems: JobItem[] = []; const archivedItems: JobItem[] = []; const tabs: key: Tab; label: string; count: number; icon: typeof Bookmark [] = [ key: 'saved', label: 'Saved', count: savedJobItems.length, icon: Bookmark , key: 'applied', label: 'Applied', count: appliedJobItems.length, icon: Briefcase , key: 'interviews', label: 'Interviews', count: interviewItems.length, icon: Calendar , key: 'archived', label: 'Archived', count: archivedItems.length, icon: Archive , ]; const currentItems = activeTab === 'saved' ? savedJobItems : activeTab === 'applied' ? appliedJobItems : activeTab === 'interviews' ? interviewItems : archivedItems; const filteredItems = searchTerm ? currentItems.filter(j => j.title.toLowerC"
    },
    {
      "file": "Notifications.tsx",
      "path": "src/pages/Notifications.tsx",
      "title": "Notifications",
      "text": "Job Alerts & Preferences Sign in to manage job alerts Nothing right now. Check back later! All Notifications Mark all read Job Alerts & Preferences Manage which job opportunities you want to be notified about Notification Settings {!isAuthenticated ? ( Sign in to manage job alerts Save searches and get notified when matching jobs are posted. Sign in ) : alertsLoading ? ( Loading job alerts... ) : alertsError ? ( {alertsError} ) : alerts.length === 0 ? ( Nothing right now. Check back later! Get updates from your recent searches Find jobs → {alert.count} {alert.enabled ? ( All Notifications All 0 && `(${unreadCount})`} {searchTerm && ( Checking your login status... Sign in to see your notifications Sign in Loading notifications... ) : pageError ? ( {pageError} ) : displayed.length === 0 ? ( No notifications to show {notif.title} {!notif.is_read && ( Open messages By creating a job alert, you agree to our{\" \"} Terms ../lib/supabaseQueries ; type NotifType = Notification[ ]; const DEFAULT_JOB_ALERTS: JobAlertSeed[] = [ { query: , enabled: true, }, { query: , enabled: false, }, { query: , enabled: true, }, ]; const typeConfig: Record< NotifType, { icon: typeof Bell; color: string; bg: string } > = { job_application: { icon: Briefcase, color: , }, message: { icon: MessageSquare, color: , }, interview: { icon: Calendar, color: }, review: { icon: Star, color: }, system: { icon: Shield, color: }, payment: { icon: CreditCard, color: }, advert: { icon: Bell, color: }, }; function formatRelativeTime(value: string): string { const date = new Date(value); if (Number.isNaN(date.getTime())) return ; const diffMs = Date.now() - date.getTime(); const diffMinutes = Math.floor(diffMs / (1000 * 60)); const diffHours = Math.floor(diffMinutes / 60); const diffDays = Math.floor(diffHours / 24); if (diffMinutes < 1) return ; if (diffMinutes < 60) return `${diffMinutes} minute${diffMinutes === 1 ? } ago`; if (diffHours < 24) return `${diffHours} hour${diffHours === 1 ? } ago`; if (diffDays < 7) return `${diffDays} day${diffDays === 1 ? } ago`; return date.toLocaleDateString(undefined, { day: , year: diffDays > 365 ? : undefined, }); } const { user, isAuthenticated, loading: authLoading } = useAuth(); const navigate = useNavigate(); const [alerts, setAlerts] = useState<JobAlertWithCount[]>([]); const [notifications, setNotifications] = useState<Notification[]>([]); const [filter, setFilter] = useState< ); const [searchTerm, setSearchTerm] = useState( ); const [loading, setLoading] = useState(false); const [alertsLoading, setAlertsLoading] = useState(false); const [pageError, setPageError] = useState( ); const [alertsError, setAlertsError] = useState( ); useEffect(() => { if (!user?.id) { setNotifications([]); setLoading(false); setPageError( ); return; } let active = true; const load = async () => { setLoading(true); setPageError( ); try { const items = await fetchNotifications(user.id); if (active) setNotifications(items as Notification[]); } catch (error) { console.error( , error); if (active) setPageError( ); } finally { if (active) setLoading(false); } }; load(); const channel = supabase .channel(`notifications-page:${user.id}`) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, (payload) => { const next = payload.new as Notification; setNotifications((prev) => { if (prev.some((item) => item.id === next.id)) return prev; return [next, ...prev]; }); }, ) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, (payload) => { const next = payload.new as Notification; setNotifications((prev) => prev.map((item) => (item.id === next.id ? next : item)), ); }, ) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, (payload) => { const removed = payload.old as Notification; setNotifications((prev) => prev.filter((item) => item.id !== removed.id), ); }, ) .subscribe(); return () => { active = false; supabase.removeChannel(channel); }; }, [user?.id]); useEffect(() => { if (!user?.id) { setAlerts([]); setAlertsLoading(false); setAlertsError( ); return; } let active = true; const loadAlerts = async () => { setAlertsLoading(true); setAlertsError( ); try { const items = await fetchJobAlertsWithCounts( user.id, DEFAULT_JOB_ALERTS, ); if (active) setAlerts(items); } catch (error) { console.error( , error); if (active) setAlertsError( ); } finally { if (active) setAlertsLoading(false); } }; loadAlerts(); const channel = supabase .channel(`job-alerts:${user.id}`) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, () => { void loadAlerts(); }, ) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, () => { void loadAlerts(); }, ) .on( , { event: , schema: , filter: `user_id=eq.${user.id}`, }, () => { void loadAlerts(); }, ) .subscribe(); return () => { active = false; supabase.removeChannel(channel); }; }, [user?.id]); const unreadCount = useMemo( () => notifications.filter((n) => !n.is_read).length, [notifications], ); const displayed = useMemo(() => { const normalizedSearch = searchTerm.trim().toLowerCase(); return notifications .filter((n) => filter === || !n.is_read) .filter((n) => { if (!normalizedSearch) return true; return ( n.title.toLowerCase().includes(normalizedSearch) || (n.content || ).toLowerCase().includes(normalizedSearch) ); }); }, [notifications, filter, searchTerm]); const toggleAlert = async (id: string) => { const current = alerts.find((item) => item.id === id); if (!current) return; const nextEnabled = !current.enabled; setAlerts((prev) => prev.map((item) => item.id === id ? { ...item, enabled: nextEnabled } : item, ), ); try { await updateJobAlertEnabled(id, nextEnabled); } catch (error) { console.error( , error); setAlerts((prev) => prev.map((item) => (item.id === id ? current : item)), ); setAlertsError( ); } }; const handleMarkRead = async (id: string) => { const current = notifications.find((item) => item.id === id); if (!current || current.is_read) return; setNotifications((prev) => prev.map((item) => item.id === id ? { ...item, is_read: true, read_at: new Date().toISOString() } : item, ), ); try { await markNotificationRead(id); } catch (error) { console.error( , error, ); setNotifications((prev) => prev.map((item) => (item.id === id ? current : item)), ); } }; const handleMarkAllRead = async () => { if (!user?.id || unreadCount === 0) return; const previous = notifications; const readAt = new Date().toISOString(); setNotifications((prev) => prev.map((item) => ({ ...item, is_read: true, read_at: item.read_at || readAt, })), ); try { await markAllNotificationsRead(user.id); } catch (error) { console.error( , error); setNotifications(previous); } }; const handleDelete = async (id: string) => { const previous = notifications; setNotifications((prev) => prev.filter((item) => item.id !== id)); try { await deleteNotificationRecord(id); } catch (error) { console.error( , error); setNotifications(previous); } }; return ( <AppLayout> <PageHero compact title= subtitle={ unreadCount > 0 ? `You have ${unreadCount} unread notification${unreadCount > 1 ? Stay updated on your job search Person checking notifications max-w-3xl mx-auto px-4 py-8 flex justify-end mb-6 flex items-center gap-1.5 text-sm text-blue-600 hover:text-blue-700 font-medium /> Mark all read </button> </div> )} <div className= > <div className= > <div> <h2 className= > <Bell className= /> Job Alerts & Preferences </h2> <p className= >Manage which job opportunities you want to be notified about</p> </div> <Link to= className= > <Settings className= /> Notification Settings </Link> </div> {!isAuthenticated ? ( <div className= > <Bell className= /> <h3 className= > Sign in to manage job alerts </h3> <p className= > Save searches and get notified when matching jobs are posted. </p> <Link to= className= > Sign in </Link> </div> ) : alertsLoading ? ( <div className= > <p className= >Loading job alerts...</p> </div> ) : alertsError ? ( <div className= > {alertsError} </div> ) : alerts.length === 0 ? ( <div className= > <Bell className= /> <h3 className= > Nothing right now. Check back later! </h3> <p className= > Get updates from your recent searches </p> <Link to= className= > Find jobs → </Link> </div> ) : ( <div className= > {alerts.map((alert) => ( <div key={alert.id} className= > <div className= > <div className= > <Search className= /> </div> <div> <p className= > <span className= >{alert.count}</span> new exciting job{alert.count === 1 ? {alert.location ? ` in ${alert.location}` : } </p> <p className= > {alert.enabled ? } </p> </div> </div> <button onClick={() => toggleAlert(alert.id)} className= title={alert.enabled ? } > {alert.enabled ? ( <ToggleRight className= /> ) : ( <ToggleLeft className= /> )} </button> </div> ))} </div> )} </div> <div> <h2 className= > All Notifications </h2> <div className= > <div className= > <button onClick={() => setFilter( )} className={`px-3 py-1.5 rounded-md text-xs font-medium transition-colors ${ filter === }`} > All </button> <button onClick={() => setFilter( )} className={`px-3 py-1.5 rounded-md text-xs font-medium transition-colors ${ filter === }`} > Unread {unreadCount > 0 && `(${unreadCount})`} </button> </div> <div className= > <Search className= /> <input type= placeholder= value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} className= /> {searchTerm && ( <button onClick={() => setSearchTerm( )} className= > <X className= /> </button> )} </div> </div> {!isAuthenticated ? ( authLoading ? ( <div className= > <div className= > <Loader2 className= /> </div> <p className= > Checking your login status... </p> </div> ) : ( <div className= > <p className= > Sign in to see your notifications </p> <Link to= className= > Sign in </Link> </div> ) ) : loading ? ( <div className= > <p className= >Loading notifications...</p> </div> ) : pageError ? ( <div className= > {pageError} </div> ) : displayed.length === 0 ? ( <div className= > <Bell className= /> <p className= >No notifications to show</p> </div> ) : ( <div className= > {displayed.map((notif) => { const cfg = typeConfig[notif.type]; const Icon = cfg.icon; return ( <div key={notif.id} className={`rounded-xl border p-4 transition-all hover:shadow-sm ${ notif.is_read ? } ${notif.type === 'message' ? 'cursor-pointer hover:border-blue-200' : 'cursor-pointer'}`} onClick={() => { handleMarkRead(notif.id); if (notif.type === 'message') { const convId = notif.data?.conversation_id; navigate( convId ? `/messages?conversationId=${encodeURIComponent(String(convId))}` : '/messages' ); } }} > <div className= > <div className={`w-9 h-9 rounded-lg ${cfg.bg} flex items-center justify-center shrink-0`} > <Icon className={`w-4.5 h-4.5 ${cfg.color}`} /> </div> <div className= > <div className= > <div> <p className={`text-sm ${notif.is_read ? font-semibold text-gray-900 }`} > { } {notif.type === : notif.type === : notif.type === : notif.type === : notif.type === : notif.type === ? notif.content || : notif.type === ? notif.content || : notif.content || } </p> </div> <div className= > {!notif.is_read && ( <span className= /> )} <button onClick={(e) => { e.stopPropagation(); handleDelete(notif.id); }} className= title= > <Trash2 className= /> </button> </div> </div> <p className= > {formatRelativeTime(notif.created_at)} {notif.type === 'message' && ( <span className= > Open messages <ArrowRight className= /> </span> )} </p> </div> </div> </div> ); })} </div> )} <div className= > <p className= > By creating a job alert, you agree to our{ } <Link to= text-blue-600 hover:underline deleteNotification as deleteNotificationRecord, fetchJobAlertsWithCounts, fetchNotifications, markAllNotificationsRead, markNotificationRead, type JobAlertSeed, type JobAlertWithCount, updateJobAlertEnabled, from \"../lib/supabaseQueries\"; Bell, Briefcase, MessageSquare, Calendar, Star, Shield, CreditCard, CheckCheck, ToggleLeft, ToggleRight, Trash2, Settings, Search, X, Loader2, ArrowRight, from \"lucide-react\"; type NotifType = Notification[\"type\"]; const DEFAULT_JOB_ALERTS: JobAlertSeed[] = [ query: \"Frontend Developer\", location: \"Lagos\", enabled: true, , query: \"Product Manager\", location: \"Remote\", enabled: false, , query: \"Software Engineer\", location: \"Abuja\", enabled: true, , ]; const typeConfig: Record = job_application: icon: Briefcase, color: \"text-blue-600\", bg: \"bg-blue-100\", , message: icon: MessageSquare, color: \"text-emerald-600\", bg: \"bg-emerald-100\", , interview: icon: Calendar, color: \"text-purple-600\", bg: \"bg-purple-100\" , review: icon: Star, color: \"text-yellow-600\", bg: \"bg-yellow-100\" , system: icon: Shield, color: \"text-gray-600\", bg: \"bg-gray-100\" , payment: icon: CreditCard, color: \"text-green-600\", bg: \"bg-green-100\" , advert: icon: Bell, color: \"text-orange-600\", bg: \"bg-orange-100\" , ; function formatRelativeTime(value: string): string const date = new Date(value); if (Number.isNaN(date.getTime())) return \"Just now\"; const diffMs = Date.now() - date.getTime(); const diffMinutes = Math.floor(diffMs / (1000 * 60)); const diffHours = Math.floor(diffMinutes / 60); const diffDays = Math.floor(diffHours / 24); if (diffMinutes 365 ? \"numeric\" : undefined, ); const user, isAuthenticated, loading: authLoading = useAuth(); const navigate = useNavigate(); const [alerts, setAlerts] = useState ([]); const [notifications, setNotifications] = useState ([]); const [filter, setFilter] = useState (\"all\"); const [searchTerm, setSearchTerm] = useState(\"\"); const [loading, setLoading] = useState(false); const [alertsLoading, setAlertsLoading] = useState(fals"
    },
    {
      "file": "Payment.tsx",
      "path": "src/pages/Payment.tsx",
      "title": "Payment",
      "text": "Checking your account Please log in to continue Pay with KoraPay Payment Successful! Pay for your plan with KoraPay and complete checkout securely. Complete Checkout Order Summary Kora not configured. Contact{\" \"} support Checking your account Please wait while we confirm your login status. Please log in to continue You must be logged in to complete payment. Sign in now to finish checkout. Login to continue Pay with KoraPay {plan.name} {plan.name} {plan.duration} {formatNaira(plan.price)} Powered by KoraPay {method} {error && ( Continue to feature (check now) {paying ? ( {isProcessing ? \"Verifying payment...\" : \"Processing...\"} ) : isCheckoutOpening ? ( Starting checkout... ) : isGatewayLoading ? ( Loading payment gateway... ) : isGatewayUnavailable ? ( Retrying gateway setup... Pay {formatNaira(plan.price)} securely {isGatewayUnavailable && !paying && !isProcessing && ( Retry payment gateway Your plan activates after secure server-side verification Payment Successful! Redirecting you to your {plan.name} feature now... Taking you there now Your {plan.name} is ready to use {paid && ( {renderSuccessScreen()} Secure payment Pay for your plan with KoraPay and complete checkout securely. Use card, USSD or bank transfer and get fast verification from our secure payment gateway. {item.icon} {item.label} Back to pricing Complete Checkout Pay for {plan.name} jobbridge.com.ng/payment Secure Checkout {renderKoraCheckoutScreen()} Order Summary Summary Order Summary {plan.name.charAt(0)} {plan.name} {plan.duration} {getPlanCreditsLabel()} {formatNaira(plan.price)} Total {formatNaira(plan.price)} {step === \"success\" ? ( ) : step === \"processing\" ? ( {item} Protected by SSL encryption Secure checkout SSL encrypted Powered by KoraPay ; createAdvertisement, fetchAdvertisementsByOwner, activateSubscription, activateAiSubscription as activateAiDb, addAdvertCredits, } from ; declare global { interface Window { Korapay: { initialize: (config: KoraPayConfig) => void; close: () => void; }; } } const isKoraScriptSource = (source?: string | null) => { if (!source) return false; return source.includes( ) || source.includes( ) || source.includes( ); }; const suppressKoraPay = (message: string, source?: string | null) => { if (typeof message !== ) return false; const sanitized = message.trim(); const isScriptError = sanitized === ; if ( sanitized.includes( ) || sanitized.includes( ) || isScriptError ) { if (isScriptError) { return isKoraScriptSource(source); } const stack = new Error().stack || ; if ( stack.includes( ) || stack.includes( ) || isKoraScriptSource(source) ) { console.warn( , message, source, ); return true; } } return false; }; interface KoraPayConfig { key: string; reference: string; amount: number; currency?: string; customer: { name: string; email: string }; notification_url?: string; narration?: string; channels?: string[]; default_channel?: string; metadata?: Record<string, string>; onClose?: () => void; onSuccess?: (data: { reference: string; payment_reference?: string; transaction_reference?: string; amount: string; status: string; }) => void; onFailed?: (data: { reference: string; payment_reference?: string; transaction_reference?: string; status: string; }) => void; onPending?: () => void; onTokenized?: (data: { token?: string; card?: { token?: string }; card_token?: string; }) => void; merchant_bears_cost?: boolean; } const PLANS: Record< string, { name: string; duration: string; price: number; credits: number; ai?: boolean; service?: boolean; business?: boolean; [key: string]: any; } > = { basic: { name: , price: 2000, credits: 1, }, standard: { name: , price: 3500, credits: 1, }, premium: { name: , price: 5000, credits: 3, }, ai_monthly: { name: , price: 1500, credits: 0, ai: true, }, ai_annual: { name: , price: 15000, credits: 0, ai: true, }, service_monthly: { name: , price: 1500, credits: 0, service: true, }, service_verified: { name: , price: 3000, credits: 0, service: true, }, service_featured: { name: , price: 5000, credits: 0, service: true, }, business_weekly: { name: , price: 2000, credits: 0, business: true, }, business_monthly: { name: , price: 7500, credits: 0, business: true, }, business_featured: { name: , price: 15000, credits: 0, business: true, }, }; type CheckoutStep = ; const KORA_SCRIPT_SRC = jobbridge_pending_payment_ref /ai-resume?fromPayment=true ; if ((plan as any).business) return `/business?create=true&paidPackage=${planKey}`; return ; } function formatNaira(n: number): string { return `₦${n.toLocaleString()}`; } const [searchParams] = useSearchParams(); const navigate = useNavigate(); const { user, fetchSubscription, fetchAiSubscription, isAuthenticated, loading: authLoading } = useAuth(); const { push } = useToasts(); const planKey = searchParams.get( ; const plan = PLANS[planKey] || PLANS.basic; const loginRedirect = `/login?redirect=${encodeURIComponent(`/payment?plan=${planKey}`)}`; const [step, setStep] = useState<CheckoutStep>( ); const [paying, setPaying] = useState(false); const [paid, setPaid] = useState(false); const [error, setError] = useState<ReactNode>( ); const [koraReady, setKoraReady] = useState(false); const [koraLoading, setKoraLoading] = useState(false); const [checkoutStarted, setCheckoutStarted] = useState(false); const [paymentReference, setPaymentReference] = useState<string>(() => { try { return sessionStorage.getItem(PENDING_PAYMENT_STORAGE_KEY) || ; } catch { return ; } }); const koraRetryCountRef = useRef(0); const isGatewayLoading = !koraReady && koraLoading; const isGatewayUnavailable = !koraReady && !koraLoading; const isCheckoutOpening = checkoutStarted && !paymentReference; const originalPaymentReferenceRef = useRef<string>( ); const koraCompletedRef = useRef(false); const receiptSentRef = useRef<Set<string>>(new Set()); const savedCardTokenRef = useRef<string>( ); const cleanupKora = () => { if (typeof window === ) return; try { const script = document.getElementById( ); if (script) { script.remove(); } } catch { } setKoraReady(false); setKoraLoading(false); setCheckoutStarted(false); }; const customerEmail = user?.email || ; const customerName = useMemo(() => { const metaName = user?.user_metadata?.full_name; if (typeof metaName === && metaName.trim()) return metaName.trim(); if (user?.email) return user.email.split( ; }, [user?.email, user?.user_metadata?.full_name]); const successTarget = getSuccessTarget(plan, planKey); const getPlanCreditsLabel = () => { if ((plan as any).business) return ; if (plan.ai) return ; return `${plan.credits} job post credit${plan.credits > 1 ? }`; }; const getWhatHappensNext = () => { if (step === ) { return [ Receipt sent to your email AI Career Tools ready Professional listing ready 1 advert credit added after verification } added after verification`, ]; } if (step === ) { return [ Webhook + Kora API reconciliation in progress AI Career Tools ready Professional listing ready 1 advert credit added after verification } added after verification`, ]; } return [ Server verifies the payment before activation AI Career Tools ready Professional listing ready 1 advert credit added after verification } added after verification`, ]; }; const clearPendingReference = () => { try { sessionStorage.removeItem(PENDING_PAYMENT_STORAGE_KEY); } catch { } originalPaymentReferenceRef.current = ; setPaymentReference( ); }; const verifyPaymentReference = async (reference: string) => { const functionsBaseUrl = getSupabaseFunctionsUrl(); if (!functionsBaseUrl) return false; const controller = new AbortController(); const timeout = window.setTimeout(() => controller.abort(), 7000); try { const res = await fetch(`${functionsBaseUrl}/verify-payment`, { method: , headers: { }, body: JSON.stringify({ reference, fallback_reference: originalPaymentReferenceRef.current || reference, original_reference: originalPaymentReferenceRef.current || reference, }), signal: controller.signal, }); const body = await res.json().catch(() => ({})); return res.ok && body?.verified === true; } catch (err) { console.warn( , err); return false; } finally { window.clearTimeout(timeout); } }; const pollPaymentUntilVerified = async (reference: string) => { const functionsBaseUrl = getSupabaseFunctionsUrl(); if (!functionsBaseUrl) return false; const maxAttempts = 8; for (let attempt = 1; attempt <= maxAttempts; attempt += 1) { try { const res = await fetch(`${functionsBaseUrl}/verify-payment`, { method: , headers: { }, body: JSON.stringify({ reference, fallback_reference: originalPaymentReferenceRef.current || reference, original_reference: originalPaymentReferenceRef.current || reference, }), }); const body = await res.json().catch(() => ({})); if (res.ok && body?.verified === true) { return true; } if (body?.charge_not_found === true) { if (attempt < maxAttempts) { await new Promise((resolve) => window.setTimeout(resolve, 3000)); continue; } return false; } if (body?.verified === false && body?.status === ) { return false; } if (attempt < maxAttempts) { await new Promise((resolve) => window.setTimeout(resolve, 2500)); } } catch (err) { console.warn( , err); if (attempt < maxAttempts) { await new Promise((resolve) => window.setTimeout(resolve, 2500)); } } } return false; }; const initializePaymentState = () => { setPaying(true); setError( ); setPaid(false); koraCompletedRef.current = false; }; const loadPendingAdvert = () => { if (!(plan as any).business) return null; try { const raw = sessionStorage.getItem('jb_pending_advert'); return raw ? JSON.parse(raw) : null; } catch { return null; } }; const buildPaymentMetadata = () => { const metadata: Record<string, unknown> = { source: 'korapay_checkout_standard', plan_name: plan.name, }; const pendingAdvert = loadPendingAdvert(); if (pendingAdvert) { metadata.advert = pendingAdvert; } return metadata; }; const recordPendingPayment = async ( reference: string, paymentMetadata: Record<string, unknown>, ) => { await recordPayment({ user_id: user.id, plan: planKey, amount: plan.price, reference, status: 'pending', currency: 'NGN', metadata: paymentMetadata, }); try { sessionStorage.setItem(PENDING_PAYMENT_STORAGE_KEY, reference); } catch { } setPaymentReference(reference); }; const notifyPendingPayment = async (reference: string) => { try { recordPaymentClick('pay_button'); } catch { } push({ message: `KoraPay checkout initialized. Complete your payment to activate ${plan.name}.`, type: 'info', }); if (user.email) { sendEmail({ type: 'payment_initiated', email: user.email, name: customerName, plan: plan.name, amount: String(plan.price), }); } }; const handleSuccessfulPayment = async ( reference: string, verifyPayment = true, ) => { setPaying(false); setError( ); setPaid(true); setStep( ); push({ message: , }); let verified = false; if (verifyPayment) { try { verified = await pollPaymentUntilVerified(reference); } catch (verifyErr) { console.warn( , verifyErr); } } try { const functionsBase = getSupabaseFunctionsUrl(); const serverUrl = functionsBase ? `${functionsBase}/verify-payment` : null; const getDurationDays = () => { if (plan.ai) return plan.duration?.includes('365') ? 365 : 30; if ((plan as any).business) { const bPlan = planKey; return bPlan === 'business_weekly' ? 7 : 30; } if (planKey === 'basic') return 7; if (planKey === 'standard') return 14; if (planKey === 'premium') return 30; return 30; }; const durationDays = getDurationDays(); if (plan.ai) { await activateAiDb(user?.id || '', durationDays).catch(e => console.warn( , e)); } else if ((plan as any).service) { await activateSubscription(user?.id || '', 'service_monthly', 0, durationDays).catch(e => console.warn( , e)); } let serverActivationVerified = false; if (serverUrl) { const body: Record<string, unknown> = { activate_plan: true, plan_key: planKey, user_id: user?.id || '', duration_days: durationDays, credits: (plan as any).business ? 1 : plan.credits || 0, amount: plan.price, reference: reference || '', fallback_reference: reference || '', original_reference: originalPaymentReferenceRef.current || reference, card_token: savedCardTokenRef.current || '', }; if ((plan as any).business) { try { const raw = sessionStorage.getItem('jb_pending_advert'); if (raw) body.pending_advert = JSON.parse(raw); } catch {} } try { const resp = await fetch(serverUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body), }); const result = await resp.json().catch(() => ({})); if (resp.ok && result?.verified) { serverActivationVerified = true; console.log(`[Payment] Server-side activation successful for plan ${planKey}`); } else { console.warn( , result); } } catch (serverErr) { console.warn( , serverErr); } } if ((plan as any).business && !serverActivationVerified && user?.id) { try { await addAdvertCredits(user.id, 1); console.log( , user.id); } catch (fallbackErr) { console.warn( , fallbackErr); } } await new Promise((resolve) => window.setTimeout(resolve, 2500)); if (plan.ai) { await fetchAiSubscription().catch(() => {}); } else { await fetchSubscription().catch(() => {}); } if (!receiptSentRef.current.has(reference) && user?.email) { receiptSentRef.current.add(reference); sendEmail({ type: , email: user.email, name: customerName, plan: plan.name, amount: String(plan.price), }); } if ((plan as any).business) { try { const raw = sessionStorage.getItem('jb_pending_advert'); if (raw) { const pending = JSON.parse(raw); const normalizedPackage = planKey.replace(/^business_/, ); const existingAds = await fetchAdvertisementsByOwner(user.id); const hasDuplicate = existingAds.some( (ad) => ad.title === pending.title && ad.package === normalizedPackage && ad.owner_id === user.id, ); if (!hasDuplicate) { const durationDays = plan.duration && plan.duration.includes('30') ? 30 : 7; const starts_at = new Date().toISOString(); const expires_at = new Date(Date.now() + durationDays * 24 * 60 * 60 * 1000).toISOString(); const advert = await createAdvertisement({ owner_id: user.id, business_name: pending.businessName || user.user_metadata?.full_name || user.email, title: pending.title, description: pending.description, category: pending.category || 'Other', package: normalizedPackage, is_featured: planKey === 'business_featured' || pending.featured || false, starts_at, expires_at, amount_paid: plan.price, }); if (user?.email) { try { sendEmail({ type: 'advert_created', email: user.email, name: customerName, advertId: advert?.id ?? null, } as any); } catch (e) { console.warn('Failed to send advert created email:', e); } } } try { sessionStorage.removeItem('jb_pending_advert'); } catch { } } } catch (e) { console.warn('Failed to create advertisement after payment:', e); } } } catch (backgroundErr) { console.warn( , backgroundErr); } finally { try { clearPendingReference(); } catch { } cleanupKora(); window.location.replace(successTarget); } }; const onKoraSuccess = async (data: { reference: string; payment_reference?: string; transaction_reference?: string; amount: string; status: string; }, reference: string) => { koraCompletedRef.current = true; const nextReference = data.reference || data.payment_reference || data.transaction_reference || reference; if (nextReference !== reference) { try { sessionStorage.setItem(PENDING_PAYMENT_STORAGE_KEY, nextReference); } catch { } } setPaymentReference(nextReference); await handleSuccessfulPayment(nextReference, false); }; const onKoraFailed = (data: { reference: string; payment_reference?: string; transaction_reference?: string; status: string; }, reference: string) => { koraCompletedRef.current = true; const failedReference = data.reference || data.payment_reference || data.transaction_reference || reference; setPaymentReference(failedReference); setStep( ); setPaying(true); setError( `KoraPay reported a failed payment. We are verifying it now. Ref: ${failedReference}`, ); push({ message: , }); cleanupKora(); }; const onKoraPending = (reference: string) => { koraCompletedRef.current = true; setStep( ); setPaying(true); setError( `Payment submitted. Waiting for secure verification for ref: ${reference}`, ); push({ message: , }); cleanupKora(); }; const onKoraClose = () => { koraCompletedRef.current = true; cleanupKora(); if (!paid) { clearPendingReference(); setPaying(false); setStep( Payment cancelled before completion. ) return; if (window.Korapay) { setKoraReady(true); setKoraLoading(false); setError( ); return; } if (document.getElementById( )) { setKoraReady(false); setKoraLoading(true); return; } setKoraReady(false); setKoraLoading(true); const script = document.createElement( ); script.id = ; script.type = ; script.src = KORA_SCRIPT_SRC; script.async = true; script.crossOrigin = ; script.referrerPolicy = ; const finalizeLoad = () => { if (window.Korapay) { setKoraReady(true); setKoraLoading(false); setError( ); return; } koraRetryCountRef.current += 1; const shouldRetry = koraRetryCountRef.current < MAX_KORA_SCRIPT_LOAD_RETRIES; if (shouldRetry) { const existing = document.getElementById( ); existing?.remove(); setTimeout(loadKoraScript, 1200); return; } setKoraLoading(false); console.error( ); setError( , ); }; script.onload = () => { window.setTimeout(finalizeLoad, 600); }; script.onerror = () => { koraRetryCountRef.current += 1; const shouldRetry = koraRetryCountRef.current < MAX_KORA_SCRIPT_LOAD_RETRIES; if (shouldRetry) { const existing = document.getElementById( ); existing?.remove(); setTimeout(loadKoraScript, 1200); return; } setKoraLoading(false); console.error( ); setError( , ); }; document.body.appendChild(script); }; useEffect(() => { loadKoraScript(); }, []); useEffect(() => { const handleGlobalError = (event: ErrorEvent) => { if (typeof event.message === && suppressKoraPay(event.message, event.filename)) { event.preventDefault(); } }; const handleUnhandledRejection = (event: PromiseRejectionEvent) => { const reason = event.reason || {}; const message = typeof reason?.message === ? reason.message : String(reason); const source = typeof reason?.filename === ? reason.filename : undefined; if (suppressKoraPay(message, source)) { event.preventDefault(); } }; window.addEventListener( , handleGlobalError); window.addEventListener( , handleUnhandledRejection); return () => { window.removeEventListener( , handleGlobalError); window.removeEventListener( , handleUnhandledRejection); cleanupKora(); }; }, []); useEffect(() => { if (!paymentReference || step !== ) return; let cancelled = false; let attempts = 0; const maxAttempts = 5; const poll = async () => { attempts += 1; try { const payment = await fetchPaymentByReference(paymentReference); if (cancelled) return; if (!payment) { if (attempts >= maxAttempts) { setPaying(false); setError( , ); } return; } const status = String(payment.status || ).toLowerCase(); if (status === ) { await handleSuccessfulPayment(paymentReference, false); return; } if (status === ) { clearPendingReference(); setPaying(false); setStep( ); setError( `Payment ${status}. No plan was activated. Please try again or contact support with ref: ${paymentReference}`, ); return; } if (attempts >= maxAttempts) { setPaying(false); setError( `Payment received. We're still waiting for secure verification from KoraPay for ref: ${paymentReference}.`, ); } } catch (pollError) { console.error( , pollError); if (attempts >= maxAttempts) { setPaying(false); setError( `Could not confirm payment status yet. Please refresh this page and check ref: ${paymentReference}`, ); } } }; poll(); const interval = window.setInterval(poll, 1000); return () => { cancelled = true; window.clearInterval(interval); }; }, [ paymentReference, step, plan.ai, plan.name, plan.price, plan.duration, planKey, fetchAiSubscription, fetchSubscription, user?.email, customerName, ]); const handlePayWithKora = async () => { if (!user?.id) { navigate(loginRedirect); return; } if (!koraReady || typeof window.Korapay === ) { setError( ); loadKoraScript(); return; } const publicKey = import.meta.env.VITE_KORA_PUBLIC_KEY || ; if (!publicKey) { setError( <> Kora not configured. Contact{ } <a href= className= > support </a> . </>, ); return; } const reference = + Math.random().toString(36).slice(2, 8); originalPaymentReferenceRef.current = reference; const functionsBaseUrl = getSupabaseFunctionsUrl(); const notificationUrl = functionsBaseUrl ? `${functionsBaseUrl}/kora-webhook` : undefined; initializePaymentState(); const paymentMetadata = buildPaymentMetadata(); try { await recordPendingPayment(reference, paymentMetadata); await notifyPendingPayment(reference); } catch (recordError) { console.error( , recordError); setPaying(false); setStep( Could not start payment securely. Please try again. , notification_url: notificationUrl, customer: { name: customerName, email: customerEmail, }, metadata: { plan: planKey, user_id: user.id, }, onSuccess: async (data) => { try { await onKoraSuccess(data, reference); } catch (e) { console.error( , e); } }, onTokenized: (data) => { try { const card = data?.card; savedCardTokenRef.current = data?.token || card?.token || data?.card_token || ; } catch { savedCardTokenRef.current = ; } }, onFailed: (data) => { try { onKoraFailed(data, reference); } catch (e) { console.error( , e); } }, onPending: () => { try { onKoraPending(reference); } catch (e) { console.error( , e); } }, onClose: () => { try { onKoraClose(); } catch (e) { console.error( , e); } }, }); } catch (koraError) { console.error( , koraError); setCheckoutStarted(false); setPaying(false); setStep( ); setError( , ); } }; function renderKoraCheckoutScreen() { if (authLoading) { return ( <div className= > <div className= > <div className= > <Loader2 className= /> </div> <h1 className= > Checking your account </h1> <p className= > Please wait while we confirm your login status. </p> </div> </div> ); } if (!isAuthenticated) { return ( <div className= > <div className= > <div className= > <Lock className= /> </div> <h1 className= > Please log in to continue </h1> <p className= > You must be logged in to complete payment. Sign in now to finish checkout. </p> </div> <button onClick={() => { window.location.href = loginRedirect; }} className= > Login to continue </button> </div> ); } const isProcessing = step === ; return ( <div className= > <div className= > <div className= > <Wallet className= /> </div> <h1 className= > Pay with KoraPay </h1> <p className= >{plan.name}</p> </div> <div className= > <div className= > <span className= >Plan</span> <span className= > {plan.name} </span> </div> <div className= > <span className= >Duration</span> <span className= > {plan.duration} </span> </div> <div className= > <span className= >Total</span> <span className= > {formatNaira(plan.price)} </span> </div> </div> <div className= > <div className= > <div className= > <Zap className= /> </div> <div> <p className= > {isProcessing ? } </p> <p className= >Powered by KoraPay</p> </div> </div> <div className= > {(isProcessing ? [ ] ).map((method) => ( <span key={method} className= > <svg className= fill= viewBox= stroke= strokeWidth={2} > <path strokeLinecap= strokeLinejoin= d= /> </svg> {method} </span> ))} </div> </div> {error && ( <div className={`mb-4 p-3 rounded-xl text-sm flex items-start gap-2 ${isProcessing ? bg-red-50 border border-red-100 text-red-700 }`} fill= /> <span>{error}</span> </div> )} {isProcessing && ( <div className= > <button onClick={async () => { if (!paymentReference) return; const functionsBaseUrl = getSupabaseFunctionsUrl(); if (!functionsBaseUrl) { push({ message: }); return; } try { push({ message: }); const res = await fetch(`${functionsBaseUrl}/verify-payment`, { method: , headers: { }, body: JSON.stringify({ reference: paymentReference, fallback_reference: originalPaymentReferenceRef.current || paymentReference, original_reference: originalPaymentReferenceRef.current || paymentReference, }), }); const body = await res.json().catch(() => ({})); if (res.ok && body?.verified) { try { if (plan.ai) await fetchAiSubscription(); else await fetchSubscription(); } catch {} clearPendingReference(); setPaid(true); setStep( ); setPaying(false); push({ message: }); setTimeout(() => navigate(successTarget), 600); return; } push({ message: }); } catch (e) { console.error( , e); push({ message: }); } }} className= > Continue to feature (check now) </button> </div> )} <button onClick={handlePayWithKora} disabled={paying || isProcessing || checkoutStarted || isGatewayLoading || isGatewayUnavailable} className= > {paying ? ( <span className= > <Loader2 className= /> {isProcessing ? } </span> ) : isCheckoutOpening ? ( <span className= > <Loader2 className= /> Starting checkout... </span> ) : isGatewayLoading ? ( <span className= > <Loader2 className= /> Loading payment gateway... </span> ) : isGatewayUnavailable ? ( <span className= > <Loader2 className= /> Retrying gateway setup... </span> ) : ( <span className= > <Lock className= /> Pay {formatNaira(plan.price)} securely </span> )} </button> {isGatewayUnavailable && !paying && !isProcessing && ( <button onClick={() => { setError( ); loadKoraScript(); }} className= > Retry payment gateway </button> )} <p className= > Your plan activates after secure server-side verification </p> </div> ); } function renderSuccessScreen() { return ( <div className= > <div className= > <div className= > <CheckCircle className= /> </div> <h1 className= > Payment Successful! </h1> <p className= > Redirecting you to your <strong className= >{plan.name}</strong> feature now... </p> </div> <div className= > <div className= > <div className= > <Loader2 className= /> </div> <div className= > <p className= > Taking you there now </p> <p className= > Your {plan.name} is ready to use </p> </div> </div> </div> <div className= > <button onClick={() => window.location.replace(successTarget)} className= > <Sparkles className= /> {plan.ai ? : plan.service ? : plan.business ? } </button> </div> </div> ); } return ( <> {paid && ( <div className= style={{ animation: }} > <div className= style={{ animation: }} > {renderSuccessScreen()} </div> </div> )} <div className={`min-h-screen bg-white pb-24 ${paid ? }`} > <Header /> <div className= > <div className= > <div className= /> <div className= /> <div className= > <p className= > Secure payment </p> <h2 className= > Pay for your plan with KoraPay and complete checkout securely. </h2> <p className= > Use card, USSD or bank transfer and get fast verification from our secure payment gateway. </p> <div className= > {[ { label: }, { label: }, { label: }, ].map((item) => ( <div key={item.label} className= > <div className= >{item.icon}</div> <p className= > {item.label} </p> </div> ))} </div> </div> </div> </div> <div className= > <div className= > <div className= /> <div className= /> </div> <div className= > <button onClick={() => navigate( )} className= > <ArrowLeft className= /> Back to pricing </button> <h1 className= > Complete Checkout </h1> <p className= > Pay for <strong className= >{plan.name}</strong> with KoraPay and activate after secure verification </p> </div> </div> <div className= > <div className= > <div className= > <div className= > <div className= > <div className= > <div className= > <div className= /> <div className= /> <div className= /> </div> <span className= > jobbridge.com.ng/payment </span> </div> <div className= > <Sparkles className= /> Secure Checkout </div> </div> </div> <div className= >{renderKoraCheckoutScreen()}</div> </div> <div className= > <div className= > <div className= > <div className= > <div className= > <div className= /> <div className= /> <div className= /> </div> <span className= > Order Summary </span> </div> <div className= > <Sparkles className= /> Summary </div> </div> </div> <div className= > <div className= > <div className= > <div className= > <Receipt className= /> </div> <h2 className= > Order Summary </h2> </div> <div className= > <div className= > <div className= > {plan.name.charAt(0)} </div> <div className= > <p className= > {plan.name} </p> <p className= >{plan.duration}</p> </div> </div> <div className= > <div className= > <span className= >Credits</span> <span className= > {getPlanCreditsLabel()} </span> </div> <div className= > <span className= >Subtotal</span> <span className= > {formatNaira(plan.price)} </span> </div> <div className= > <span className= > Total </span> <span className= > {formatNaira(plan.price)} </span> </div> </div> </div> <div className= > <p className= > <Sparkles className= /> {step === : step === } </p> <div className= > {getWhatHappensNext().map((item, i) => ( <div key={i} className= > <div className={`w-6 h-6 rounded-full flex items-center justify-center shrink-0 mt-0.5 ${ step === : step === }`} > {step === ? ( <svg className= fill= viewBox= stroke= strokeWidth={2.5} > <path strokeLinecap= strokeLinejoin= d= /> </svg> ) : step === ? ( <Loader2 className= /> ) : ( <div className= /> )} </div> <p className={`text-sm ${step === }`} > {item} </p> </div> ))} </div> </div> <div className= > <ShieldCheck className= /> <span>Protected by SSL encryption</span> </div> </div> </div> </div> </div> </div> <div className= > <div className= > <span className= > <Lock className= /> Secure checkout </span> <span className= >|</span> <span className= > <ShieldCheck className= /> SSL encrypted </span> <span className= >|</span> <span className= > <Wallet className= ArrowLeft, Wallet, ShieldCheck, CheckCircle, Loader2, Lock, Receipt, Sparkles, Circle, Zap, from \"lucide-react\"; createAdvertisement, fetchAdvertisementsByOwner, activateSubscription, activateAiSubscription as activateAiDb, addAdvertCredits, from \"../lib/supabaseQueries\"; declare global interface Window Korapay: initialize: (config: KoraPayConfig) => void; close: () => void; ; const isKoraScriptSource = (source?: string | null) => if (!source) return false; return source.includes(\"korapay\") || source.includes(\"korablobstorage\") || source.includes(\"korapay-collections\"); ; const suppressKoraPay = (message: string, source?: string | null) => if (typeof message !== \"string\") return false; const sanitized = message.trim(); const isScriptError = sanitized === \"Script error.\" || sanitized === \"Script error\"; if ( sanitized.includes(\"hasAttribute\") || sanitized.includes(\"Cannot read properties of null\") || isScriptError ) if (isScriptError) return isKoraScriptSource(source); const stack = new Error().stack || \"\"; if ( stack.includes(\"korapay\") || stack.includes(\"korapay-collections\") || isKoraScriptSource(source) ) console.warn( \"[KoraPay] Suppressed cleanup/error issue (iOS/Safari compatibility)\", message, source, ); return true; return false; ; interface KoraPayConfig key: string; reference: string; amount: number; currency?: string; customer: name: string; email: string ; notification_url?: string; narration?: string; channels?: string[]; default_channel?: string; metadata?: Record ; onClose?: () => void; onSuccess?: (data: reference: string; payment_reference?: string; transaction_reference?: string; amount: string; status: string; ) => void; onFailed?: (data: reference: string; payment_reference?: string; transaction_reference?: string; status: string; ) => void; onPending?: () => void; onTokenized?: (data: token?: string; card?: token?: string ; card_token?: string; ) => void; merchant_bears_cost?: boolean; const PLANS: Record = basic: name: \"Basic Job Post\", duratio"
    },
    {
      "file": "Pricing.tsx",
      "path": "src/pages/Pricing.tsx",
      "title": "JobBridge Pricing",
      "text": "JobBridge Pricing Job Posting Plans {plan.name} Job Post Provider Recruitment & Placement Fee Low-Skilled Jobs Skilled Jobs Service Provider Listing Plans {plan.name} How Search Results Work Business Advertisement Plans {plan.name} JobBridge AI Career Suite {plan.name} Training & Certification {course.name} Need help choosing the right training? JobBridge Pricing Choose the right plan for your needs {label} Job Posting Plans {plan.popular && ( Most Popular {plan.name} Job Post ₦{plan.price.toLocaleString()} {plan.duration} {feature} {plan.name === 'Basic' ? 'Choose Basic' : plan.name === 'Standard' ? 'Choose Standard' : 'Choose Premium'} Provider Featured Job Top of Search Results Homepage Banner Recruitment & Placement Fee When JobBridge successfully helps you hire: Low-Skilled Jobs Security guards, Drivers, Cleaners, Sales attendants, Dispatch riders ₦5,000 per hire Skilled Jobs Teachers, Accountants, Managers, IT Staff ₦10,000 per hire Service Provider Listing Plans {plan.popular && ( Best Value {plan.name} {plan.price} {plan.note} {feature} {plan.tier === 'basic' ? ( Get Started Free Subscribe Now How Search Results Work Featured professionals appear at the top, followed by verified, then basic listings: Tunde Designs Creative Hub John Graphics Sarah Graphics Business Advertisement Plans Promote your restaurant, hotel, fashion brand, phone repair shop, school, hospital, and more. {plan.popular && ( Most Popular {plan.name} ₦{plan.price.toLocaleString()} {plan.duration} {feature} Create Advert JobBridge AI Career Suite Unlock powerful AI tools designed to help you stand out and get hired faster. {plan.name} {plan.savetag && ( {plan.savetag} ₦{plan.price.toLocaleString()} {plan.period} {feature} Subscribe Training & Certification Get trained by JobBridge in essential skills and earn recognized certifications. Coming Soon {course.name} {course.desc} Duration: {course.duration} ₦{course.price.toLocaleString()} Need help choosing the right training? Click \"Get Trained\" to chat with us on WhatsApp. We'll help you find the right training program based on your interests, experience, and career goals, and get certified. Get Trained min-h-screen bg-gray-50 pb-24 max-w-6xl mx-auto px-4 py-6 bg-gradient-to-r from-blue-700 to-blue-800 rounded-2xl p-6 mb-6 text-white relative overflow-hidden /> <img src= className= /> <div className= > <h1 className= >JobBridge Pricing</h1> <p className= >Choose the right plan for your needs</p> </div> </div> { } <div className= > {[ { id: 'jobs', label: 'Job Postings', icon: Briefcase }, { id: 'services', label: 'Service Marketplace', icon: ShoppingBag }, { id: 'business', label: 'Business Ads', icon: Users }, { id: 'ai', label: 'AI Career Tools', icon: Bot }, { id: 'training', label: 'Training', icon: BookOpen }, ].map(({ id, label, icon: Icon }) => ( <button key={id} onClick={() => setActiveTab(id as typeof activeTab)} className={`flex items-center gap-2 px-4 py-2.5 rounded-xl font-medium text-sm whitespace-nowrap transition-colors ${ activeTab === id ? 'bg-blue-700 text-white' : 'bg-white text-gray-700 border border-gray-200 hover:border-blue-300' }`} > <Icon className= /> {label} </button> ))} </div> { } {activeTab === 'jobs' && ( <AnimatedSection direction= > <div> <h2 className= >Job Posting Plans</h2> <div className= > {[ { name: 'Basic', duration: '7 days', price: 2000, features: ['Job listing', 'Basic visibility', 'Up to 5 applicants', 'Email notifications'] }, { name: 'Standard', duration: '14 days', price: 3500, popular: true, features: ['Job listing', 'Medium visibility', 'Up to 15 applicants', 'Email + SMS alerts', 'Priority support'] }, { name: 'Premium', duration: '30 days', price: 5000, features: ['Job listing', 'High visibility', 'Unlimited applicants', 'Priority support', 'Featured placement option', 'Social media promotion'] }, ].map((plan) => ( <Card3D key={plan.name} className={`bg-white rounded-xl p-5 border-2 ${plan.popular ? 'border-blue-500 relative' : 'border-gray-100'}`} strength={6} > {plan.popular && ( <div className= > Most Popular </div> )} <h3 className= >{plan.name} Job Post</h3> <div className= > <span className= >₦{plan.price.toLocaleString()}</span> </div> <p className= >{plan.duration}</p> <ul className= > {plan.features.map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> <button onClick={() => handleChoosePlan(plan.name.toLowerCase())} className= > {plan.name === 'Basic' ? 'Choose Basic' : plan.name === 'Standard' ? 'Choose Standard' : 'Choose Premium'} </button> </Card3D> ))} </div> </div> <div className= > { } <Card3D className= strength={6}> <h3 className= > <Star className= /> Provider </h3> <div className= > <div className= > <span className= >Featured Job</span> <span className= >₦1,000</span> </div> <div className= > <span className= >Top of Search Results</span> <span className= >₦3,000</span> </div> <div className= > <span className= >Homepage Banner</span> <span className= >₦5,000</span> </div> </div> <button onClick={() => window.open('https: Learn More </button> </Card3D> { } <Card3D className= strength={6}> <h3 className= > <Users className= /> Recruitment & Placement Fee </h3> <p className= >When JobBridge successfully helps you hire:</p> <div className= > <div className= > <h4 className= >Low-Skilled Jobs</h4> <p className= >Security guards, Drivers, Cleaners, Sales attendants, Dispatch riders</p> <p className= >₦5,000 per hire</p> </div> <div className= > <h4 className= >Skilled Jobs</h4> <p className= >Teachers, Accountants, Managers, IT Staff</p> <p className= >₦10,000 per hire</p> </div> </div> </Card3D> </div> </div></AnimatedSection> )} { } {activeTab === 'services' && ( <AnimatedSection direction= > <h2 className= >Service Provider Listing Plans</h2> <div className= > {[ { name: 'Monthly Listing', price: 'FREE', note: 'at launch', priceNum: 0, features: ['Profile on JobBridge', 'Name and contact info', 'Description of services', 'Location', 'Receive inquiries'], tier: 'basic', }, { name: 'Verified Professional', price: '₦3,000', note: '/month', priceNum: 3000, popular: true, features: ['Everything in Monthly Listing', 'Verified badge ✓', 'ID verification', 'Phone verification', 'Increased trust with customers'], tier: 'verified', }, { name: 'Featured Professional', price: '₦5,000', note: '/month', priceNum: 5000, features: ['Everything in Verified', 'Featured badge ⭐', 'Top of search results', 'Priority placement', 'Featured on homepage', 'Promotion on WhatsApp & social media'], tier: 'featured', }, ].map((plan) => ( <Card3D key={plan.name} className={`bg-white rounded-xl p-5 border-2 ${plan.popular ? 'border-blue-500 relative' : 'border-gray-100'}`} strength={6} > {plan.popular && ( <div className= > Best Value </div> )} <h3 className= >{plan.name}</h3> <div className= > <span className= >{plan.price}</span> <span className= >{plan.note}</span> </div> <ul className= > {plan.features.map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> {plan.tier === 'basic' ? ( <Link to= className= > Get Started Free </Link> ) : ( <button onClick={() => navigate(`/payment?plan=service_${plan.tier}`)} className= > Subscribe Now </button> )} </Card3D> ))} </div> <Card3D className= strength={5}> <h3 className= >How Search Results Work</h3> <p className= >Featured professionals appear at the top, followed by verified, then basic listings:</p> <div className= > <div className= > <span className= >⭐</span> <span className= >Tunde Designs</span> <span className= >Featured</span> </div> <div className= > <span className= >⭐</span> <span className= >Creative Hub</span> <span className= >Featured</span> </div> <div className= > <span className= >✓</span> <span className= >John Graphics</span> <span className= >Verified</span> </div> <div className= > <span className= >○</span> <span className= >Sarah Graphics</span> <span className= >Basic</span> </div> </div> </Card3D> </div></AnimatedSection> )} { } {activeTab === 'business' && ( <AnimatedSection direction= > <h2 className= >Business Advertisement Plans</h2> <p className= >Promote your restaurant, hotel, fashion brand, phone repair shop, school, hospital, and more.</p> <div className= > {[ { name: 'Weekly Ad', duration: '7 days', price: 2000, features: ['Display on platform', 'Category placement', 'Contact info visible', 'View analytics'] }, { name: 'Monthly Ad', duration: '30 days', price: 7500, popular: true, features: ['Everything in Weekly', 'Extended visibility', 'Priority listing', 'Performance reports', 'Editable content'] }, { name: 'Featured Business', duration: '30 days', price: 15000, features: ['Everything in Monthly', 'Homepage spotlight', 'Category prominence', 'Social media boost', 'WhatsApp status promo', 'Dedicated banner'] }, ].map((plan) => ( <Card3D key={plan.name} className={`bg-white rounded-xl p-5 border-2 ${plan.popular ? 'border-blue-500 relative' : 'border-gray-100'}`} strength={6} > {plan.popular && ( <div className= > Most Popular </div> )} <h3 className= >{plan.name}</h3> <div className= > <span className= >₦{plan.price.toLocaleString()}</span> </div> <p className= >{plan.duration}</p> <ul className= > {plan.features.map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> <Link to= className= > Create Advert </Link> </Card3D> ))} </div> </div></AnimatedSection> )} { } {activeTab === 'ai' && ( <AnimatedSection direction= > <div className= > <div className= > <Bot className= /> <h2 className= >JobBridge AI Career Suite</h2> </div> <p className= >Unlock powerful AI tools designed to help you stand out and get hired faster.</p> </div> <div className= > {[ { name: 'Monthly Subscription', price: 1500, period: '/month', features: ['AI CV Builder', 'AI Professional CV Upgrade', 'AI Cover Letter Generator', 'AI Interview Preparation', 'Multiple CV Templates', 'Job Application Tracker', 'AI Career Assistant'] }, { name: 'Annual Subscription', price: 15000, period: '/year', savetag: 'Save 15%+', features: ['Everything in Monthly', 'Priority support', 'Early access to new features', 'Unlimited CV revisions', 'Export to PDF/Word', 'Custom templates', 'All future updates'] }, ].map((plan) => ( <Card3D key={plan.name} className= strength={6}> <h3 className= >{plan.name}</h3> {plan.savetag && ( <span className= >{plan.savetag}</span> )} <div className= > <span className= >₦{plan.price.toLocaleString()}</span> <span className= >{plan.period}</span> </div> <ul className= > {plan.features.map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> <button onClick={() => navigate(`/payment?plan=ai_${plan.name === 'Monthly Subscription' ? 'monthly' : 'annual'}`)} className= > Subscribe </button> </Card3D> ))} </div> </div></AnimatedSection> )} { } {activeTab === 'training' && ( <AnimatedSection direction= > <h2 className= >Training & Certification</h2> <p className= >Get trained by JobBridge in essential skills and earn recognized certifications.</p> <div className= > {[ { name: 'Customer Service', price: 5000, duration: '4 weeks', desc: 'Learn professional customer handling, communication, and service excellence.' }, { name: 'Microsoft Office', price: 10000, duration: '6 weeks', desc: 'Master Word, Excel, PowerPoint, and Outlook for professional environments.' }, { name: 'Digital Skills', price: 15000, duration: '8 weeks', desc: 'Build skills in digital marketing, social media, and online business tools.' }, { name: 'Sales Training', price: 5000, duration: '4 weeks', desc: 'Learn effective selling techniques, negotiation, and closing strategies.' }, ].map((course) => ( <Card3D key={course.name} className= strength={6}> <div className= > <span className= >Coming Soon</span> </div> <div className= > <BookOpen className= /> <h3 className= >{course.name}</h3> </div> <p className= >{course.desc}</p> <div className= > <span>Duration: {course.duration}</span> </div> <div className= > <span className= >₦{course.price.toLocaleString()}</span> </div> </Card3D> ))} </div> <Card3D className= strength={5}> <div className= > <MessageCircle className= /> <div> <h3 className= >Need help choosing the right training?</h3> <p className= to chat with us on WhatsApp. We'll help you find the right training program based on your interests, experience, and career goals, and get certified.</p> <a href= rel= className= > <MessageCircle className= /> Get Trained <ExternalLink className= const openModal = useModal(); const navigate = useNavigate(); const location = useLocation(); const handleChoosePlan = (plan: string) => navigate(`/payment?plan=$ plan `); ; const [activeTab, setActiveTab] = useState ('jobs'); useEffect(() => if (!location.hash) return; const h = location.hash.replace('#', ''); if (h === 'ai') setActiveTab('ai'); else if (h === 'services') setActiveTab('services'); else if (h === 'business') setActiveTab('business'); else if (h === 'training') setActiveTab('training'); else if (h === 'jobs') setActiveTab('jobs'); , [location.hash]); return ( JobBridge Pricing Choose the right plan for your needs [ id: 'jobs', label: 'Job Postings', icon: Briefcase , id: 'services', label: 'Service Marketplace', icon: ShoppingBag , id: 'business', label: 'Business Ads', icon: Users , id: 'ai', label: 'AI Career Tools', icon: Bot , id: 'training', label: 'Training', icon: BookOpen , ].map(( id, label, icon: Icon ) => ( setActiveTab(id as typeof activeTab) className= `flex items-center gap-2 px-4 py-2.5 rounded-xl font-medium text-sm whitespace-nowrap transition-colors $ activeTab === id ? 'bg-blue-700 text-white' : 'bg-white text-gray-700 border border-gray-200 hover:border-blue-300' ` > label )) activeTab === 'jobs' && ( Job Posting Plans [ name: 'Basic', duration: '7 days', price: 2000, features: ['Job listing', 'Basic visibility', 'Up to 5 applicants', 'Email notifications'] , name: 'Standard', duration: '14 days', price: 3500, popular: true, features: ['Job listing', 'Medium visibility', 'Up to 15 applicants', 'Email + SMS alerts', 'Priority support'] , name: 'Premium', duration: '30 days', price: 5000, features: ['Job listing', 'High visibility', 'Unlimited applicants', 'Priority support', 'Featured placement option', 'Social media promotion'] , ].map((plan) => ( plan.popular && ( Most Popular ) plan.name Job Post ₦ plan.price.toLocaleString() plan.duration plan.features.map((feature) => ( feature )) handleChoosePlan(plan.name.toLowerCase()) clas"
    },
    {
      "file": "Privacy.tsx",
      "path": "src/pages/Privacy.tsx",
      "title": "Privacy Center",
      "text": "Privacy Center Our Privacy Promise {item.title} {section.title} Have a Privacy Question? Back to Home Privacy Center Last updated: June 2026 Your privacy matters to us. This policy explains how JobBridge collects, uses, stores, and protects your personal information when you use our platform. Our Privacy Promise {item.title} {item.desc} {idx + 1} {section.title} {section.content} Have a Privacy Question? Our support team is ready to help with any questions about your data and privacy. Email Privacy Team Visit Support Page date at the top of this page will reflect the most recent revision. Your continued use of the platform after changes constitutes acceptance of the updated policy.', }, { title: 'Contact Us', content: 'If you have questions, concerns, or requests regarding your privacy or this policy, please contact our support team: Email jobbridgesupport@gmail.com, visit the Support page at /support, or use the WhatsApp chat at 09136171354. We aim to respond to all privacy inquiries within 48 hours.', }, ]; return ( <div className= > <Header /> <main className= inline-flex items-center gap-2 text-sm font-medium text-blue-700 hover:text-blue-800 mb-6 /> Back to Home </Link> { } <div className= > <div className= > <div className= /> <div className= /> </div> <div className= > <div className= > <Shield className= /> </div> <div> <h1 className= >Privacy Center</h1> <p className= >Last updated: June 2026</p> <p className= > Your privacy matters to us. This policy explains how JobBridge collects, uses, stores, and protects your personal information when you use our platform. </p> </div> </div> </div> { } <AnimatedSection direction= > <div className= > <h2 className= >Our Privacy Promise</h2> <div className= > {[ { title: 'We never sell your data', desc: 'Your personal information is yours. We do not sell it to anyone.' }, { title: 'You stay in control', desc: 'View, edit, or delete your data anytime from your Profile and Settings.' }, { title: 'Secure by default', desc: 'Encrypted in transit and at rest. Industry-standard security practices.' }, ].map((item) => ( <div key={item.title} className= > <CheckCircle className= /> <h3 className= >{item.title}</h3> <p className= >{item.desc}</p> </div> ))} </div> </div> </AnimatedSection> { } <AnimatedSection direction= delay={100}> <div className= > {SECTIONS.map((section, idx) => ( <details key={section.title} className= > <summary className= > <div className= > <span className= > {idx + 1} </span> <h2 className= >{section.title}</h2> </div> <ChevronRight className= /> </summary> <div className= > <p className= >{section.content}</p> </div> </details> ))} </div> </AnimatedSection> { } <AnimatedSection direction= delay={200}> <div className= > <div className= > <div className= /> </div> <div className= > <Mail className= /> <h2 className= >Have a Privacy Question?</h2> <p className= > Our support team is ready to help with any questions about your data and privacy. </p> <div className= > <a href= className= > <Mail className= /> Email Privacy Team </a> <Link to= className= > Visit Support Page <ChevronRight className= const SECTIONS = [ title: 'What Information We Collect', content: 'We collect only the information you provide to us: your name, email address, phone number, profile details (professional headline, experience, skills, education, location), resume/CV documents, job application data, payment information (processed securely by KoraPay — we never store card details), and communications with other users. We also collect basic usage data like page views and feature interactions to improve our platform.', , title: 'How We Use Your Information', content: 'Your information is used to: create and maintain your account, match you with relevant job opportunities, enable recruiters to find your profile, process your applications, provide AI-powered features (resume builder, cover letter generator, interview prep), process payments and activate subscriptions, send important account notifications, improve and personalize your experience on the platform.', , title: 'Information Sharing', content: 'We do not sell your personal data to third parties. Your profile information is visible to recruiters and employers on the platform as part of the job matching process. We share data with trusted service providers strictly for platform operations: KoraPay (payment processing), OpenAI (AI features — anonymized queries), Supabase (database hosting), and Resend (transactional emails). These providers are contractually bound to protect your data.', , title: 'Data Storage & Security', content: 'Your data is stored securely on Supabase infrastructure with encryption in transit (TLS) and at rest. Passwords are hashed and never stored in plain text. Access to your data is restricted to authenticated users with appropriate permissions. We implement security best practices including rate limiting, input sanitization, and regular security reviews. However, no online platform can guarantee 100% security — we encourage you to use strong passwords and enable two-factor authentication.', , title: 'Your"
    },
    {
      "file": "Profile.tsx",
      "path": "src/pages/Profile.tsx",
      "title": "Loading your profile...",
      "text": "Loading your profile... Please sign in {form.full_name || 'Your name'} Keep your profile polished {section === 'personal' ? 'Personal details' : section === 'professional' ? 'Professional details' : section === 'provider' ? 'Provider details' : section} Change password Education Skills Honors & awards Languages {isEditing ? 'Edit' : 'Add'} {section.charAt(0).toUpperCase() + section.slice(1)} {percentage}% {field.label} {isTextarea ? ( ) : isSelect ? ( {option.label} Loading your profile... Please wait while we confirm your account. Please sign in You must be signed in to view and edit your profile. Sign in to continue {avatarUploading && ( Professional profile {form.full_name || 'Your name'} {profileHeadline} {roleLabel} {providerBadge && ( {providerBadge} {primaryLocation} Connections {experience.length + education.length + honors.length + languages.length || 0} Profile strength {profileStatus} View jobs Explore providers Edit profile Security Recent profile activity and insights that help recruiters connect with you faster. Profile views Your profile is visible to recruiters and providers across JobBridge. Profile strength {completionPct}% complete — {activeFields.length - filledFieldsCount} field{activeFields.length - filledFieldsCount !== 1 ? 's' : ''} remaining to reach 100%. Experience {experience.length === 0 ? ( No experience entries yet. Click \"Add\" to include your work history. {entry.title} {entry.company}{entry.period ? ` · ${entry.period}` : ''} {entry.description && {entry.description} openSectionEditor('experience', entry as unknown as Record Notifications Enable browser push notifications to receive job alerts and messages directly. {!pushSupported ? ( Push not supported or not configured. ) : pushSubscribed ? ( Disable Notifications Enable Notifications Profile editor Keep your profile polished Update your details to improve visibility with recruiters and clients. {filledFieldsCount}/{activeFields.length} fields • {completionPct}% complete Edit your profile directly in the fields below and save when ready. {savedBadgeMounted && ( Reset {saving ? ( Saving... ) : saveSuccess ? ( {saveSuccess && ( Profile updated successfully. {saveError} Loading your profile... {renderFormField(key, field)} Your email is read-only because it is linked to your account. Save your profile to make it visible to recruiters and providers. Reset changes {saving ? ( Saving... ) : saveSuccess ? ( Change password Update your account password securely from this page. Secure {passwordError && ( {passwordError} New password Confirm password Use a strong password of at least 8 characters. {passwordSaving ? ( Updating... {education.length === 0 ? ( No education entries yet. Click \"Add\" to include your academic background. {entry.institution} {entry.degree}{entry.period ? ` · ${entry.period}` : ''} openSectionEditor('education', entry as unknown as Record Endorse Brief skill description for recruiters and clients. Honors & awards {honors.length === 0 ? ( No honors or awards listed yet. Click \"Add\" to showcase your achievements. {entry.title} {entry.date && {entry.date} {entry.description} openSectionEditor('honors', entry as unknown as Record {languages.length === 0 ? ( No languages added yet. Click \"Add\" to list your language proficiencies. {entry.language} {entry.proficiency && {entry.proficiency} openSectionEditor('languages', entry as unknown as Record {isEditing ? 'Edit' : 'Add'} {section.charAt(0).toUpperCase() + section.slice(1)} {field.type === 'textarea' ? ( ) : field.type === 'select' ? ( {opt.label} Cancel {isEditing ? 'Save changes' : 'Add entry'} ../lib/profileValidation ; const PROFILE_FIELDS = { full_name: { label: , weight: 2, icon: User }, phone: { label: , weight: 1, icon: Phone }, location: { label: , weight: 1, icon: Globe }, professional_headline: { label: , weight: 2, icon: Award }, years_of_experience: { label: , weight: 1, icon: Clock }, bio: { label: , weight: 2, icon: BookOpen }, specialty: { label: , weight: 2, icon: Hash }, hourly_rate: { label: , weight: 1, icon: DollarSign }, skills: { label: , weight: 1, icon: Hash }, }; const DEFAULT_AVATAR = `data:image/svg+xml;utf8,${encodeURIComponent( '<svg xmlns= : percentage >= 50 ? ; return ( <div className= > <svg className= > <circle cx= /> <circle cx= r={radius} fill= stroke={color} strokeWidth= strokeLinecap= strokeDasharray={circumference} strokeDashoffset={strokeDashoffset} style={{ transition: }} /> </svg> <div className= > <span className= >{percentage}%</span> <span className= >Complete</span> </div> </div> ); } const { user, profile: userProfile, updatePassword, isAuthenticated, loading: authLoading } = useAuth(); const { push } = useToasts(); const navigate = useNavigate(); const [saveSuccess, setSaveSuccess] = useState(false); const [savedBadgeVisible, setSavedBadgeVisible] = useState(false); const [savedBadgeMounted, setSavedBadgeMounted] = useState(false); const [saving, setSaving] = useState(false); const [saveError, setSaveError] = useState( ); const [avatarUploading, setAvatarUploading] = useState(false); const [newPassword, setNewPassword] = useState( ); const [confirmPassword, setConfirmPassword] = useState( ); const [passwordError, setPasswordError] = useState( ); const [passwordSaving, setPasswordSaving] = useState(false); const [form, setForm] = useState<Record<string, string>>({}); const [profileLoading, setProfileLoading] = useState(true); const [avatarLoadFailed, setAvatarLoadFailed] = useState(false); const avatarInputRef = useRef<HTMLInputElement>(null); const initialFormRef = useRef<Record<string, string> | null>(null); const editorRef = useRef<HTMLDivElement | null>(null); const securityRef = useRef<HTMLDivElement | null>(null); const activityRef = useRef<HTMLDivElement | null>(null); const [experience, setExperience] = useState<ExperienceEntry[]>([]); const [education, setEducation] = useState<EducationEntry[]>([]); const [honors, setHonors] = useState<HonorEntry[]>([]); const [languages, setLanguages] = useState<LanguageEntry[]>([]); const [editingSection, setEditingSection] = useState<string | null>(null); const [editingItem, setEditingItem] = useState<Record<string, unknown> | null>(null); const [showSectionModal, setShowSectionModal] = useState(false); const openSectionEditor = (section: string, item: Record<string, unknown> | null = null) => { setEditingSection(section); setEditingItem(item); setShowSectionModal(true); }; const closeSectionEditor = () => { setEditingSection(null); setEditingItem(null); setShowSectionModal(false); }; const saveSectionItem = (section: string, data: Record<string, unknown>) => { switch (section) { case 'experience': { setExperience(prev => { const existing = editingItem?.id ? prev.map(e => e.id === editingItem.id ? { ...e, ...data } as ExperienceEntry : e) : [...prev, { id: generateId(), ...data } as unknown as ExperienceEntry]; return existing; }); break; } case 'education': { setEducation(prev => { const existing = editingItem?.id ? prev.map(e => e.id === editingItem.id ? { ...e, ...data } as EducationEntry : e) : [...prev, { id: generateId(), ...data } as unknown as EducationEntry]; return existing; }); break; } case 'honors': { setHonors(prev => { const existing = editingItem?.id ? prev.map(e => e.id === editingItem.id ? { ...e, ...data } as HonorEntry : e) : [...prev, { id: generateId(), ...data } as unknown as HonorEntry]; return existing; }); break; } case 'languages': { setLanguages(prev => { const existing = editingItem?.id ? prev.map(e => e.id === editingItem.id ? { ...e, ...data } as LanguageEntry : e) : [...prev, { id: generateId(), ...data } as unknown as LanguageEntry]; return existing; }); break; } } closeSectionEditor(); }; const deleteSectionItem = (section: string, id: string) => { switch (section) { case 'experience': setExperience(prev => prev.filter(e => e.id !== id)); break; case 'education': setEducation(prev => prev.filter(e => e.id !== id)); break; case 'honors': setHonors(prev => prev.filter(e => e.id !== id)); break; case 'languages': setLanguages(prev => prev.filter(e => e.id !== id)); break; } push({ message: `${section} entry removed.`, type: 'info' }); }; const loadSectionData = (freshRecord: Record<string, unknown>) => { const sections = freshRecord.profile_sections as Record<string, unknown> | undefined; if (sections) { if (Array.isArray(sections.experience)) setExperience(sections.experience as ExperienceEntry[]); if (Array.isArray(sections.education)) setEducation(sections.education as EducationEntry[]); if (Array.isArray(sections.honors)) setHonors(sections.honors as HonorEntry[]); if (Array.isArray(sections.languages)) setLanguages(sections.languages as LanguageEntry[]); } }; const normalizeAvatarUrl = (raw?: string) => { const value = (raw || ).trim(); if (!value || value === ; return value; }; const avatarSrc = avatarLoadFailed ? DEFAULT_AVATAR : normalizeAvatarUrl(form.avatar_url) || DEFAULT_AVATAR; useEffect(() => { setAvatarLoadFailed(false); }, [form.avatar_url]); useEffect(() => { let cancelled = false; async function loadProfile() { if (!user) { if (!cancelled) setProfileLoading(false); return; } if (!cancelled) { setProfileLoading(true); setSaveError( ); } try { const fresh = await fetchProfile(user.id); if (cancelled) return; if (fresh) { const fields: Record<string, string> = {}; const freshRecord = fresh as Record<string, unknown>; Object.keys(PROFILE_FIELDS).forEach((key) => { const val = freshRecord[key]; if (key === && Array.isArray(val)) { fields[key] = val.join( ); } else if (key === ) { fields[key] = val == null ? : formatPhoneInput(String(val)); } else { fields[key] = val == null ? : String(val); } }); fields.avatar_url = String(freshRecord.avatar_url || ); fields.email = fresh.email || user?.email || ; setForm(fields); initialFormRef.current = fields; loadSectionData(freshRecord); } else { const fallbackFields = { full_name: user.user_metadata?.full_name || , email: user.email || , phone: , location: , professional_headline: , years_of_experience: , bio: , specialty: , hourly_rate: , skills: , avatar_url: , }; setForm(fallbackFields); initialFormRef.current = fallbackFields; } } catch (err: unknown) { if (!cancelled) { const message = err instanceof Error && err.message ? err.message : ; setSaveError(message); } } finally { if (!cancelled) setProfileLoading(false); } } loadProfile(); return () => { cancelled = true; }; }, [user]); const activeFields = useMemo(() => { return Object.entries(PROFILE_FIELDS).filter(([key]) => { if ([ ].includes(key)) { return userProfile?.role === ; } return true; }); }, [userProfile?.role]); const completionPct = useMemo(() => { const totalWeight = activeFields.reduce((sum, [, field]) => sum + field.weight, 0); const filled = activeFields.reduce( (sum, [key, field]) => sum + (form[key]?.trim() ? field.weight : 0), 0, ); return totalWeight > 0 ? Math.round((filled / totalWeight) * 100) : 0; }, [form, activeFields]); const profileStatus = useMemo(() => { if (completionPct >= 85) return ; if (completionPct >= 60) return ; if (completionPct >= 35) return ; }, [completionPct]); const roleLabel = useMemo(() => { if (userProfile?.role === ; if (userProfile?.role === ; }, [userProfile?.role]); const providerBadge = useMemo(() => { if (userProfile?.role !== ) return null; if (userProfile?.is_featured) return ; if (userProfile?.is_verified) return ; }, [userProfile?.role, userProfile?.is_featured, userProfile?.is_verified]); const filledFieldsCount = useMemo( () => activeFields.filter(([key]) => !!form[key]?.trim()).length, [activeFields, form], ); const topSkills = useMemo(() => { if (!form.skills) return []; return form.skills .split( ) .map((skill) => skill.trim()) .filter(Boolean) .slice(0, 4); }, [form.skills]); const primaryLocation = form.location?.trim() || ; const profileHeadline = form.professional_headline || ; const sectionGroups = useMemo(() => { const groups: Record<string, typeof activeFields> = {}; activeFields.forEach(([key, field]) => { if (!groups[field.section]) groups[field.section] = []; groups[field.section].push([key, field]); }); return groups; }, [activeFields]); const updateField = (field: string, value: string) => { let nextValue = value; if (field === ) { nextValue = formatPhoneInput(value); } else if (field !== ) { nextValue = lightSanitizeProfileText(value); } setForm((prev) => ({ ...prev, [field]: nextValue })); }; useEffect(() => { if (!savedBadgeVisible) return; setSavedBadgeMounted(true); const hideTimer = window.setTimeout(() => setSavedBadgeVisible(false), 3200); return () => window.clearTimeout(hideTimer); }, [savedBadgeVisible]); useEffect(() => { if (!savedBadgeMounted || savedBadgeVisible) return; const cleanupTimer = window.setTimeout(() => setSavedBadgeMounted(false), 300); return () => window.clearTimeout(cleanupTimer); }, [savedBadgeMounted, savedBadgeVisible]); const saveButtonClass = `inline-flex items-center justify-center rounded-full px-5 py-2.5 text-sm font-semibold transition-all duration-200 ${ saving ? 'bg-blue-200 text-blue-800 cursor-wait' : saveSuccess || savedBadgeVisible ? 'bg-emerald-600 text-white shadow-xl shadow-emerald-500/20 transform scale-105 animate-pulse' : 'bg-blue-600 text-white hover:bg-blue-700' }`; const handleConnect = () => { navigate('/providers'); push({ message: 'Browse providers and connect with service professionals.', type: 'success' }); }; const handleMessage = () => { navigate('/support'); push({ message: 'Message support or explore help options.', type: 'info' }); }; const handleMore = () => { navigate('/about'); push({ message: 'Explore more JobBridge resources and tools.', type: 'info' }); }; const handleViewJobs = () => { navigate('/jobs'); push({ message: 'Opening the latest opportunities for you.', type: 'success' }); }; const handleExploreProviders = () => { navigate('/providers'); push({ message: 'Opening trusted service providers and specialists.', type: 'info' }); }; const [pushSupported, setPushSupported] = useState(false); const [pushSubscribed, setPushSubscribed] = useState(false); const VAPID_PUBLIC = import.meta.env.VITE_VAPID_PUBLIC_KEY || ''; useEffect(() => { if (typeof window === 'undefined') return; setPushSupported('serviceWorker' in navigator && 'PushManager' in window && !!VAPID_PUBLIC); (async () => { try { const reg = await registerServiceWorker(); if (!reg) return; const sub = await reg.pushManager.getSubscription(); setPushSubscribed(!!sub); } catch (e) { } })(); }, []); const handleSubscribeClick = async () => { try { if (!VAPID_PUBLIC) { push({ message: 'Push is not configured on this site.', type: 'error' }); return; } await subscribeToPush(VAPID_PUBLIC); setPushSubscribed(true); push({ message: 'Subscribed to browser notifications.', type: 'success' }); } catch (e) { push({ message: 'Could not subscribe: ' + (e instanceof Error ? e.message : String(e)), type: 'error' }); } }; const handleUnsubscribeClick = async () => { try { await unsubscribeFromPush(); setPushSubscribed(false); push({ message: 'Unsubscribed from browser notifications.', type: 'success' }); } catch (e) { push({ message: 'Could not unsubscribe: ' + (e instanceof Error ? e.message : String(e)), type: 'error' }); } }; const scrollToSection = (target: ) => { const section = target === ? editorRef.current : target === ? securityRef.current : activityRef.current; section?.scrollIntoView({ behavior: }); }; useEffect(() => { const params = new URLSearchParams(window.location.search); if (params.get( ) { const timer = window.setTimeout(() => { securityRef.current?.scrollIntoView({ behavior: }); }, 300); return () => window.clearTimeout(timer); } }, []); const handlePasswordChange = async () => { if (!user) return; setPasswordError( ); if (!newPassword.trim() || !confirmPassword.trim()) { setPasswordError('Please enter and confirm your new password.'); return; } if (newPassword !== confirmPassword) { setPasswordError('Passwords do not match.'); return; } if (newPassword.length < 8) { setPasswordError('Password must be at least 8 characters long.'); return; } setPasswordSaving(true); try { const { error } = await updatePassword(newPassword.trim()); if (error) throw error; push({ message: 'Password updated successfully.', type: 'success' }); setNewPassword( ); setConfirmPassword( ); } catch (err: unknown) { const message = err instanceof Error && err.message ? err.message : 'Failed to update password. Please try again.'; setPasswordError(message); push({ message, type: 'error' }); } finally { setPasswordSaving(false); } }; const handleReset = () => { if (initialFormRef.current) { setForm({ ...initialFormRef.current }); setSaveError( ); setSaveSuccess(false); } }; const handleSave = async () => { if (!user) return; setSaveError( ); setSaving(true); try { const phoneValue = form.phone || ; const phoneCheck = phoneValue ? validatePhoneNumber(phoneValue) : { ok: true, normalized: }; if (!phoneCheck.ok) throw new Error(phoneCheck.message); const updates: Record<string, unknown> = { updated_at: new Date().toISOString() }; activeFields.forEach(([key]) => { if (key === ) { updates[key] = (form[key] || ) .split( ) .map((skill: string) => sanitizeProfileText(skill)) .filter(Boolean); return; } if (key === ) { updates[key] = phoneCheck.normalized || null; return; } const raw = form[key] || ; updates[key] = raw ? sanitizeProfileText(raw) : null; }); if (form.avatar_url) updates.avatar_url = form.avatar_url; else updates.avatar_url = null; updates.profile_sections = { experience, education, honors, languages, }; await updateProfile(user.id, updates); const normalizedPhone = phoneCheck.normalized ? formatPhoneInput(phoneCheck.normalized) : ; const updatedForm = { ...form, phone: normalizedPhone, }; setForm(updatedForm); initialFormRef.current = updatedForm; setSaveSuccess(true); setSavedBadgeVisible(true); push({ message: }); window.setTimeout(() => setSaveSuccess(false), 5000); } catch (err: unknown) { const message = err instanceof Error && err.message ? err.message : ; setSaveError(message); push({ message, type: }); } finally { setSaving(false); } }; const handleAvatarUpload = async (file?: File) => { if (!user || !file) return; const allowedTypes = [ ]; const maxSize = 2 * 1024 * 1024; if (!allowedTypes.includes(file.type)) { setSaveError( ); return; } if (file.size > maxSize) { setSaveError( ); return; } setSaveError( ); setAvatarUploading(true); try { const ext = file.name.split( ; const path = `${user.id}/${Date.now()}.${ext}`; const { error: uploadError } = await supabase.storage.from( ).upload(path, file, { upsert: true }); if (uploadError) throw uploadError; const { data } = supabase.storage.from( ).getPublicUrl(path); const publicUrl = data?.publicUrl || ; if (!publicUrl) throw new Error( ); setForm((prev) => ({ ...prev, avatar_url: publicUrl })); await updateProfile(user.id, { avatar_url: publicUrl, updated_at: new Date().toISOString() }); setSaveSuccess(true); setTimeout(() => setSaveSuccess(false), 2500); } catch (err: unknown) { const message = err instanceof Error && err.message ? err.message : ; setSaveError(message); } finally { setAvatarUploading(false); if (avatarInputRef.current) avatarInputRef.current.value = ; } }; const renderFormField = ( key: string, field: (typeof PROFILE_FIELDS)[keyof typeof PROFILE_FIELDS], ) => { const Icon = field.icon; const selectOptions: Record<string, Array<{ value: string; label: string }>> = { specialty: [...PROVIDER_CATEGORIES, ].map((value) => ({ value, label: value })), }; const isSelect = key in selectOptions; const isTextarea = key === ; return ( <div key={key} className= > <label className= > <Icon className= /> {field.label} </label> {isTextarea ? ( <textarea name={key} id={`field-${key}`} value={form[key] || } onChange={(e) => updateField(key, e.target.value)} rows={4} placeholder={`Enter your ${field.label.toLowerCase()}...`} className= /> ) : isSelect ? ( <select name={key} id={`field-${key}`} value={form[key] || } onChange={(e) => updateField(key, e.target.value)} className= > <option value= >Select...</option> {selectOptions[key].map((option) => ( <option key={option.value} value={option.value}> {option.label} </option> ))} </select> ) : ( <input name={key} id={`field-${key}`} type={ key === ].includes(key) ? } value={form[key] || } onChange={(e) => updateField(key, e.target.value)} placeholder={`Enter your ${field.label.toLowerCase()}...`} readOnly={key === } tabIndex={key === ? -1 : 0} className= /> )} </div> ); }; const missingFields = activeFields .filter(([key]) => !form[key]?.trim() && key !== ) .slice(0, 3); if (authLoading) { return ( <div className= > <Header /> <div className= > <div className= > <Loader2 className= /> </div> <h1 className= >Loading your profile...</h1> <p className= >Please wait while we confirm your account.</p> </div> </div> ); } if (!authLoading && !isAuthenticated) { return ( <div className= > <Header /> <div className= > <div className= > <ShieldCheck className= /> </div> <h1 className= >Please sign in</h1> <p className= >You must be signed in to view and edit your profile.</p> <button onClick={() => navigate(`/login?redirect=${encodeURIComponent('/profile')}`)} className= > Sign in to continue </button> </div> </div> ); } return ( <div className= > <div className= > <div className= /> <div className= /> <div className= /> </div> <Header /> <FloatingDecorations className= /> <input ref={avatarInputRef} type= image/png,image/jpeg,image/webp onChange={(e) => handleAvatarUpload(e.target.files?.[0])} /> <main className= > <div className= > <aside className= > <div className= > <div className= > <div className= /> </div> <div className= > <div className= > <button type= onClick={() => avatarInputRef.current?.click()} className= > <img src={avatarSrc} alt= onError={() => setAvatarLoadFailed(true)} className= /> <div className= > <div className= > <Camera className= /> </div> </div> {avatarUploading && ( <div className= > <Loader className= /> </div> )} </button> <div className= > <div className= > <Sparkles className= /> Professional profile </div> <h1 className= >{form.full_name || 'Your name'}</h1> <p className= >{profileHeadline}</p> <p className= > <span className= >{roleLabel}</span> {providerBadge && ( <span className= > {providerBadge} </span> )} <span className= >•</span> <span>{primaryLocation}</span> </p> </div> </div> <div className= > <div className= > <p className= >Connections</p> <p className= >{experience.length + education.length + honors.length + languages.length || 0}</p> </div> <div className= > <p className= >Profile strength</p> <p className= >{profileStatus}</p> </div> </div> <div className= > <button type= onClick={handleViewJobs} className= > <Briefcase className= /> View jobs </button> <button type= onClick={handleExploreProviders} className= > <Users2 className= /> Explore providers </button> <button type= onClick={() => scrollToSection( )} className= > <Sparkles className= /> Edit profile </button> <button type= onClick={() => scrollToSection( )} className= > <ShieldCheck className= /> Security </button> </div> </div> </div> <div ref={activityRef} className= > <div className= > <div> <p className= >Activity</p> <p className= >Recent profile activity and insights that help recruiters connect with you faster.</p> </div> <span className= >Active</span> </div> <div className= > <div className= > <p className= >Profile views</p> <p className= >Your profile is visible to recruiters and providers across JobBridge.</p> </div> <div className= > <p className= >Profile strength</p> <p className= >{completionPct}% complete — {activeFields.length - filledFieldsCount} field{activeFields.length - filledFieldsCount !== 1 ? 's' : ''} remaining to reach 100%.</p> </div> </div> </div> <div className= > <div className= > <p className= >Experience</p> <button type= onClick={() => openSectionEditor('experience')} className= > <Plus className= /> Add </button> </div> {experience.length === 0 ? ( <div className= > No experience entries yet. Click to include your work history. </div> ) : ( <div className= > {experience.map((entry) => ( <div key={entry.id} className= > <div className= /> <div className= > <p className= >{entry.title}</p> <p className= >{entry.company}{entry.period ? ` · ${entry.period}` : ''}</p> {entry.description && <p className= >{entry.description}</p>} </div> <div className= > <button type= onClick={() => openSectionEditor('experience', entry as unknown as Record<string, unknown>)} className= > <Pencil className= /> </button> <button type= onClick={() => deleteSectionItem('experience', entry.id)} className= > <Trash2 className= /> </button> </div> </div> ))} </div> )} </div> <div className= > <p className= >Notifications</p> <p className= >Enable browser push notifications to receive job alerts and messages directly.</p> <div className= > {!pushSupported ? ( <div className= >Push not supported or not configured.</div> ) : pushSubscribed ? ( <button onClick={handleUnsubscribeClick} className= >Disable Notifications</button> ) : ( <button onClick={handleSubscribeClick} className= >Enable Notifications</button> )} </div> </div> </aside> <section className= > <div ref={editorRef} className= > <div className= > <div> <p className= >Profile editor</p> <h2 className= >Keep your profile polished</h2> <p className= >Update your details to improve visibility with recruiters and clients.</p> </div> <div className= > <div className= > {filledFieldsCount}/{activeFields.length} fields • {completionPct}% complete </div> <ProfileCompletionRing percentage={completionPct} /> </div> </div> <div className= > <div className= > <div className= > Edit your profile directly in the fields below and save when ready. </div> {savedBadgeMounted && ( <span className={`inline-flex items-center gap-2 rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700 ring-1 ring-blue-100 transition-all duration-300 ${ savedBadgeVisible ? 'opacity-100 translate-y-0' : 'opacity-0 -translate-y-1' }`} > <Check className= /> Saved </span> )} </div> <div className= > <button onClick={handleReset} disabled={saving} className= > Reset </button> <button onClick={handleSave} disabled={saving} className={saveButtonClass} > {saving ? ( <> <Loader className= /> Saving... </> ) : saveSuccess ? ( <> <Check className= /> Saved </> ) : ( 'Save changes' )} </button> </div> </div> </div> <div className= > {saveSuccess && ( <div className= > Profile updated successfully. </div> )} {saveError && ( <div className= > {saveError} </div> )} {profileLoading ? ( <div className= > <div className= /> <p className= >Loading your profile...</p> </div> ) : ( <form onSubmit={(e) => { e.preventDefault(); handleSave(); }} className= > {Object.entries(sectionGroups).map(([section, fields]) => ( <div key={section} className= > <div className= > <h3 className= > {section === 'personal' ? 'Personal details' : section === 'professional' ? 'Professional details' : section === 'provider' ? 'Provider details' : section} </h3> <span className= > {section === 'personal' ? 'Core' : section === 'professional' ? 'Career' : section === 'provider' ? 'Service' : section} </span> </div> <div className= > {fields.map(([key, field]) => ( <div key={key} className={key === 'bio' ? 'md:col-span-2' : ''}> {renderFormField(key, field)} </div> ))} </div> </div> ))} <div className= > Your email is read-only because it is linked to your account. </div> <div className= > <div className= >Save your profile to make it visible to recruiters and providers.</div> <div className= > <button onClick={handleReset} disabled={saving} className= > Reset changes </button> <button type= disabled={saving} className={saveButtonClass} > {saving ? ( <> <Loader className= /> Saving... </> ) : saveSuccess ? ( <> <Check className= /> Saved </> ) : ( 'Save profile' )} </button> </div> </div> </form> )} </div> <div ref={securityRef} className= > <div className= > <div> <h3 className= >Change password</h3> <p className= >Update your account password securely from this page.</p> </div> <span className= > Secure </span> </div> <div className= > {passwordError && ( <div className= > {passwordError} </div> )} <div className= > <div> <label className= >New password</label> <input type= value={newPassword} onChange={(e) => setNewPassword(e.target.value)} placeholder= className= /> </div> <div> <label className= >Confirm password</label> <input type= value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder= className= /> </div> </div> <div className= > <p className= >Use a strong password of at least 8 characters.</p> <button type= onClick={handlePasswordChange} disabled={passwordSaving} className={`inline-flex items-center justify-center rounded-2xl px-6 py-3 text-sm font-semibold transition ${ passwordSaving ? 'bg-blue-200 text-blue-800 cursor-wait' : 'bg-blue-600 text-white hover:bg-blue-700' }`} > {passwordSaving ? ( <> <Loader className= /> Updating... </> ) : ( 'Change password' )} </button> </div> </div> </div> <div className= > { } <div className= > <div className= > <h3 className= >Education</h3> <button type= onClick={() => openSectionEditor('education')} className= > <Plus className= /> Add </button> </div> {education.length === 0 ? ( <div className= > No education entries yet. Click to include your academic background. </div> ) : ( <div className= > {education.map((entry) => ( <div key={entry.id} className= > <GraduationCap className= /> <div className= > <p className= >{entry.institution}</p> <p className= >{entry.degree}{entry.period ? ` · ${entry.period}` : ''}</p> </div> <div className= > <button type= onClick={() => openSectionEditor('education', entry as unknown as Record<string, unknown>)} className= > <Pencil className= /> </button> <button type= onClick={() => deleteSectionItem('education', entry.id)} className= > <Trash2 className= /> </button> </div> </div> ))} </div> )} </div> { } <div className= > <div className= > <h3 className= >Skills</h3> <button type= onClick={() => scrollToSection( )} className= > <Plus className= /> Add </button> </div> <div className= > {(topSkills.length ? topSkills : ['Creative Strategy', 'Advertising']).map((skill) => ( <div key={skill} className= > <div className= > <p className= >{skill}</p> <button type= onClick={() => push({ message: `You endorsed ${skill}!`, type: 'success' })} className= > Endorse </button> </div> <p className= >Brief skill description for recruiters and clients.</p> </div> ))} </div> </div> { } <div className= > <div className= > <h3 className= >Honors & awards</h3> <button type= onClick={() => openSectionEditor('honors')} className= > <Plus className= /> Add </button> </div> {honors.length === 0 ? ( <div className= > No honors or awards listed yet. Click to showcase your achievements. </div> ) : ( <div className= > {honors.map((entry) => ( <div key={entry.id} className= > <Medal className= /> <div className= > <p className= >{entry.title}</p> {entry.date && <p className= >{entry.date}</p>} {entry.description && <p className= >{entry.description}</p>} </div> <div className= > <button type= onClick={() => openSectionEditor('honors', entry as unknown as Record<string, unknown>)} className= > <Pencil className= /> </button> <button type= onClick={() => deleteSectionItem('honors', entry.id)} className= > <Trash2 className= /> </button> </div> </div> ))} </div> )} </div> { } <div className= > <div className= > <h3 className= >Languages</h3> <button type= onClick={() => openSectionEditor('languages')} className= > <Plus className= /> Add </button> </div> {languages.length === 0 ? ( <div className= > No languages added yet. Click to list your language proficiencies. </div> ) : ( <div className= > {languages.map((entry) => ( <div key={entry.id} className= > <LanguagesIcon className= /> <div className= > <p className= >{entry.language}</p> {entry.proficiency && <p className= >{entry.proficiency}</p>} </div> <div className= > <button type= onClick={() => openSectionEditor('languages', entry as unknown as Record<string, unknown>)} className= > <Pencil className= /> </button> <button type= onClick={() => deleteSectionItem('languages', entry.id)} className= > <Trash2 className= /> </button> </div> </div> ))} </div> )} </div> </div> </section> </div> </main> { } {showSectionModal && ( <SectionEditorModal section={editingSection || ''} item={editingItem} onSave={saveSectionItem} onClose={closeSectionEditor} /> )} <BottomNav /> </div> ); } interface SectionField { name: string; label: string; type: 'text' | 'textarea' | 'select'; options?: { value: string; label: string }[]; required?: boolean; } const SECTION_FIELDS: Record<string, SectionField[]> = { experience: [ { name: 'title', label: 'Job Title', type: 'text', required: true }, { name: 'company', label: 'Company', type: 'text', required: true }, { name: 'period', label: 'Period (e.g. Jan 2020 - Present)', type: 'text' }, { name: 'description', label: 'Description', type: 'textarea' }, ], education: [ { name: 'institution', label: 'Institution', type: 'text', required: true }, { name: 'degree', label: 'Degree / Qualification', type: 'text', required: true }, { name: 'period', label: 'Period (e.g. 2017-2019)', type: 'text' }, ], honors: [ { name: 'title', label: 'Award / Honor Title', type: 'text', required: true }, { name: 'date', label: 'Date (e.g. January 2018)', type: 'text' }, { name: 'description', label: 'Description', type: 'textarea' }, ], languages: [ { name: 'language', label: 'Language', type: 'text', required: true }, { name: 'proficiency', label: 'Proficiency', type: 'select', required: true, options: [ { value: 'Native', label: 'Native' }, { value: 'Full professional proficiency', label: 'Full professional proficiency' }, { value: 'Professional working proficiency', label: 'Professional working proficiency' }, { value: 'Limited working proficiency', label: 'Limited working proficiency' }, { value: 'Elementary proficiency', label: 'Elementary proficiency' }, ], }, ], }; function SectionEditorModal({ section, item, onSave, onClose, }: { section: string; item: Record<string, unknown> | null; onSave: (section: string, data: Record<string, unknown>) => void; onClose: () => void; }) { const fields = SECTION_FIELDS[section] || []; const [formData, setFormData] = useState<Record<string, string>>(() => { const initial: Record<string, string> = {}; fields.forEach((f) => { initial[f.name] = item?.[f.name] ? String(item[f.name]) : ''; }); return initial; }); const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); onSave(section, formData); }; const isEditing = !!item?.id; return ( <div className= > <div className= > <div className= > <h3 className= > {isEditing ? 'Edit' : 'Add'} {section.charAt(0).toUpperCase() + section.slice(1)} </h3> <button type= onClick={onClose} className= > <X className= /> </button> </div> <form onSubmit={handleSubmit} className= > {fields.map((field) => ( <div key={field.name}> <label className= > {field.label} {field.required && <span className= >*</span>} </label> {field.type === 'textarea' ? ( <textarea value={formData[field.name] || ''} onChange={(e) => setFormData((prev) => ({ ...prev, [field.name]: e.target.value }))} rows={3} className= placeholder={`Enter ${field.label.toLowerCase()}...`} /> ) : field.type === 'select' ? ( <select value={formData[field.name] || ''} onChange={(e) => setFormData((prev) => ({ ...prev, [field.name]: e.target.value }))} className= > <option value= >Select...</option> {field.options?.map((opt) => ( <option key={opt.value} value={opt.value}>{opt.label}</option> ))} </select> ) : ( <input type= value={formData[field.name] || ''} onChange={(e) => setFormData((prev) => ({ ...prev, [field.name]: e.target.value }))} className= placeholder={`Enter ${field.label.toLowerCase()}...`} /> )} </div> ))} <div className= > <button type= onClick={onClose} className= > Cancel </button> <button type= className= formatPhoneInput, sanitizeProfileText, lightSanitizeProfileText, validatePhoneNumber, from \"../lib/profileValidation\"; Camera, Check, Loader, Loader2, User, Phone, Award, BookOpen, Hash, Globe, DollarSign, Clock, Briefcase, ShieldCheck, Sparkles, Users2, Plus, Trash2, Pencil, X, GraduationCap, Medal, LanguagesIcon, from \"lucide-react\"; const PROFILE_FIELDS = full_name: label: \"Full Name\", section: \"personal\", weight: 2, icon: User , phone: label: \"Phone Number\", section: \"personal\", weight: 1, icon: Phone , location: label: \"Location\", section: \"personal\", weight: 1, icon: Globe , professional_headline: label: \"Professional Headline\", section: \"professional\", weight: 2, icon: Award , years_of_experience: label: \"Years of Experience\", section: \"professional\", weight: 1, icon: Clock , bio: label: \"Bio / About\", section: \"professional\", weight: 2, icon: BookOpen , specialty: label: \"Service Specialty (Providers)\", section: \"provider\", weight: 2, icon: Hash , hourly_rate: label: \"Hourly Rate (NGN)\", section: \"provider\", weight: 1, icon: DollarSign , skills: label: \"Skills (comma-separated)\", section: \"provider\", weight: 1, icon: Hash , ; const DEFAULT_AVATAR = `data:image/svg+xml;utf8,$ encodeURIComponent( ' = 80 ? \"#0b6df4\" : percentage >= 50 ? \"#3b82f6\" : \"#2563eb\"; return ( percentage % Complete ); const user, profile: userProfile, updatePassword, isAuthenticated, loading: authLoading = useAuth(); const push = useToasts(); const navigate = useNavigate(); const [saveSuccess, setSaveSuccess] = useState(false); const [savedBadgeVisible, setSavedBadgeVisible] = useState(false); const [savedBadgeMounted, setSavedBadgeMounted] = useState(false); const [saving, setSaving] = useState(false); const [saveError, setSaveError] = useState(\"\"); const [avatarUploading, setAvatarUploading] = useState(false); const [newPassword, setNewPassword] = useState(\"\"); const [confirmPassword, setConfirmPassword] = useState(\"\"); const [passwordError, setPasswordError] = useState(\"\"); const [pa"
    },
    {
      "file": "ProfileVisibility.tsx",
      "path": "src/pages/ProfileVisibility.tsx",
      "title": "Hiring Visibility",
      "text": "Hiring Visibility Hiring Visibility Control whether employers can discover your profile in talent search. Hiring employers can find you Currently: {visible ? 'Visible' : 'Hidden'} {visible ? 'On' : 'Off'} Back to profile Browse jobs max-w-3xl mx-auto px-4 py-8 bg-white border border-gray-100 rounded-2xl p-6 flex items-center gap-3 mb-2 w-10 h-10 rounded-xl bg-primary-fixed text-primary flex items-center justify-center /> </div> <h1 className= >Hiring Visibility</h1> </div> <p className= > Control whether employers can discover your profile in talent search. </p> <div className= > <div> <p className= >Hiring employers can find you</p> <p className= >Currently: {visible ? 'Visible' : 'Hidden'}</p> </div> <button onClick={toggle} className={`px-4 py-2 rounded-lg text-sm font-semibold transition-colors ${ visible ? 'bg-primary text-white' : 'bg-surface-container text-gray-700' }`} > {visible ? 'On' : 'Off'} </button> </div> <div className= > <Link to= px-4 py-2 rounded-xl border border-outline-variant text-sm text-gray-700 hover:bg-gray-50 inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-primary text-white text-sm font-semibold hover:bg-primary-container const STORAGE_KEY = 'jobbridge_profile_visible'; function initialVisibility() try return localStorage.getItem(STORAGE_KEY) !== 'false'; catch return true; const [visible, setVisible] = useState (initialVisibility); const toggle = () => const next = !visible; setVisible(next); try localStorage.setItem(STORAGE_KEY, String(next)); catch ; return ( Hiring Visibility Control whether employers can discover your profile in talent search. Hiring employers can find you Currently: visible ? 'Visible' : 'Hidden' visible ? 'On' : 'Off' Back to profile Browse jobs );"
    },
    {
      "file": "Promotional.tsx",
      "path": "src/pages/Promotional.tsx",
      "title": "Promotional",
      "text": "Career momentum starts here Trusted by hiring teams AI-driven match intelligence Search top candidates instantly Live career insights updated every minute {currentSlide.title} {currentSlide.subtitle} {currentSlide.primary.label} {currentSlide.secondary.label} Real-time job discovery Verified employer network AI-powered career guidance Verified employers only Career momentum starts here Swipe the slides to explore what JobBridge can do for your career or hiring journey — from talent search to AI resume building. Trusted hiring Fast placements 24 days avg. Swipe to preview Live updates {item} Verified employers Trusted by hiring teams Every employer is vetted so you can apply with confidence. Our platform highlights verified postings from real companies. Career acceleration AI-driven match intelligence JobBridge uses AI to surface roles that fit your profile, speed up hiring, and help your resume rise above the noise. Real-time talent Search top candidates instantly Employers can filter, shortlist, and message talent in real time — making hiring faster and more precise. min-h-screen bg-slate-950 text-slate-100 relative overflow-hidden absolute inset-0 bg-cover bg-center opacity-90 absolute inset-0 bg-gradient-to-br from-slate-950/90 via-slate-950/50 to-slate-950/90 relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 grid gap-10 lg:grid-cols-[1.2fr_0.8fr] items-center > <div className= > <span className= /> Live career insights updated every minute </div> <div className= > <p className= > {currentSlide.title} </p> <p className= > {currentSlide.subtitle} </p> </div> <div className= > <Link to={currentSlide.primary.href} className= > {currentSlide.primary.label} <ArrowRight className= /> </Link> <Link to={currentSlide.secondary.href} className= > {currentSlide.secondary.label} <ArrowRight className= /> </Link> </div> <div className= > <span className= > <Search className= /> Real-time job discovery </span> <span className= > <Users className= /> Verified employer network </span> <span className= > <Zap className= /> AI-powered career guidance </span> </div> </div> <div className= > <div className= /> <div className= > <div className= > <div className= > <span className= /> Verified employers only </div> <h2 className= >Career momentum starts here</h2> <p className= > Swipe the slides to explore what JobBridge can do for your career or hiring journey — from talent search to AI resume building. </p> </div> <div className= > <div className= > <p className= >Trusted hiring</p> <p className= >2,300+</p> </div> <div className= > <p className= >Fast placements</p> <p className= >24 days avg.</p> </div> </div> </div> <div className= > <div className= > <span className= /> Swipe to preview </div> <div className= > {pager.map((step) => ( <button key={step} onClick={() => setCurrentIndex(step)} className={`h-2.5 rounded-full transition-all duration-300 ${step === currentIndex ? 'w-8 bg-emerald-300' : 'w-3 bg-slate-600/80'}`} aria-label={`Go to slide ${step + 1}`} /> ))} </div> </div> </div> </div> </div> </section> <section className= > <div className= > <div className= > Live updates </div> <div className= > <div className= style={{ animationDuration: '24s' }}> {tickerItems.concat(tickerItems).map((item, index) => ( <span key={index} className= > <span className= /> {item} </span> ))} </div> </div> </div> </section> <section className= > <div className= > <div className= > <p className= >Verified employers</p> <h3 className= >Trusted by hiring teams</h3> <p className= >Every employer is vetted so you can apply with confidence. Our platform highlights verified postings from real companies.</p> </div> <div className= > <p className= >Career acceleration</p> <h3 className= >AI-driven match intelligence</h3> <p className= >JobBridge uses AI to surface roles that fit your profile, speed up hiring, and help your resume rise above the noise.</p> </div> <div className= > <p className= >Real-time talent</p> <h3 className= >Search top candidates instantly</h3> <p className= const slides = [ title: 'Discover verified employers hiring now', subtitle: 'Explore thousands of active roles across top companies with trusted employer verification and real-time openings.', primary: label: 'Find Jobs', href: '/jobs' , secondary: label: 'See Employer Trust', href: '/providers' , image: pexel(5668855, 1400, 700), , title: 'Hire talent with AI-powered matching', subtitle: 'Connect to vetted professionals instantly and scale your team with our intelligent talent search engine.', primary: label: 'Hire Talent', href: '/providers' , secondary: label: 'Talent Search', href: '/talent-search' , image: pexel(3771111, 1400, 700), , title: 'Build an AI-optimized career profile', subtitle: 'Create a resume and profile that stands out to recruiters using JobBridge AI insights and tailored recommendations.', primary: label: 'AI Resume', href: '/ai-resume' , secondary: label: 'Complete Profile', href: '/profile' , image: pexel(8456191, 1400, 700), , ]; const tickerItems = [ '24,500+ jobs actively hiring now', 'Average placement time: 24 days', '500+ new talent connections daily', 'AI career matches updated every minute', 'Verified recruiters trust JobBridge for talent sourcing', ]; const [currentIndex, setCurrentIndex] = useState(0); const startX = useRef (null); const isDragging = useRef(false); useEffect(() => const interval = setInterval(() => setCurrentIndex((prev) => (prev + 1) % slides.length); , 6500); return () => clearInterval(interval); , []); const currentSlide = slides[currentIndex]; const pager = useMemo( () => slides.map((_, index) => index), [], ); const handlePointerDown = (event: React.PointerEvent ) => startX.current = event.clientX; isDragging.current = true; ; const handlePointerUp = (event: React.PointerEvent ) => if (!isDragging.current || startX.current === null) return; const delta = event.clientX - startX.current; if (Math.abs(delta) > 50) if (delta (prev + 1) % slides.length); else setCurrentIndex((prev) => (prev - 1 + slides.length) % s"
    },
    {
      "file": "Providers.tsx",
      "path": "src/pages/Providers.tsx",
      "title": "Find Trusted Service Providers",
      "text": "{p.name} Find Trusted Service Providers Featured Professionals Verified Professionals All Service Providers Service Directory Become a Service Provider {chatProvider.name} Featured Professional {p.img ? ( ) : null} {!p.img && ( {getInitials(p.name)} {p.verified && {p.specialty} {p.location && ( {p.location} {renderStars(p.rating)} {p.rating.toFixed(1)} ({p.reviews} {p.reviews === 1 ? 'review' : 'reviews'}) New — no reviews yet 0 ? `₦${p.hourlyRate.toLocaleString()}/hr` : 'Rate negotiable'} {spec} View Profile Rate Provider Find Trusted Service Providers Browse our directory of verified professionals across Nigeria. {cat} {sortedProviders.length} Featured Professionals {featuredProviders.length} renderProviderCard(p))} Verified Professionals {verifiedProviders.length} renderProviderCard(p))} All Service Providers {standardProviders.length} renderProviderCard(p))} No providers found Service Directory Browse all service categories to find what you need {showDirectory ? ( {showDirectory && ( Become a Service Provider Join thousands of professionals offering services on JobBridge. Get clients, build your reputation, and grow your business. Monthly Listing FREE at launch Profile on JobBridge Contact information Receive inquiries Featured Professional ₦5,000/month Top of search results Featured badge Priority support View Pricing Plans {chatProvider.img ? ( {getInitials(chatProvider.name)} {chatProvider.name} {chatProvider.specialty} {msg.text} {msg.timestamp.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })} flex items-center gap-0.5 bg-gradient-to-r from-amber-500 to-amber-600 px-4 py-2 flex items-center gap-2 w-3.5 h-3.5 text-white shrink-0 text-white text-xs font-semibold tracking-wide > { } <div className= > {p.img ? ( <img src={p.img} alt={p.name} className= onError={(e) => { (e.target as HTMLImageElement).style.display = 'none'; (e.target as HTMLImageElement).nextElementSibling?.classList.remove('hidden'); }} /> ) : null} <div className={`${p.img ? '' : ''} ${!p.img ? 'flex items-center gap-3' : ''}`}> {!p.img && ( <div className= > {getInitials(p.name)} </div> )} <div className={p.img ? '' : ''}> <div className= > <h3 className= >{p.name}</h3> {p.verified && <BadgeCheck className= />} </div> <p className= >{p.specialty}</p> {p.location && ( <p className= > <MapPin className= /> {p.location} </p> )} </div> </div> </div> { } <div className= > {p.reviews > 0 ? ( <> {renderStars(p.rating)} <span className= > {p.rating.toFixed(1)} ({p.reviews} {p.reviews === 1 ? 'review' : 'reviews'}) </span> </> ) : ( <span className= > New — no reviews yet </span> )} </div> { } <div className= > {p.hourlyRate > 0 ? `₦${p.hourlyRate.toLocaleString()}/hr` : 'Rate negotiable'} </div> { } {p.specializations.length > 0 && ( <div className= > {p.specializations.map(spec => ( <span key={spec} className= > {spec} </span> ))} </div> )} { } <div className= > <button onClick={() => openChat(p)} className= > <MessageCircle className= /> Chat </button> <button onClick={() => openModal('profile', { name: p.name, role: p.specialty, specialty: p.specialty, match: `${p.rating}★`, skills: p.specializations, bio: '', location: p.location, avatarUrl: p.img, hourlyRate: p.hourlyRate, email: p.email, verified: p.verified, reviews: p.reviews, })} className= > View Profile </button> <Link to={`/reviews?providerId=${encodeURIComponent(p.id)}`} className= > <Star className= /> Rate Provider </Link> </div> </div> </div> ); return ( <div className= > <Header /> <div className= > { } <div className= > <div className= /> <div className= > <h1 className= >Find Trusted Service Providers</h1> <p className= > Browse our directory of verified professionals across Nigeria. </p> <div className= > <Search className= size={20} /> <input type= placeholder= value={searchQuery} onChange={e => setSearchQuery(e.target.value)} className= /> </div> </div> </div> { } <div className= > <div className= > {categories.map(cat => ( <button key={cat} onClick={() => setSelectedCategory(cat)} className={`px-4 py-2 rounded-full whitespace-nowrap font-medium transition-all text-sm ${ selectedCategory === cat ? 'bg-blue-700 text-white shadow-md shadow-blue-200' : 'bg-white text-slate-700 border border-slate-200 hover:border-blue-300 hover:text-blue-700' }`} > {cat} </button> ))} </div> </div> { } <div className= > <p className= > <span className= >{sortedProviders.length}</span>{' '} {sortedProviders.length === 1 ? 'provider' : 'providers'} found {selectedCategory !== 'All' ? ` in ${selectedCategory}` : ''} </p> </div> { } {sortedProviders.length > 0 ? ( <> { } {featuredProviders.length > 0 && ( <section className= > <div className= > <span className= > <Sparkles className= /> </span> <h2 className= >Featured Professionals</h2> <span className= >{featuredProviders.length}</span> </div> <div className= > {featuredProviders.map(p => renderProviderCard(p))} </div> </section> )} { } {verifiedProviders.length > 0 && ( <section className= > <div className= > <span className= > <BadgeCheck className= /> </span> <h2 className= >Verified Professionals</h2> <span className= >{verifiedProviders.length}</span> </div> <div className= > {verifiedProviders.map(p => renderProviderCard(p))} </div> </section> )} { } {standardProviders.length > 0 && ( <section className= > <div className= > <span className= > <Users className= /> </span> <h2 className= >All Service Providers</h2> <span className= >{standardProviders.length}</span> </div> <div className= > {standardProviders.map(p => renderProviderCard(p))} </div> </section> )} </> ) : ( <div className= > <p className= >No providers found</p> <p className= > {searchQuery ? 'Try a different search term or category.' : 'No providers are currently listed in this category.'} </p> </div> )} { } <div className= > <button onClick={() => setShowDirectory(!showDirectory)} className= > <div className= > <h2 className= >Service Directory</h2> <p className= >Browse all service categories to find what you need</p> </div> {showDirectory ? ( <ChevronUp className= /> ) : ( <ChevronDown className= /> )} </button> {showDirectory && ( <div className= > <div className= > {categoryList.map(cat => ( <button key={cat} onClick={() => { setSelectedCategory(cat); setShowDirectory(false); }} className={`flex items-center justify-between px-4 py-3 rounded-lg border transition-colors text-left ${ selectedCategory === cat ? 'bg-blue-50 border-blue-200 text-blue-700' : 'bg-slate-50 border-slate-100 hover:border-blue-200 hover:bg-blue-50/50 text-slate-700' }`} > <span className= >{cat}</span> <ArrowRight className= /> </button> ))} </div> </div> )} </div> { } <div className= > <div className= > <div className= > <h2 className= >Become a Service Provider</h2> <p className= > Join thousands of professionals offering services on JobBridge. Get clients, build your reputation, and grow your business. </p> <div className= > <div className= > <div className= >Monthly Listing</div> <div className= >FREE at launch</div> <ul className= > <li className= >•</span> Profile on JobBridge</li> <li className= >•</span> Contact information</li> <li className= >•</span> Receive inquiries</li> </ul> </div> <div className= > <div className= >Featured Professional</div> <div className= >₦5,000/month</div> <ul className= > <li className= >•</span> Top of search results</li> <li className= >•</span> Featured badge</li> <li className= >•</span> Priority support</li> </ul> </div> </div> <Link to= className= > View Pricing Plans <ArrowRight size={18} /> </Link> </div> </div> </div> </div> { } {chatProvider && ( <div className= > <div className= > <div className= > {chatProvider.img ? ( <img src={chatProvider.img} alt={chatProvider.name} className= /> ) : ( <div className= > {getInitials(chatProvider.name)} </div> )} <div className= > <h3 className= >{chatProvider.name}</h3> <p className= >{chatProvider.specialty}</p> </div> <button onClick={closeChat} className= > <X className= /> </button> </div> <div className= > {messages.map(msg => ( <div key={msg.id} className={`flex ${msg.sender === 'user' ? 'justify-end' : 'justify-start'}`}> <div className={`max-w-[80%] px-4 py-2.5 rounded-2xl ${ msg.sender === 'user' ? 'bg-blue-700 text-white rounded-br-md' : 'bg-slate-100 text-slate-900 rounded-bl-md' }`}> <p className= >{msg.text}</p> <p className={`text-xs mt-1 ${msg.sender === 'user' ? 'text-blue-200' : 'text-slate-400'}`}> {msg.timestamp.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })} </p> </div> </div> ))} <div ref={messagesEndRef} /> </div> <form onSubmit={sendMessage} className= > <div className= > <input type= value={newMessage} onChange={e => setNewMessage(e.target.value)} placeholder= className= /> <button type= disabled={!newMessage.trim()} className= > <Send className= interface Message id: number; text: string; sender: 'user' | 'provider'; timestamp: Date; interface ProviderDisplay id: string; email: string; name: string; specialty: string; rating: number; reviews: number; hourlyRate: number; specializations: string[]; img: string; verified: boolean; featured: boolean; tier: string; location: string; const categoryList: string[] = [...PROVIDER_CATEGORIES]; const SERVICE_KEYWORDS: Record = 'Technology': ['software', 'developer', 'web', 'mobile', 'programming', 'coding', 'frontend', 'backend', 'fullstack', 'app', 'data analyst', 'data scientist', 'virtual assistant', 'it', 'computer', 'phone repair', 'tech', 'ui/ux', 'ui ux'], 'Creative & Media': ['design', 'graphic', 'ui', 'ux', 'video', 'editor', 'photographer', 'photography', 'content creator', 'social media manager', 'copywriter', 'musician', 'saxophonist', 'dj', 'music', 'creative', 'animator', 'branding'], 'Business & Administration': ['administrative', 'virtual assistant', 'hr', 'human resources', 'project manager', 'accountant', 'accounting', 'business development', 'tax', 'admin', 'customer support', 'secretary'], 'Sales & Marketing': ['sales', 'marketing', 'digital marketing', 'social media', 'seo', 'content', 'copywriter', 'brand', 'advertising', 'business development', 'affiliate'], 'Engineering & Construction': ['civil engineer', 'mechanical engineer', 'electrical engineer', 'architect', 'quantity surveyor', 'building', 'construction', 'mason', 'interior design', 'landscaping', 'furniture', 'welder', 'engineer'], 'Skilled Trades': ['electrician', 'plumber', 'carpenter', 'welder', 'mason', 'painter', 'panel beater', 'generator repair', 'appliance repair', 'mechanic', 'auto mechanic', 'ac installer', 'borehole driller', 'repair', 'maintenance'], 'Beauty & Fashion': ['barber', 'hair stylist', 'makeup artist', 'nail tech', 'beauty salon', 'barbing salon', 'spa', 'wellness', 'fashion designer', 'tailor', 'fashion house', 'nail', 'makeup', 'hair', 'salon'], 'Food & Hospitali"
    },
    {
      "file": "Recruiter.tsx",
      "path": "src/pages/Recruiter.tsx",
      "title": "Recruiter",
      "text": "{job.title} Recruiter subscription is free for the first month. AI Job Description Writer {generatedJD.title} Active Job Postings ({filteredJobs.length}) Premium Job Post Standard Job Post Basic Job Post Applications ({filteredApps.length}) {selectedApp.professional_headline || selectedApp.applicant?.professional_headline || 'Applicant'} {app.applicant?.full_name || 'Anonymous'} Hire smarter with JobBridge Upgrade to Premium {job.title} {job.is_featured && {job.company} · {job.location} {job.applications_count || 0} applicant{(job.applications_count || 0) !== 1 ? 's' : ''} {job.views || 0} views {job.type} {job.salary_range && ( {job.salary_range} View Recruiter offer Recruiter subscription is free for the first month. Start posting jobs now and pay only from month two. You’ll receive daily JobBridge updates, email alerts, and live notification pop-ups for recruiter activity. No recruiter fee for month one. {subscription.status === 'active' ? ( {subscription.credits} job post credit{subscription.credits !== 1 ? 's' : ''} remaining {subscription.tier && ( {subscription.tier} plan First month is free for recruiters on new subscriptions. No active plan Subscribe to post jobs View Plans {subscription.status === 'active' ? ( Post New Job Subscribe to Post AI Write Description Subscribe to AI Write AI Job Description Writer Enter the job title, company name, salary range, and up to 3 core requirements. AI will generate an optimized, bias-free job description you can review and publish. Job Title * Company Name Salary Range {generating === 'jd' ? 'Generating...' : Generate Description {generatedJD && ( {generatedJD.title} Company Name Salary Range {generatedJD.description} Responsibilities: Requirements: Publish This Job Active Job Postings ({filteredJobs.length}) {jobsLoading ? ( ) : filteredJobs.length === 0 ? ( No jobs posted yet Click \"Post New Job\" to create your first listing. Premium Job Post {premiumJobs.length} renderJobCard(job))} Standard Job Post {standardJobs.length} renderJobCard(job))} Basic Job Post {basicJobs.length} renderJobCard(job))} Applications ({filteredApps.length}) {appsLoading ? ( ) : selectedApp ? ( Back to all applications {selectedApp.professional_headline || selectedApp.applicant?.professional_headline || 'Applicant'} {selectedApp.applicant?.full_name || 'Applicant'} · {selectedApp.applicant?.email} Applied for: {selectedApp.job?.title || 'Job'} · {new Date(selectedApp.created_at).toLocaleDateString()} {selectedApp.status} {selectedApp.date_of_birth || selectedApp.applicant?.date_of_birth || '-'} {selectedApp.gender || selectedApp.applicant?.gender || '-'} Experience: {selectedApp.years_of_experience || selectedApp.applicant?.years_of_experience || '-'} {selectedApp.function || selectedApp.applicant?.function || '-'} Work Type: {selectedApp.work_type || selectedApp.applicant?.work_type || '-'} Qualification: {selectedApp.highest_qualification || selectedApp.applicant?.highest_qualification || '-'} {selectedApp.location || selectedApp.applicant?.location || '-'} Availability: {selectedApp.availability || selectedApp.applicant?.availability || '-'} {selectedApp.salary_expectation || selectedApp.applicant?.salary_expectation ? `NGN ${Number.parseInt(selectedApp.salary_expectation || selectedApp.applicant?.salary_expectation || '0', 10).toLocaleString()}` : '-'} {(selectedApp.is_disabled || selectedApp.applicant?.is_disabled) === 'yes' ? Internally Displaced Cover Letter: {selectedApp.cover_letter} Download CV {resumeError && {resumeError} {selectedApp.status !== 'shortlisted' && ( Shortlist Mark Reviewed Reject Hire ) : filteredApps.length === 0 ? ( No applications yet Applications from job seekers will appear here. {app.applicant?.full_name || 'Anonymous'} {app.status} {app.professional_headline || app.applicant?.professional_headline || 'Applicant'} {app.function} · {app.location} · {app.years_of_experience} exp · NGN {parseInt(app.salary_expectation || 0).toLocaleString()} Applied for: {app.job?.title || 'Job'} · {new Date(app.created_at).toLocaleDateString()} Hire smarter with JobBridge Upgrade to Premium Get advanced analytics, unlimited job postings, and priority candidate matching. {subscription.status === 'active' ? 'Get More Credits' : 'Subscribe Now'} border border-gray-100 rounded-xl p-4 sm:p-5 hover:shadow-md transition-shadow flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between sm:mb-4 > <div className= > <h3 className= >{job.title}</h3> {job.is_featured && <span className= >Featured</span>} {!job.is_active && <span className= >Inactive</span>} </div> <p className= >{job.company} · {job.location}</p> <div className= > <span className= > <Users className= /> {job.applications_count || 0} applicant{(job.applications_count || 0) !== 1 ? 's' : ''} </span> <span className= > <Eye className= /> {job.views || 0} views </span> <span className= > <Clock className= /> {job.type} </span> </div> {job.salary_range && ( <p className= >{job.salary_range}</p> )} </div> <div className= > <button onClick={() => navigate(`/jobs?id=${job.id}`)} className= > <ExternalLink className= /> View </button> </div> </div> </Card3D> ); return ( <div className= > <Header /> <PageHero title= Post jobs, review candidates, and fill roles faster with AI-powered matching Recruiter reviewing candidate profiles > <div className= > <div className= > <div className= > <div className= > <p className= > Recruiter offer </p> <h2 className= > Recruiter subscription is free for the first month. </h2> <p className= > Start posting jobs now and pay only from month two. You’ll receive daily JobBridge updates, email alerts, and live notification pop-ups for recruiter activity. </p> </div> <div className= > No recruiter fee for month one. </div> </div> </div> </div> </AnimatedSection> <div className= > { } <AnimatedSection direction= > <div className= > {stats.map(({ label, value, icon: Icon, color }) => ( <Card3D key={label} className= strength={6}> <div className= > <div className={`w-10 h-10 sm:w-11 sm:h-11 rounded-xl flex items-center justify-center shrink-0 ${color}`}> <Icon className= /> </div> <div className= > <div className= >{value}</div> <div className= >{label}</div> </div> </div> </Card3D> ))} </div> </AnimatedSection> { } <AnimatedSection direction= > {subscription.status === 'active' ? ( <div className= > <div className= > <CheckCircle className= /> <span className= >{subscription.credits} job post credit{subscription.credits !== 1 ? 's' : ''} remaining</span> {subscription.tier && ( <span className= >{subscription.tier} plan</span> )} </div> <div className= >First month is free for recruiters on new subscriptions.</div> </div> ) : ( <div className= > <div className= > <CreditCard className= /> <span className= >No active plan</span> <span className= >Subscribe to post jobs</span> </div> <Link to= text-xs bg-amber-600 text-white px-3 py-1.5 rounded-lg font-medium hover:bg-amber-700 transition-colors inline-flex items-center grid grid-cols-1 gap-6 grid grid-cols-2 gap-3 mb-8 flex items-center justify-center gap-2 bg-blue-700 text-white font-semibold px-6 py-3.5 rounded-xl hover:bg-blue-800 transition-colors shadow-lg /> Post New Job </button> ) : ( <Link to= className= > <Lock className= /> Subscribe to Post </Link> )} {subscription.status === 'active' ? ( <button onClick={() => setShowAIJD(!showAIJD)} className={`flex items-center justify-center gap-2 font-semibold px-6 py-3.5 rounded-xl transition-colors shadow-lg ${ showAIJD ? 'bg-purple-700 text-white' : 'bg-white text-purple-700 border-2 border-purple-200 hover:bg-purple-50' }`} > <Sparkles className= /> AI Write Description </button> ) : ( <Link to= className= > <Lock className= /> Subscribe to AI Write </Link> )} </div></AnimatedSection> { } {showAIJD && ( <AnimatedSection direction= bg-white rounded-2xl border border-purple-200 shadow-sm p-6 mb-6 flex items-center justify-between mb-4 text-base font-bold text-gray-900 flex items-center gap-2 w-5 h-5 text-purple-600 p-1 hover:bg-gray-100 rounded-lg w-4 h-4 text-gray-400 text-sm text-gray-500 mb-4 > <div> <label className= >Job Title *</label> <input value={aiTitle} onChange={e => setAiTitle(e.target.value)} placeholder= className= /> </div> <div> <label className= >Company Name</label> <input value={aiCompany} onChange={e => setAiCompany(e.target.value)} placeholder= className= /> </div> <div> <label className= >Salary Range</label> <input value={aiSalary} onChange={e => setAiSalary(e.target.value)} placeholder= className= /> </div> {aiReqs.map((req, i) => ( <input key={i} value={req} onChange={e => { const next = [...aiReqs]; next[i] = e.target.value; setAiReqs(next); }} placeholder={`Core requirement ${i + 1} (e.g. 5+ years React experience)`} className= /> ))} <button onClick={handleGenerateJD} disabled={generating === 'jd' || !aiTitle.trim()} className= > {generating === 'jd' ? 'Generating...' : <><Sparkles className= /> Generate Description</>} </button> </div> {generatedJD && ( <div className= > <h4 className= >{generatedJD.title}</h4> <div className= > <div> <label className= >Company Name</label> <input value={aiCompany} onChange={e => { setAiCompany(e.target.value); setGeneratedJD({ ...generatedJD, company: e.target.value }); }} placeholder= className= /> </div> <div> <label className= >Salary Range</label> <input value={aiSalary} onChange={e => { setAiSalary(e.target.value); setGeneratedJD({ ...generatedJD, salary: e.target.value }); }} placeholder= className= /> </div> </div> <p className= >{generatedJD.description}</p> <div className= > <p className= >Responsibilities:</p> <ul className= > {(generatedJD.responsibilities || []).map((r: string, i: number) => ( <li key={i}>{r}</li> ))} </ul> </div> <div className= > <p className= >Requirements:</p> <ul className= > {(generatedJD.requirements || []).map((r: string, i: number) => ( <li key={i}>{r}</li> ))} </ul> </div> <div className= > <p className= >Benefits:</p> <ul className= > {(generatedJD.benefits || []).map((b: string, i: number) => ( <li key={i}>{b}</li> ))} </ul> </div> <button onClick={() => openProtectedModal({ action: 'post-job', requiredRole: 'recruiter', modalData: { autoPublish: true, title: generatedJD.title, description: generatedJD.description, company: generatedJD.company || user?.user_metadata?.company || '', salary: generatedJD.salary || '', requirements: generatedJD.requirements, benefits: generatedJD.benefits, }, })} className= > <Plus className= /> Publish This Job </button> </div> )} </div></AnimatedSection> )} { } <AnimatedSection direction= bg-white rounded-2xl border border-gray-100 shadow-sm p-4 sm:p-6 mb-6 flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between mb-4 sm:mb-6 text-lg sm:text-xl font-bold text-gray-900 space-y-4 stagger-children stagger-visible flex items-center justify-center py-12 w-8 h-8 border-4 border-blue-600 border-t-transparent rounded-full animate-spin > <Briefcase className= /> <p className= >No jobs posted yet</p> <p className= to create your first listing.</p> </div> ) : ( <> { } {premiumJobs.length > 0 && ( <div className= > <div className= > <Sparkles className= /> <h3 className= >Premium Job Post</h3> <span className= >{premiumJobs.length}</span> </div> <div className= > {premiumJobs.map(job => renderJobCard(job))} </div> </div> )} { } {standardJobs.length > 0 && ( <div className= > <div className= > <Award className= /> <h3 className= >Standard Job Post</h3> <span className= >{standardJobs.length}</span> </div> <div className= > {standardJobs.map(job => renderJobCard(job))} </div> </div> )} { } {basicJobs.length > 0 && ( <div className= > <div className= > <Briefcase className= /> <h3 className= >Basic Job Post</h3> <span className= >{basicJobs.length}</span> </div> <div className= > {basicJobs.map(job => renderJobCard(job))} </div> </div> )} </> )} </div> </div></AnimatedSection> { } <AnimatedSection direction= bg-white rounded-2xl border border-gray-100 shadow-sm p-4 sm:p-6 mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between mb-4 sm:mb-6 text-lg sm:text-xl font-bold text-gray-900 flex items-center justify-center py-12 w-8 h-8 border-4 border-blue-600 border-t-transparent rounded-full animate-spin flex items-center gap-1 text-sm text-blue-700 hover:underline mb-4 /> Back to all applications </button> <div className= > <div className= > <div className= > <h3 className= >{selectedApp.professional_headline || selectedApp.applicant?.professional_headline || 'Applicant'}</h3> <p className= >{selectedApp.applicant?.full_name || 'Applicant'} · {selectedApp.applicant?.email}</p> <p className= >Applied for: {selectedApp.job?.title || 'Job'} · {new Date(selectedApp.created_at).toLocaleDateString()}</p> </div> <span className={`text-xs font-bold px-3 py-1 rounded-full capitalize ${ selectedApp.status === 'pending' ? 'bg-amber-100 text-amber-700' : selectedApp.status === 'shortlisted' ? 'bg-green-100 text-green-700' : selectedApp.status === 'reviewed' ? 'bg-blue-100 text-blue-700' : selectedApp.status === 'rejected' ? 'bg-red-100 text-red-700' : selectedApp.status === 'hired' ? 'bg-emerald-100 text-emerald-700' : 'bg-gray-100 text-gray-600' }`}>{selectedApp.status}</span> </div> <div className= > <div><span className= >DOB:</span> <span className= >{selectedApp.date_of_birth || selectedApp.applicant?.date_of_birth || '-'}</span></div> <div><span className= >Gender:</span> <span className= >{selectedApp.gender || selectedApp.applicant?.gender || '-'}</span></div> <div><span className= >Experience:</span> <span className= >{selectedApp.years_of_experience || selectedApp.applicant?.years_of_experience || '-'}</span></div> <div><span className= >Function:</span> <span className= >{selectedApp.function || selectedApp.applicant?.function || '-'}</span></div> <div><span className= >Work Type:</span> <span className= >{selectedApp.work_type || selectedApp.applicant?.work_type || '-'}</span></div> <div><span className= >Qualification:</span> <span className= >{selectedApp.highest_qualification || selectedApp.applicant?.highest_qualification || '-'}</span></div> <div><span className= >Location:</span> <span className= >{selectedApp.location || selectedApp.applicant?.location || '-'}</span></div> <div><span className= >Availability:</span> <span className= >{selectedApp.availability || selectedApp.applicant?.availability || '-'}</span></div> <div className= >Salary:</span> <span className= >{selectedApp.salary_expectation || selectedApp.applicant?.salary_expectation ? `NGN ${Number.parseInt(selectedApp.salary_expectation || selectedApp.applicant?.salary_expectation || '0', 10).toLocaleString()}` : '-'}</span></div> </div> {(selectedApp.is_disabled || selectedApp.applicant?.is_disabled) === 'yes' ? <span className= >PWD</span> : null} {(selectedApp.is_displaced || selectedApp.applicant?.is_displaced) === 'yes' ? <span className= >Internally Displaced</span> : null} {selectedApp.cover_letter && ( <div> <p className= >Cover Letter:</p> <p className= inline-flex items-center gap-2 text-sm text-blue-700 hover:underline bg-transparent border-none cursor-pointer p-0 /> Download CV </button> {resumeError && <p className= >{resumeError}</p>} </div> )} { } <div className= > {selectedApp.status !== 'shortlisted' && ( <button onClick={() => updateStatus(selectedApp.id, 'shortlisted')} className= > Shortlist </button> )} {selectedApp.status !== 'reviewed' && ( <button onClick={() => updateStatus(selectedApp.id, 'reviewed')} className= > Mark Reviewed </button> )} {selectedApp.status !== 'rejected' && ( <button onClick={() => updateStatus(selectedApp.id, 'rejected')} className= > Reject </button> )} {selectedApp.status !== 'hired' && ( <button onClick={() => updateStatus(selectedApp.id, 'hired')} className= > Hire </button> )} </div> </div> </div> ) : filteredApps.length === 0 ? ( <div className= > <Users className= /> <p className= >No applications yet</p> <p className= >Applications from job seekers will appear here.</p> </div> ) : ( <div className= > {filteredApps.map((app: any) => ( <Card3D key={app.id} className= onClick={() => setSelectedApp(app)} strength={6}> <div className= > <div className= > <div className= > <h3 className= >{app.applicant?.full_name || 'Anonymous'}</h3> <span className={`text-[10px] font-semibold px-2 py-0.5 rounded-full capitalize ${ app.status === 'pending' ? 'bg-amber-100 text-amber-700' : app.status === 'shortlisted' ? 'bg-green-100 text-green-700' : app.status === 'reviewed' ? 'bg-blue-100 text-blue-700' : app.status === 'rejected' ? 'bg-red-100 text-red-700' : app.status === 'hired' ? 'bg-emerald-100 text-emerald-700' : 'bg-gray-100 text-gray-600' }`}>{app.status}</span> </div> <p className= >{app.professional_headline || app.applicant?.professional_headline || 'Applicant'}</p> <p className= > {app.function} · {app.location} · {app.years_of_experience} exp · NGN {parseInt(app.salary_expectation || 0).toLocaleString()} </p> <p className= >Applied for: {app.job?.title || 'Job'} · {new Date(app.created_at).toLocaleDateString()}</p> </div> <ChevronDown className= /> </div> </Card3D> ))} </div> )} </div></AnimatedSection> { } <AnimatedSection direction= bg-white rounded-2xl border border-gray-100 shadow-sm p-4 sm:p-6 mb-6 text-lg sm:text-xl font-bold text-gray-900 mb-4 Recruiter platform demo bg-gradient-to-br from-blue-600 to-blue-700 rounded-2xl p-4 sm:p-6 text-white relative overflow-hidden absolute inset-0 opacity-10 absolute bottom-0 right-0 w-40 h-40 rounded-full bg-white blur-3xl relative flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between text-base sm:text-lg font-bold mb-2 text-blue-100 text-sm shrink-0 bg-white text-blue-700 font-semibold px-4 sm:px-6 py-2.5 rounded-lg hover:bg-blue-50 transition-colors whitespace-nowrap inline-flex items-center self-start sm:self-auto Plus, Users, Briefcase, Calendar, Award, Eye, MessageSquare, Clock, CheckCircle, Edit, TrendingUp, Sparkles, Star, ChevronDown, ChevronUp, X, CreditCard, Download, FileText, ExternalLink, Lock, from 'lucide-react'; const openModal = useModal(); const openProtectedModal = useAuthRequired(); const user, subscription, subscriptionLoaded = useAuth(); const navigate = useNavigate(); const [searchParams] = useSearchParams(); const [postJobOpened, setPostJobOpened] = useState(false); const [myJobs, setMyJobs] = useState ([]); const [jobsLoading, setJobsLoading] = useState(true); async function fetchMyJobs() if (!user?.id) return; try const data, error = await supabase .from('jobs') .select('*') .eq('recruiter_id', user.id) .order('created_at', ascending: false ); if (!error && data) setMyJobs(data); catch (e) console.error('fetch my jobs error', e); finally setJobsLoading(false); useEffect(() => fetchMyJobs(); , [user?.id]); const shouldOpenPostJob = searchParams.get('postJob') === 'true'; useEffect(() => if (!shouldOpenPostJob || postJobOpened) return; setPostJobOpened(true); if (!subscriptionLoaded) return; if (subscription.status === 'active') openProtectedModal( action: 'post-job', requiredRole: 'recruiter' ); navigate('/recruiter', replace: true ); return; navigate('/pricing', replace: true ); , [shouldOpenPostJob, postJobOpened, subscription.status, subscriptionLoaded, openProtectedModal, navigate]); useEffect(() => const handler = () => fetchMyJobs(); window.addEventListener('jobs:updated', handler); return () => window.removeEventListener('jobs:updated', handler); , [user?.id]); const totalApplicants = myJobs.reduce((sum, j) => sum + (j.applications_count || 0), 0); const stats = [ label: 'Active Jobs', value: String(myJobs.filter(j => j.is_active).length), icon: Briefcase, color: 'bg-blue-50 text-blue-700' , label: 'Candidates', value: String(totalApplicants), icon: Users, color: 'bg-emerald-50 text-emerald-700' , label: 'Views', value: String(myJobs.reduce((sum,"
    },
    {
      "file": "ResetPassword.tsx",
      "path": "src/pages/ResetPassword.tsx",
      "title": "Set a New Password",
      "text": "Set a New Password Password Updated! Link Invalid or Expired Set a New Password Choose a strong password for your JobBridge account {bg} {logoHeader} Password Updated! Your password has been changed successfully. You can review or update it anytime from the change password section in your profile. Continue to Profile {bg} {logoHeader} Verifying your reset link... {bg} {logoHeader} Link Invalid or Expired This password reset link is invalid or has expired. Please request a new one from the sign-in page. Back to Sign In {bg} {logoHeader} New Password Confirm New Password {error && ( {saving ? ( Updating Password... Update Password Changed your mind?{' '} Back to Sign In absolute inset-0 overflow-hidden pointer-events-none absolute top-20 left-20 w-96 h-96 bg-blue-500/20 rounded-full blur-3xl animate-pulse absolute bottom-20 right-20 w-72 h-72 bg-indigo-400/20 rounded-full blur-3xl animate-pulse absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[min(600px,90vw)] h-[min(600px,90vw)] bg-blue-600/5 rounded-full blur-3xl > <div className= > <JobBridgeLogo variant= iconSize={40} /> </div> <h1 className= >Set a New Password</h1> <p className= >Choose a strong password for your JobBridge account</p> </div> ); if (success) { return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <div className= > <CheckCircle className= /> </div> <h2 className= >Password Updated!</h2> <p className= > Your password has been changed successfully. You can review or update it anytime from the change password section in your profile. </p> <Link to= className= > Continue to Profile </Link> </div> </div> </div> ); } if (checking) { return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <Loader2 className= /> <p className= >Verifying your reset link...</p> </div> </div> </div> ); } if (invalid) { return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <div className= > <AlertCircle className= /> </div> <h2 className= >Link Invalid or Expired</h2> <p className= > This password reset link is invalid or has expired. Please request a new one from the sign-in page. </p> <Link to= className= > Back to Sign In </Link> </div> </div> </div> ); } return ( <div className= > {bg} <div className= > {logoHeader} <div className= > <form onSubmit={handleSubmit} className= noValidate> <div> <label htmlFor= block text-sm font-medium text-gray-700 mb-1.5 > <Lock className= /> <input id= type= value={newPassword} onChange={(e) => setNewPassword(e.target.value)} placeholder= autoFocus className={`w-full pl-12 pr-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ error ? 'border-red-400 bg-red-50' : 'border-gray-300' }`} /> </div> </div> <div> <label htmlFor= block text-sm font-medium text-gray-700 mb-1.5 > <KeyRound className= /> <input id= type= value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder= className={`w-full pl-12 pr-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ error ? 'border-red-400 bg-red-50' : 'border-gray-300' }`} /> </div> </div> {error && ( <div className= > <AlertCircle className= /> <span>{error}</span> </div> )} <button type= disabled={saving} className= > {saving ? ( <> <Loader2 className= /> Updating Password... </> ) : ( <> <CheckCircle className= /> Update Password </> )} </button> </form> <p className= > Changed your mind?{' '} <Link to= font-semibold text-blue-700 hover:text-blue-800 transition-colors const updatePassword = useAuth(); const [checking, setChecking] = useState(true); const [ready, setReady] = useState(false); const [invalid, setInvalid] = useState(false); const [newPassword, setNewPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const [success, setSuccess] = useState(false); useEffect(() => let cancelled = false; const finalizeReady = () => if (cancelled) return; setChecking(false); setReady(true); ; const data: sub = supabase.auth.onAuthStateChange((event) => if (event === 'PASSWORD_RECOVERY') finalizeReady(); ); const check = async () => try const searchParams = new URLSearchParams(window.location.search); const code = searchParams.get('code'); if (code) const error = await supabase.auth.exchangeCodeForSession(code); if (error) console.warn('[ResetPassword] code exchange failed:', error); try window.history.replaceState( , document.title, window.location.pathname); catch const data: session , = await supabase.auth.getSession(); if (cancelled) return; if (session?.user) finalizeReady(); return; setTimeout(() => if (cancelled) return; setChecking(false); setInvalid(true); , 2500); catch (err) if (cancelled) return; console.error('[ResetPassword] session check failed:', err); setChecking(false); setInvalid(true); ; check(); return () => cancelled = true; sub.subscription.unsubscribe(); ; , []); const handleSubmit = async (e: React.FormEvent) => e.preventDefault(); setError(''); if (!newPassword.trim() || !confirmPassword.trim()) setError('Please enter and confirm your new password.'); return; if (newPassword !== confirmPassword) setError('Passwords do not match.'); return; if (newPassword.length ); const logoHeader = ( Set a New Password Choose a strong password for your JobBridge account ); if (success) return ( bg logoHeader Password Updated! Your password has been changed successfully. You can review or update it anytime from"
    },
    {
      "file": "Revenue.tsx",
      "path": "src/pages/Revenue.tsx",
      "title": "Revenue",
      "text": "Revenue Breakdown Growth Projections (6 Months) Pricing Tiers {plan.name} Revenue Streams {stream.name} Investor Metrics {change} Revenue Breakdown Premium Subscriptions Enterprise Growth Projections (6 Months) Expected MRR Conversion Rate {proj.month} {proj.expectedMrr} {proj.users} {proj.conversionRate} Pricing Tiers {plan.name} {plan.price} {plan.period} {feature} {plan.buttonVariant === 'primary' ? ( {plan.button} ) : plan.name === 'Enterprise' ? ( {plan.button} {plan.button} Revenue Streams {stream.name} {stream.growth} {stream.revenue} Investor Metrics {metric.value} {metric.label} {metric.description} min-h-screen bg-stone-50 Revenue & Growth Dashboard Real-time metrics and growth forecasts Business growth and revenue charts max-w-7xl mx-auto px-4 sm:px-6 py-8 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-8 bg-white rounded-2xl p-6 border border-gray-100 shadow-sm flex items-start justify-between mb-4 /> </div> <span className= > {change} </span> </div> <div className= >{value}</div> <div className= >{label}</div> </div> ))} </div> <div className= > { } <div className= > { } <div className= > <h2 className= >Revenue Breakdown</h2> <div className= > <div className= style={{ background: 'conic-gradient(from 0deg, rgb(29, 78, 216) 0deg 241.2deg, rgb(7, 89, 133) 241.2deg 387.54deg, rgb(253, 224, 71) 387.54deg 360deg)', }} /> </div> <div className= > <div className= > <div className= > <div className= /> <span className= >Premium Subscriptions</span> </div> <span className= >67%</span> </div> <div className= > <div className= > <div className= style={{ backgroundColor: 'rgb(7, 89, 133)' }} /> <span className= >Enterprise</span> </div> <span className= >23%</span> </div> <div className= > <div className= > <div className= /> <span className= >Services</span> </div> <span className= >10%</span> </div> </div> </div> { } <div className= > <div className= > <h2 className= >Growth Projections (6 Months)</h2> </div> <div className= > <table className= > <thead> <tr className= > <th className= >Month</th> <th className= >Expected MRR</th> <th className= >Users</th> <th className= >Conversion Rate</th> </tr> </thead> <tbody> {projections.map((proj, i) => ( <tr key={i} className= > <td className= >{proj.month}</td> <td className= >{proj.expectedMrr}</td> <td className= >{proj.users}</td> <td className= >{proj.conversionRate}</td> </tr> ))} </tbody> </table> </div> </div> { } <div className= > <h2 className= >Pricing Tiers</h2> <div className= > {plans.map((plan, i) => ( <div key={i} className={`rounded-xl border-2 p-6 ${ plan.isCurrentPlan ? 'border-blue-700 bg-blue-50' : 'border-gray-200 bg-white' }`} > <h3 className= >{plan.name}</h3> <div className= > <span className= >{plan.price}</span> <span className= >{plan.period}</span> </div> <ul className= > {plan.features.map((feature, j) => ( <li key={j} className= > <span className= >✓</span> {feature} </li> ))} </ul> {plan.buttonVariant === 'primary' ? ( <button onClick={() => openModal('premium')} className= > {plan.button} </button> ) : plan.name === 'Enterprise' ? ( <button onClick={() => openModal('message', { name: 'Sales Team' })} className= > {plan.button} </button> ) : ( <button disabled className= > {plan.button} </button> )} </div> ))} </div> </div> </div> { } <div className= > { } <div className= > <h2 className= >Revenue Streams</h2> <div className= > {revenueStreams.map((stream, i) => ( <div key={i} className= > <div className= > <h3 className= >{stream.name}</h3> <span className= > {stream.growth} </span> </div> <p className= >{stream.revenue}</p> </div> ))} </div> </div> { } <div className= > <h2 className= >Investor Metrics</h2> <div className= > {investorMetrics.map((metric, i) => ( <div key={i} className= > <div className= > <div className= >{metric.value}</div> <span className= >{metric.label}</span> </div> <p className= TrendingUp, Percent, Users, Zap, Globe, FileText, from 'lucide-react'; const openModal = useModal(); const kpis = [ label: 'MRR', value: '₦284K', change: '+18%', icon: TrendingUp, color: 'bg-blue-50 text-blue-700', , label: 'ARR', value: '₦3.4M', change: '+22%', icon: TrendingUp, color: 'bg-emerald-50 text-emerald-700', , label: 'Churn Rate', value: '2.1%', change: '-0.3%', icon: Percent, color: 'bg-amber-50 text-amber-700', , label: 'LTV', value: '₦847', change: '+12%', icon: Users, color: 'bg-rose-50 text-rose-700', , ]; const projections = [ month: 'Jan', expectedMrr: '₦280K', users: '2.05M', conversionRate: '6.2%' , month: 'Feb', expectedMrr: '₦298K', users: '2.15M', conversionRate: '6.5%' , month: 'Mar', expectedMrr: '₦315K', users: '2.25M', conversionRate: '6.8%' , month: 'Apr', expectedMrr: '₦332K', users: '2.35M', conversionRate: '7.1%' , month: 'May', expectedMrr: '₦351K', users: '2.45M', conversionRate: '7.4%' , month: 'Jun', expectedMrr: '₦368K', users: '2.55M', conversionRate: '7.7%' , ]; const plans = [ name: 'Free', price: '₦0', period: '/mo', features: ['5 job applications/mo', 'Basic matching', 'Community support'], button: 'Starter', buttonVariant: 'secondary', , name: 'Professional', price: '₦29', period: '/mo', features: ['Unlimited applications', 'AI matching', 'Priority support', 'Advanced analytics'], button: 'Upgrade', buttonVariant: 'primary', isCurrentPlan: true, , name: 'Enterprise', price: '₦99', period: '/mo per seat', features: ['Everything included', 'API access', 'White-label', 'Dedicated CSM', 'SSO'], button: 'Contact Sales', buttonVariant: 'secondary', , ]; const Crown = TrendingUp; const Building = Users; const Briefcase = TrendingUp; const revenueStreams = [ name: 'Premium Subscriptions', revenue: '₦190K', growth: '+24%', icon: Crown, , name: 'Enterprise Licenses', revenue: '₦65K', growth: '+18%', icon: Building, , name: 'Service Marketplace', revenue: '₦18K', growth: '+12%', icon: Briefcase, , name: 'AI Resume Tools', revenue: '₦8"
    },
    {
      "file": "Reviews.tsx",
      "path": "src/pages/Reviews.tsx",
      "title": "Reviews & Ratings",
      "text": "Reviews & Ratings Leave a Review {selectedProvider ? `Reviews for ${selectedProvider.full_name || selectedProvider.email}` : 'All Reviews'} {active || '—'} Reviews & Ratings Leave a star rating for a service provider and see what the community says. {averageRating.toFixed(1)} based on {reviews.length} review{reviews.length === 1 ? '' : 's'} Leave a Review Rate a provider and share your experience. You can update or delete your review anytime. {!isAuthenticated ? ( Sign in to leave a review and star rating. Sign in Provider to review — Select a provider — {p.full_name || p.email || 'Provider'} — {p.specialty || p.service_category || 'Professional'} {providers.length === 0 && ( No providers available yet. Your rating Your review {myExisting && selectedProvider && ( You already reviewed {selectedProvider.full_name || 'this provider'} — save to update it. {error} Reviewing: {selectedProvider.full_name || selectedProvider.email} {submitting ? ( {selectedProvider ? `Reviews for ${selectedProvider.full_name || selectedProvider.email}` : 'All Reviews'} {reviews.length} total {loading ? ( ) : reviews.length === 0 ? ( No reviews yet. Be the first to leave a review! {review.reviewer_name || 'Anonymous'} reviewed {review.reviewee_name || 'a provider'} {review.rating}.0 · {formatDate(review.created_at)} {myReviewIds.has(review.id) && ( {review.comment && ( {review.comment} Back to profile flex items-center gap-1 aria-label={`Rate ${n} star${n > 1 ? 's' : ''}`} onMouseEnter={() => setHover(n)} onClick={() => onChange(n)} className= > <Star style={{ width: size, height: size }} className={`${n <= active ? 'fill-amber-400 text-amber-400' : 'text-gray-300'} transition-colors`} /> </button> ))} <span className= > {active || '—'} </span> </div> ); } function StarDisplay({ rating, size = 14 }: { rating: number; size?: number }) { return ( <div className= > {Array.from({ length: 5 }).map((_, i) => ( <Star key={i} style={{ width: size, height: size }} className={i < Math.round(rating) ? 'fill-amber-400 text-amber-400' : 'text-gray-300'} /> ))} </div> ); } function formatDate(iso: string) { try { return new Date(iso).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric', }); } catch { return ''; } } const location = useLocation(); const { user, profile } = useAuth(); const [reviews, setReviews] = useState<Review[]>([]); const [providers, setProviders] = useState<Profile[]>([]); const [loading, setLoading] = useState(true); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(''); const [selectedProviderId, setSelectedProviderId] = useState(''); const [rating, setRating] = useState(0); const [comment, setComment] = useState(''); const [myExisting, setMyExisting] = useState<Review | null>(null); const isAuthenticated = !!user; const loadReviews = useCallback(async () => { setLoading(true); const data = await fetchReviews(selectedProviderId || undefined); setReviews(data); setLoading(false); }, [selectedProviderId]); useEffect(() => { loadReviews().catch(() => setLoading(false)); }, [loadReviews]); useEffect(() => { if (!isAuthenticated) return; fetchProviders() .then(setProviders) .catch(() => setProviders([])); }, [isAuthenticated]); useEffect(() => { const params = new URLSearchParams(location.search); const providerId = params.get('providerId') || ''; if (providerId) { setSelectedProviderId(providerId); } }, [location.search]); useEffect(() => { if (!isAuthenticated || !selectedProviderId) { setMyExisting(null); setRating(0); setComment(''); return; } let cancelled = false; fetchMyReviewForTarget(user.id, selectedProviderId).then((existing) => { if (cancelled) return; setMyExisting(existing); setRating(existing?.rating || 0); setComment(existing?.comment || ''); }); return () => { cancelled = true; }; }, [isAuthenticated, selectedProviderId, user?.id]); const selectedProvider = useMemo( () => providers.find((p) => p.id === selectedProviderId) || null, [providers, selectedProviderId], ); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (!isAuthenticated || !user) return; if (!selectedProviderId) { setError('Please choose a provider to review.'); return; } if (rating < 1) { setError('Please select a star rating (1–5).'); return; } setSubmitting(true); setError(''); try { if (myExisting) { await updateReview(myExisting.id, { rating, comment }); } else { await createReview({ reviewerId: user.id, reviewerName: profile?.full_name || user.email || 'A user', revieweeId: selectedProviderId, targetType: 'provider', rating, comment, }); } await loadReviews(); const updatedReview = await fetchMyReviewForTarget(user.id, selectedProviderId); setMyExisting(updatedReview); setRating(updatedReview?.rating || rating); setComment(updatedReview?.comment || comment); } catch (err) { console.error('[Reviews] submit failed:', err); setError('Could not save your review. Please try again.'); } finally { setSubmitting(false); } }; const handleDelete = async (review: Review) => { if (!isAuthenticated) return; if (!window.confirm('Delete this review?')) return; try { await deleteReview(review.id); await loadReviews(); } catch (err) { console.error('[Reviews] delete failed:', err); } }; const myReviewIds = useMemo( () => new Set(reviews.filter((r) => r.reviewer_id === user?.id).map((r) => r.id)), [reviews, user?.id], ); const averageRating = useMemo(() => { if (reviews.length === 0) return 0; return reviews.reduce((sum, r) => sum + Number(r.rating), 0) / reviews.length; }, [reviews]); return ( <AppLayout> <section className= > { } <div className= > <div className= > <div className= > <Star className= /> </div> <h1 className= >Reviews & Ratings</h1> </div> <p className= > Leave a star rating for a service provider and see what the community says. </p> {reviews.length > 0 && ( <div className= > <StarDisplay rating={averageRating} size={18} /> <span className= > {averageRating.toFixed(1)} </span> <span className= > based on {reviews.length} review{reviews.length === 1 ? '' : 's'} </span> </div> )} </div> { } <div className= > <h2 className= >Leave a Review</h2> <p className= > Rate a provider and share your experience. You can update or delete your review anytime. </p> {!isAuthenticated ? ( <div className= > <LogIn className= /> <p className= > Sign in to leave a review and star rating. </p> <Link to= className= > Sign in <ArrowRight className= /> </Link> </div> ) : ( <form onSubmit={handleSubmit} className= > { } <div> <label className= > Provider to review </label> <select value={selectedProviderId} onChange={(e) => setSelectedProviderId(e.target.value)} className= > <option value= >— Select a provider —</option> {providers.map((p) => ( <option key={p.id} value={p.id}> {p.full_name || p.email || 'Provider'} — {p.specialty || p.service_category || 'Professional'} </option> ))} </select> {providers.length === 0 && ( <p className= >No providers available yet.</p> )} </div> { } <div> <label className= > Your rating </label> <StarRatingInput value={rating} onChange={setRating} /> </div> { } <div> <label className= > Your review </label> <textarea value={comment} onChange={(e) => setComment(e.target.value)} rows={3} maxLength={1000} placeholder= className= /> </div> {myExisting && selectedProvider && ( <div className= > <CheckCircle2 className= /> You already reviewed {selectedProvider.full_name || 'this provider'} — save to update it. </div> )} {error && ( <p className= > {error} </p> )} {selectedProvider && ( <div className= > Reviewing: <span className= >{selectedProvider.full_name || selectedProvider.email}</span> </div> )} <button type= disabled={submitting || !selectedProviderId} className= > {submitting ? ( <Loader2 className= /> ) : ( <Send className= /> )} {myExisting ? 'Update Review' : 'Submit Review'} </button> </form> )} </div> { } <div className= > <h2 className= > {selectedProvider ? `Reviews for ${selectedProvider.full_name || selectedProvider.email}` : 'All Reviews'} </h2> <span className= >{reviews.length} total</span> </div> {loading ? ( <div className= > <Loader2 className= /> </div> ) : reviews.length === 0 ? ( <div className= > <Star className= /> <p className= > No reviews yet. Be the first to leave a review! </p> </div> ) : ( <div className= > {reviews.map((review) => ( <div key={review.id} className= > <div className= > <div className= > <div className= > <p className= > {review.reviewer_name || 'Anonymous'} </p> <span className= > reviewed {review.reviewee_name || 'a provider'} </span> </div> <div className= > <StarDisplay rating={review.rating} /> <span className= > {review.rating}.0 · {formatDate(review.created_at)} </span> </div> </div> {myReviewIds.has(review.id) && ( <button type= onClick={() => handleDelete(review)} aria-label= className= > <Trash2 className= /> </button> )} </div> {review.comment && ( <p className= >{review.comment}</p> )} </div> ))} </div> )} <div className= > <Link to= className= > Back to profile <ArrowRight className= fetchReviews, createReview, updateReview, deleteReview, fetchProviders, fetchMyReviewForTarget, from '../lib/supabaseQueries'; function StarRatingInput( value, onChange, size = 32, : value: number; onChange: (v: number) => void; size?: number; ) const [hover, setHover] = useState(0); const active = hover || value; return ( setHover(0) > [1, 2, 3, 4, 5].map((n) => ( 1 ? 's' : '' ` onMouseEnter= () => setHover(n) onClick= () => onChange(n) className=\"p-0.5 transition-transform hover:scale-125 focus:outline-none\" > )) active || '—' ); function StarDisplay( rating, size = 14 : rating: number; size?: number ) return ( Array.from( length: 5 ).map((_, i) => ( )) ); function formatDate(iso: string) try return new Date(iso).toLocaleDateString(undefined, year: 'numeric', month: 'short', day: 'numeric', ); catch return ''; const location = useLocation(); const user, profile = useAuth(); const [reviews, setReviews] = useState ([]); const [providers, setProviders] = useState ([]); const [loading, setLoading] = useState(true); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(''); const [selectedProviderId, setSelectedProviderId] = useState(''); const [rating, setRating] = useState(0); const [comment, setComment] = useState(''); const [myExisting, setMyExisting] = useState (null); const isAuthenticated = !!user; const loadReviews = useCallback(async () => setLoading(true); const data = await fetchReviews(selectedProviderId || undefined); setReviews(data); setLoading(false); , [selectedProviderId]); useEffect(() => loadReviews().catch(() => setLoading(false)); , [loadReviews]); useEffect(() => if (!isAuthenticated) return; fetchProviders() .then(setProviders) .catch(() => setProviders([])); , [isAuthenticated]); useEffect(() => const params = new URLSearchParams(location.search); const providerId = params.get('providerId') || ''; if (providerId) setSelectedProviderId(providerId); , [location.search]); useEffect(() => if (!isAuthenticated || !s"
    },
    {
      "file": "Services.tsx",
      "path": "src/pages/Services.tsx",
      "title": "Services",
      "text": "{provider.name} How It Works {item.title} Post a Request {category} {provider.rating} ({provider.reviews}) {provider.name} {provider.title} ${provider.rate} {skill} Hire How It Works {item.icon} {item.title} {item.description} flex flex-col min-h-screen bg-gray-50 flex-1 pb-20 md:pb-0 subtitle= images={HERO_CAROUSELS.providers} imageAlt= compact /> <div className= > <button onClick={() => openModal('service-request')} className= > <Zap className= /> Post a Request </button> </div> { } <section className= > <div className= > <div className= > {categories.map((category) => ( <button key={category} onClick={() => setActiveCategory(category)} className={`px-5 py-2 rounded-full font-medium whitespace-nowrap transition duration-200 ${ activeCategory === category ? 'bg-blue-700 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200' }`} > {category} </button> ))} </div> </div> </section> { } <section className= > <div className= > {filteredProviders.map((provider) => ( <div key={provider.id} className= > <div className= > { } <div className= > <img src={provider.img} alt={provider.name} className= /> <div className= > <Star className= /> <span className= >{provider.rating}</span> <span className= >({provider.reviews})</span> </div> </div> { } <h3 className= >{provider.name}</h3> <p className= >{provider.title}</p> { } <div className= > <p className= > ${provider.rate} <span className= >/hr</span> </p> </div> { } <div className= > {provider.skills.map((skill, idx) => ( <span key={idx} className= > {skill} </span> ))} </div> { } <div className= > <button onClick={() => openModal('hire', { name: provider.name, role: provider.title })} className= > Hire </button> <button onClick={() => openModal('message', { name: provider.name })} className= > <MessageSquare className= /> </button> </div> </div> </div> ))} </div> </section> { } <section className= > <div className= > <h2 className= >How It Works</h2> <div className= > {[ { step: '1', title: 'Post Request', description: 'Tell us what you need. Our experts will match with your requirements.', icon: <Zap className= />, }, { step: '2', title: 'Get Matched', description: 'Review profiles and proposals from top professionals.', icon: <Users className= />, }, { step: '3', title: 'Start Working', description: 'Collaborate with your chosen professional and get results.', icon: <CheckCircle className= />, }, ].map((item, idx) => ( <div key={idx} className= > <div className= > {item.icon} </div> <h3 className= >{item.title}</h3> <p className= const Services = () => const openModal = useModal(); const [activeCategory, setActiveCategory] = useState('All'); const categories = ['All', 'Development', 'Design', 'Marketing', 'Writing', 'Finance', 'Legal', 'Consulting', 'Other']; const providers = [ id: 1, name: 'Alex Rivera', title: 'Full-Stack Developer', rating: 5.0, reviews: 128, rate: 95, skills: ['React', 'Node.js', 'PostgreSQL'], img: 'https: bgColor: 'bg-blue-600', , id: 2, name: 'Mia Chen', title: 'Brand Designer', rating: 4.9, reviews: 95, rate: 80, skills: ['Figma', 'Illustrator', 'Branding'], img: 'https: bgColor: 'bg-blue-500', , id: 3, name: 'Sam Taylor', title: 'SEO Specialist', rating: 4.8, reviews: 72, rate: 65, skills: ['SEO', 'Analytics', 'Content'], img: 'https: bgColor: 'bg-blue-600', , id: 4, name: 'Jordan Lee', title: 'React Developer', rating: 4.9, reviews: 110, rate: 90, skills: ['React', 'TypeScript', 'Next.js'], img: 'https: bgColor: 'bg-blue-700', , id: 5, name: 'Chris Morgan', title: 'Content Writer', rating: 4.7, reviews: 88, rate: 55, skills: ['Copywriting', 'SEO', 'Strategy'], img: 'https: bgColor: 'bg-blue-500', , id: 6, name: 'Pat Williams', title: 'Financial Advisor', rating: 5.0, reviews: 65, rate: 120, skills: ['Finance', 'Tax', 'Planning'], img: 'https: bgColor: 'bg-blue-600', , id: 7, name: 'Riley Smith', title: 'Legal Consultant', rating: 4.8, reviews: 54, rate: 150, skills: ['Contracts', 'IP', 'Compliance'], img: 'https: bgColor: 'bg-blue-700', , id: 8, name: 'Dana Brown', title: 'Marketing Lead', rating: 4.9, reviews: 92, rate: 85, skills: ['Campaigns', 'Social', 'Analytics'], img: 'https: bgColor: 'bg-blue-600', , ]; const categoryMap: Record = 'Development': ['Full-Stack Developer', 'React Developer', 'Backend Engineer'], 'Design': ['Brand Designer', 'UX Researcher'], 'Marketing': ['SEO Specialist', 'Marketing Lead', 'Content Writer'], 'Writing': ['Content Writer'], 'Finance': ['Financial Advisor'], 'Legal': ['Legal Consultant'], 'Consulting': ['Financial Advisor', 'Le"
    },
    {
      "file": "Signup.tsx",
      "path": "src/pages/Signup.tsx",
      "title": "Welcome to JobBridge",
      "text": "Welcome to JobBridge Choose how you want to join Sign up as a Job Seeker Sign up as Recruiter Sign up as Service Provider Check Your Email What happens next? {selectedRole === \"recruiter\" ? \"Create Recruiter Account\" : selectedRole === \"provider\" ? \"Create Service Provider Account\" : selectedRole === \"admin\" ? \"Create Admin Account\" : \"Create Job Seeker Account\"} {bg} Welcome to JobBridge Connect. Hire. Grow. Nigeria's #1 Professional Network {step === \"role\" ? ( Choose how you want to join Select your role to get started Sign up as a Job Seeker Browse jobs, apply with AI-optimized resumes, and land your dream role {feature} Sign up as Recruiter Post jobs, find talent, and hire top candidates {feature} Sign up as Service Provider Offer services, get clients, grow your business {feature} Already have an account?{\" \"} Sign in Admin accounts are provisioned internally ) : emailSent ? ( Check Your Email We sent a confirmation link to {formData.email} {emailStatusMessage ? ( Email delivery issue {emailStatusMessage} ) : null} What happens next? Open the email we just sent you Click the Confirm your email button in the email Return here and with your password {resendMessage && ( {resendMessage} {resending ? ( Resending... Resend confirmation email Didn't receive the email? Check your spam folder or click resend above. Go to Sign In Use a different email ← Back to role selection {selectedRole === \"recruiter\" ? ( ) : selectedRole === \"provider\" ? ( ) : selectedRole === \"admin\" ? ( Full Name * {nameError && ( {nameError} Email Address * {emailError && ( {emailError} Password * {showPassword ? 0 && formData.password.length Password must be at least 6 characters Company Name (Optional) Service Category * Select your service category {category} I agree to the{\" \"} Terms of Service Privacy Policy {typeof error === \"string\" ? error : \"An error occurred. Please try again.\"} {loading ? ( Creating Account... Create Account Your information is secure and encrypted Already have an account?{\" \"} Sign in By continuing, you agree to JobBridge's{\" \"} Terms of Service Privacy Policy ; function getPasswordStrength(pw: string): { score: number; label: string; color: string; bg: string } { if (!pw) return { score: 0, label: '', color: '', bg: '' }; let score = 0; if (pw.length >= 8) score++; if (pw.length >= 12) score++; if (/[A-Z]/.test(pw)) score++; if (/[0-9]/.test(pw)) score++; if (/[^A-Za-z0-9]/.test(pw)) score++; if (score <= 1) return { score, label: 'Very Weak', color: 'text-red-600', bg: 'bg-red-500' }; if (score === 2) return { score, label: 'Weak', color: 'text-orange-600', bg: 'bg-orange-500' }; if (score === 3) return { score, label: 'Fair', color: 'text-yellow-600', bg: 'bg-yellow-500' }; if (score === 4) return { score, label: 'Strong', color: 'text-blue-600', bg: 'bg-blue-500' }; return { score, label: 'Very Strong', color: 'text-green-600', bg: 'bg-green-500' }; } const RESEND_COOLDOWN = 60; const { signUp } = useAuth(); const navigate = useNavigate(); const [step, setStep] = useState< ); const [selectedRole, setSelectedRole] = useState<UserRole>(null); const [formData, setFormData] = useState({ name: , serviceCategory: , agreeToTerms: false, }); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null); const [nameError, setNameError] = useState( ); const [emailError, setEmailError] = useState( ); const [showPassword, setShowPassword] = useState(false); const [emailSent, setEmailSent] = useState(false); const [emailStatusMessage, setEmailStatusMessage] = useState<string | null>(null); const [resending, setResending] = useState(false); const [resendCooldown, setResendCooldown] = useState(0); const [resendMessage, setResendMessage] = useState( ); const handleRoleSelect = (role: UserRole) => { setSelectedRole(role); setStep( ); }; const strength = getPasswordStrength(formData.password); const startCooldown = useCallback(() => { setResendCooldown(RESEND_COOLDOWN); const interval = setInterval(() => { setResendCooldown((prev) => { if (prev <= 1) { clearInterval(interval); return 0; } return prev - 1; }); }, 1000); }, []); const handleResendEmail = async () => { if (resendCooldown > 0) return; setResending(true); setResendMessage( ); const { error: signupErr } = await signUp( formData.email, formData.password, formData.name, selectedRole, formData.company, selectedRole === ? formData.serviceCategory : undefined, ); setResending(false); if (signupErr) { if ( signupErr.message.toLowerCase().includes( ) || signupErr.message.toLowerCase().includes( ) ) { setResendMessage( ); startCooldown(); } else { setResendMessage(`Could not resend: ${signupErr.message}`); } } else { setResendMessage( ); startCooldown(); } }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!selectedRole) return; setError(null); setNameError( ); setEmailError( ); if (!checkRateLimit( , 5, 60000)) { setError( ); return; } if (formData.name.trim().length < 2) { setNameError( Please enter a valid email address. Password must be at least 6 characters. && !formData.serviceCategory) { setError( ); return; } (async () => { setLoading(true); try { const { error: signupErr, session: newSession, emailWarning } = await signUp( formData.email, formData.password, formData.name, selectedRole, formData.company, selectedRole === ? formData.serviceCategory : undefined, ); if (signupErr) { let msg = ; const errObj: any = signupErr; if (typeof errObj.message === && errObj.message.trim()) { msg = errObj.message.trim(); } console.error( , signupErr); setError(msg); window.dispatchEvent( new CustomEvent( , { detail: { message: msg, type: }, }), ); setLoading(false); return; } if (emailWarning) { setEmailStatusMessage(emailWarning); } else { setEmailStatusMessage(null); } if (newSession) { setLoading(false); navigate( ); window.dispatchEvent( new CustomEvent( , { detail: { message: , type: , }, }), ); } else { setEmailSent(true); setLoading(false); startCooldown(); window.dispatchEvent( new CustomEvent( , { detail: { message: , type: , }, }), ); } } catch (e: any) { console.error( , e); const errorMessage = typeof e?.message === && e.message.trim() ? e.message.trim() : ; setError(errorMessage); setLoading(false); } })(); }; const handleBack = () => { setStep( ); setSelectedRole(null); setError(null); setNameError( ); setEmailError( ); setEmailStatusMessage(null); }; const bg = ( <div className= > <div className= /> <div className= style={{ animationDelay: '1s' }} /> <img src= absolute inset-0 w-full h-full object-cover opacity-5 min-h-screen bg-gradient-to-br from-blue-900 via-blue-800 to-indigo-900 flex items-center justify-center p-4 relative relative w-full max-w-2xl > <div className= > <JobBridgeLogo variant= iconSize={40} /> </div> <h1 className= >Welcome to JobBridge</h1> <p className= > Connect. Hire. Grow. Nigeria's #1 Professional Network </p> </div> {step === ? ( <div className= > <h2 className= > Choose how you want to join </h2> <p className= > Select your role to get started </p> <div className= > { } <button onClick={() => handleRoleSelect( )} className= > <div className= > <div className= > <User className= /> </div> <div className= > <h3 className= > Sign up as a Job Seeker </h3> <p className= > Browse jobs, apply with AI-optimized resumes, and land your dream role </p> </div> <ArrowRight className= /> </div> <div className= > <ul className= Cover letter generator ].map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> </div> </button> <div className= > { } <button onClick={() => handleRoleSelect( )} className= > <div className= > <div className= > <Building className= /> </div> <div className= > <h3 className= >Sign up as Recruiter</h3> <p className= >Post jobs, find talent, and hire top candidates</p> </div> <ArrowRight className= /> </div> <div className= > <ul className= Access talent database ].map((feature) => ( <li key={feature} className= > <Check className= /> {feature} </li> ))} </ul> </div> </button> { } <button onClick={() => handleRoleSelect( )} className= > <div className= > <div className= > <Wrench className= /> </div> <div className= > <h3 className= >Sign up as Service Provider</h3> <p className= >Offer services, get clients, grow your business</p> </div> <ArrowRight className= /> </div> <div className= > <ul className= Get featured visibility flex items-center gap-2 text-sm text-gray-600 w-4 h-4 text-emerald-600 mt-8 pt-6 border-t border-gray-100 text-center text-sm text-gray-500 } <Link to= font-semibold text-blue-700 hover:text-blue-800 transition-colors mt-3 text-xs text-gray-400 flex items-center justify-center gap-1 mx-auto /> Admin accounts are provisioned internally </p> </div> </div> ) : emailSent ? ( <div className= > <div className= > <Mail className= /> </div> <h2 className= >Check Your Email</h2> <p className= >We sent a confirmation link to</p> <p className= >{formData.email}</p> {emailStatusMessage ? ( <div className= > <p className= >Email delivery issue</p> <p className= >{emailStatusMessage}</p> </div> ) : null} <div className= > <h3 className= >What happens next?</h3> <ol className= > <li className= > <span className= >1.</span> <span>Open the email we just sent you</span> </li> <li className= > <span className= >2.</span> <span>Click the <strong>Confirm your email</strong> button in the email</span> </li> <li className= > <span className= >3.</span> <span>Return here and <strong>Sign in</strong> with your password</span> </li> </ol> </div> { } <div className= > {resendMessage && ( <p className={`text-sm mb-3 font-medium ${resendMessage.startsWith('Could') ? 'text-red-600' : 'text-green-600'}`}> {resendMessage} </p> )} <button onClick={handleResendEmail} disabled={resending || resendCooldown > 0} className= > {resending ? ( <> <Loader2 className= /> Resending... </> ) : resendCooldown > 0 ? ( `Resend available in ${resendCooldown}s` ) : ( <> <RefreshCw className= /> Resend confirmation email </> )} </button> </div> <p className= > Didn't receive the email? Check your spam folder or click resend above. </p> <button onClick={() => navigate( )} className= > Go to Sign In </button> <button onClick={() => { setEmailSent(false); setStep( ); setResendMessage( ); setResendCooldown(0); }} className= > Use a different email </button> </div> ) : ( <div className= > <button onClick={handleBack} className= > ← Back to role selection </button> <div className= > <div className={`w-10 h-10 rounded-xl flex items-center justify-center ${ selectedRole === : selectedRole === : selectedRole === }`} > {selectedRole === ? ( <Building className= /> ) : selectedRole === ? ( <Wrench className= /> ) : selectedRole === ? ( <Shield className= /> ) : ( <User className= /> )} </div> <div> <h2 className= > {selectedRole === : selectedRole === : selectedRole === } </h2> <p className= > {selectedRole === } </p> </div> </div> <form onSubmit={handleSubmit} className= noValidate> { } <div> <label className= > Full Name * </label> <input type= required minLength={2} value={formData.name} onChange={(e) => { setFormData({ ...formData, name: e.target.value }); if (e.target.value.trim().length >= 2) setNameError( ); }} className={`w-full px-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ nameError ? }`} placeholder= /> {nameError && ( <p className= > <AlertCircle className= /> {nameError} </p> )} </div> { } <div> <label className= > Email Address * </label> <input type= required value={formData.email} onChange={(e) => { setFormData({ ...formData, email: e.target.value }); if (emailError) setEmailError( ); }} className={`w-full px-4 py-3 border rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition ${ emailError ? }`} placeholder= /> {emailError && ( <p className= > <AlertCircle className= /> {emailError} </p> )} </div> { } <div> <label className= > Password * </label> <div className= > <input type={showPassword ? } required minLength={6} value={formData.password} onChange={(e) => setFormData({ ...formData, password: e.target.value }) } className= placeholder= /> <button type= onClick={() => setShowPassword(!showPassword)} className= aria-label={showPassword ? } > {showPassword ? <EyeOff className= /> : <Eye className= />} </button> </div> { } {formData.password.length > 0 && ( <div className= > <div className= > {[1, 2, 3, 4, 5].map((i) => ( <div key={i} className={`h-1.5 flex-1 rounded-full transition-all duration-300 ${ i <= strength.score ? strength.bg : }`} /> ))} </div> <p className={`text-xs font-medium ${strength.color}`}> {strength.label} {strength.score < 3 && } </p> </div> )} {formData.password.length > 0 && formData.password.length < 6 && ( <p className= > Password must be at least 6 characters </p> )} </div> { } {selectedRole === && ( <div> <label className= > Company Name (Optional) </label> <input type= value={formData.company} onChange={(e) => setFormData({ ...formData, company: e.target.value }) } className= placeholder= /> </div> )} { } {selectedRole === && ( <div> <label className= > Service Category * </label> <select required value={formData.serviceCategory} onChange={(e) => setFormData({ ...formData, serviceCategory: e.target.value }) } className= > <option value= >Select your service category</option> {PROVIDER_CATEGORIES.map((category) => ( <option key={category} value={category}> {category} </option> ))} </select> </div> )} { } <div className= > <input type= id= checked={formData.agreeToTerms} onChange={(e) => setFormData({ ...formData, agreeToTerms: e.target.checked }) } className= /> <label htmlFor= text-sm text-gray-600 } <Link to= target= rel= className= > Terms of Service </Link>{ } and{ } <Link to= target= rel= className= > Privacy Policy </Link> </label> </div> { } {error !== null && ( <div className= > <AlertCircle className= /> <span>{typeof error === An error occurred. Please try again. id= disabled={ !formData.agreeToTerms || loading || formData.password.length < 6 } className={`w-full py-3.5 rounded-xl font-semibold text-white transition-all active:scale-[0.98] flex items-center justify-center gap-2 shadow-lg ${ formData.agreeToTerms && formData.password.length >= 6 && !loading ? selectedRole === : selectedRole === : selectedRole === }`} > {loading ? ( <> <Loader2 className= /> Creating Account... </> ) : ( <> Create Account <ArrowRight className= /> </> )} </button> <div className= > <Shield className= /> <span>Your information is secure and encrypted</span> </div> </form> <p className= > Already have an account?{ } <Link to= font-semibold text-blue-700 hover:text-blue-800 transition-colors text-center text-blue-300 text-xs mt-6 } <Link to= underline hover:text-blue-200 transition-colors } and acknowledge our{ } <Link to= underline hover:text-blue-200 transition-colors Wrench, ArrowRight, Check, Shield, Building, Eye, EyeOff, User, Lock, Mail, AlertCircle, Loader2, RefreshCw, from \"lucide-react\"; function getPasswordStrength(pw: string): score: number; label: string; color: string; bg: string if (!pw) return score: 0, label: '', color: '', bg: '' ; let score = 0; if (pw.length >= 8) score++; if (pw.length >= 12) score++; if (/[A-Z]/.test(pw)) score++; if (/[0-9]/.test(pw)) score++; if (/[^A-Za-z0-9]/.test(pw)) score++; if (score (\"role\"); const [selectedRole, setSelectedRole] = useState (null); const [formData, setFormData] = useState( name: \"\", email: \"\", password: \"\", company: \"\", serviceCategory: \"\", agreeToTerms: false, ); const [loading, setLoading] = useState(false); const [error, setError] = useState (null); const [nameError, setNameError] = useState(\"\"); const [emailError, setEmailError] = useState(\"\"); const [showPassword, setShowPassword] = useState(false); const [emailSent, setEmailSent] = useState(false); const [emailStatusMessage, setEmailStatusMessage] = useState (null); const [resending, setResending] = useState(false); const [resendCooldown, setResendCooldown] = useState(0); const [resendMessage, setResendMessage] = useState(\"\"); const handleRoleSelect = (role: UserRole) => setSelectedRole(role); setStep(\"form\"); ; const strength = getPasswordStrength(formData.password); const startCooldown = useCallback(() => setResendCooldown(RESEND_COOLDOWN); const interval = setInterval(() => setResendCooldown((prev) => if (prev if (resendCooldown > 0) return; setResending(true); setResendMessage(\"\"); const error: signupErr = await signUp( formData.email, formData.password, formData.name, selectedRole, formData.company, selectedRole === \"provider\" ? formData.serviceCategory : undefined, ); setResending(false); if (signupErr) if ( signupErr.message.toLowerCase().includes(\"already\") || signupErr.message.toLowerCase().includes(\"registered\") ) setResendMessage(\"Confirmation email resent! Check your inbox.\"); startCooldown(); else s"
    },
    {
      "file": "Support.tsx",
      "path": "src/pages/Support.tsx",
      "title": "Support",
      "text": "How can we help you? Frequently Asked Questions {category.title} {searchQuery && `(${filteredFaqs.length})`} {faq.question} Contact Support How can we help you? Our support team is here for you All systems operational Frequently Asked Questions {category.title} {searchQuery && `(${filteredFaqs.length})`} {openCategories[category.title] && ( {faq.question} part.match(/jobbridgesupport@gmail\\.com/) ? ( Contact Support Mon-Fri, 9am-10pm CAT Email Support jobbridgesupport@gmail.com dashboard to see all submitted applications, their status, and any recruiter messages.', }, { question: 'Can I withdraw an application?', answer: 'Yes, you can withdraw applications from the page if the status is min-h-screen bg-white flex flex-col > { } <div className= > <div className= > <img src= className= /> <div className= > <div className= > <h1 className= > How can we help you? </h1> <p className= >Our support team is here for you</p> </div> </div> </div> <div className= > <Search className= /> <input type= placeholder= value={searchQuery} onChange={e => setSearchQuery(e.target.value)} className= /> </div> </div> { } <div className= > <div className= > <CheckCircle className= /> <div> <p className= >All systems operational</p> </div> </div> </div> <div className= > { } <AnimatedSection direction= > <h2 className= >Frequently Asked Questions</h2> <div className= > {faqCategories.map((category) => { const filteredFaqs = searchQuery ? category.faqs.filter(f => f.question.toLowerCase().includes(searchQuery.toLowerCase()) || f.answer.toLowerCase().includes(searchQuery.toLowerCase()) ) : category.faqs; if (searchQuery && filteredFaqs.length === 0) return null; return ( <div key={category.title}> <button onClick={() => toggleCategory(category.title)} className= > <h3 className= >{category.title} {searchQuery && `(${filteredFaqs.length})`}</h3> <ChevronDown className={`w-5 h-5 text-gray-600 transition ${ openCategories[category.title] ? 'rotate-180' : '' }`} /> </button> {openCategories[category.title] && ( <div className= > {filteredFaqs.map((faq, idx) => ( <div key={idx} className= > <h4 className= >{faq.question}</h4> <p className= text-blue-700 hover:underline > <h2 className= >Contact Support</h2> <div className= > { } <Card3D onClick={() => window.open('https: className= strength={6} > <button className= > <MessageCircle className= /> <div> <p className= >Live Chat</p> <p className= >09136171354</p> <p className= >Mon-Fri, 9am-10pm CAT</p> </div> </button> </Card3D> { } <Card3D className= strength={6}> <a href= className= > <Mail className= /> <div> <p className= >Email Support</p> <p className= >jobbridgesupport@gmail.com</p> </div> </a> </Card3D> { } <Card3D className= strength={6}> <a href= className= > <Phone className= /> <div> <p className= >Call Us</p> <p className= interface FAQ question: string; answer: string; interface FAQCategory title: string; faqs: FAQ[]; const openModal = useModal(); const [openCategories, setOpenCategories] = useState >( ); const [searchQuery, setSearchQuery] = useState(''); const faqCategories: FAQCategory[] = [ title: 'Account & Profile', faqs: [ question: 'How do I reset my password?', answer: 'Click \"Forgot Password\" on the login page and enter your email. We\\'ll send you a reset link valid for 24 hours.', , question: 'Can I change my email address?', answer: 'Yes, go to Settings > Account and click \"Change Email\". You\\'ll need to verify your new email address.', , question: 'How do I delete my account?', answer: 'Navigate to Settings > Account > Delete Account. Note that this action is permanent and cannot be undone.', , ], , title: 'Job Applications', faqs: [ question: 'Can I apply to multiple jobs at once?', answer: 'Yes, JobBridge allows you to apply to unlimited jobs. You can also save jobs to apply later from your saved list.', , question: 'How do I track my applications?', answer: 'Use the \"My Applications\" dashboard to see all submitted applications, their status, and any recruiter messages.', , question: 'Can I withdraw an application?', answer: 'Yes, you can withdraw applications from the \"My Applications\" page if the status is \"Applied\" or \"In Review\".', , ], , title: 'Premium & Billing', faqs: [ question: 'What payment methods do you accept?', answer: 'We accept all major credit cards (Visa, Mastercard, American Express), PayPal, and Apple Pay.', , question: 'Can I cancel my subscription anytime?', answer: 'Yes, you can cancel anytime from your billing settings. Your access continues until the end of your billing cycle.', , question: 'Do you offer refunds?', answer: 'We offer 30-day money-back guarantee if you\\'re not satisfied with your subscription. Contact jobbridgesupport@gmail.com.', , ], , title: 'Technical Issues', faqs: [ question: 'The website is loading slowly. What should I d"
    },
    {
      "file": "TalentSearch.tsx",
      "path": "src/pages/TalentSearch.tsx",
      "title": "TalentSearch",
      "text": "{filteredCandidates.length} candidates found {candidate.name} Saved (3) Name / Title Location Experience Level Availability Search Advanced Filters Skills (comma-separated) Min Salary (₦) Preferred Companies {filteredCandidates.length} candidates found Best Match Experience {candidate.img ? ( {getInitials(candidate.name)} {candidate.name} {candidate.role} {candidate.company} {candidate.matchScore}% {candidate.location} {candidate.experienceLevel} {candidate.immediate && ( Immediate {(candidate.salary / 1000).toFixed(0)}k {skill} View Profile Connect Message {getInitials(candidate.name)} {candidate.name} {candidate.role} View All Post a Job min-h-screen bg-gray-50 flex flex-col flex-1 pb-20 md:pb-0 subtitle= images={HERO_CAROUSELS.talent} imageAlt= compact /> { } <section className= > <div className= > { } <div className= > <div> <label className= > Name / Title </label> <input type= placeholder= value={searchName} onChange={(e) => setSearchName(e.target.value)} className= /> </div> <div> <label className= > Location </label> <input type= placeholder= value={searchLocation} onChange={(e) => setSearchLocation(e.target.value)} className= /> </div> <div> <label className= > Experience Level </label> <select value={experienceLevel} onChange={(e) => setExperienceLevel(e.target.value)} className= > <option>All</option> <option>Entry</option> <option>Mid</option> <option>Senior</option> <option>Executive</option> </select> </div> <div> <label className= > Availability </label> <select value={availability} onChange={(e) => setAvailability(e.target.value)} className= > <option>All</option> <option>Immediate</option> <option>2 weeks</option> <option>1 month</option> </select> </div> </div> { } <div className= > <button onClick={handleSearch} className= > <Search size={18} /> Search </button> <button onClick={() => setShowAdvanced(!showAdvanced)} className= > <ChevronDown size={18} className={`transform transition-transform ${showAdvanced ? 'rotate-180' : ''}`} /> Advanced Filters </button> </div> { } {showAdvanced && ( <div className= > <div> <label className= > Skills (comma-separated) </label> <input type= placeholder= value={searchSkills} onChange={e => setSearchSkills(e.target.value)} className= /> </div> <div> <label className= > Min Salary (₦) </label> <input type= placeholder= value={minSalary} onChange={e => setMinSalary(e.target.value)} className= /> </div> <div> <label className= > Preferred Companies </label> <input type= placeholder= value={preferredCompanies} onChange={e => setPreferredCompanies(e.target.value)} className= /> </div> </div> )} </div> </section> <div className= > <div className= > { } <div className= > { } <div className= > <h2 className= > {filteredCandidates.length} candidates found </h2> <div className= > <label className= >Sort by:</label> <select value={sortBy} onChange={(e) => setSortBy(e.target.value)} className= > <option>Best Match</option> <option>Recent</option> <option>Experience</option> <option>Salary</option> </select> </div> </div> { } <div className= > {filteredCandidates.map(candidate => ( <div key={candidate.id} className= > { } <div className= > <div className= > {candidate.img ? ( <img src={candidate.img} alt={candidate.name} className= /> ) : ( <div className={`${getAvatarColor(candidate.id)} text-white w-12 h-12 rounded-full flex items-center justify-center font-bold text-sm`}> {getInitials(candidate.name)} </div> )} <div className= > <h3 className= >{candidate.name}</h3> <p className= >{candidate.role}</p> <p className= >{candidate.company}</p> </div> </div> {candidate.matchScore >= 85 && ( <span className= > {candidate.matchScore}% </span> )} </div> { } <div className= > <div className= > <MapPin size={16} className= /> <span>{candidate.location}</span> </div> <div className= > <Briefcase size={16} className= /> <span className={`inline-block px-2 py-1 rounded text-xs font-semibold ${getExperienceBadgeColor(candidate.experienceLevel)}`}> {candidate.experienceLevel} </span> {candidate.immediate && ( <span className= > <span className= ></span> Immediate </span> )} </div> <div className= > <span className= >₦</span> <span>{(candidate.salary / 1000).toFixed(0)}k</span> </div> </div> { } <div className= > <div className= > {candidate.skills.slice(0, 4).map((skill, idx) => ( <span key={idx} className= > {skill} </span> ))} </div> </div> { } <div className= > <button onClick={() => openModal('profile', { name: candidate.name, role: candidate.role, match: `${candidate.matchScore}%` })} className= > View Profile </button> <div className= > <button onClick={() => openModal('connect', { name: candidate.name })} className= > Connect </button> <button onClick={() => openModal('message', { name: candidate.name })} className= > <MessageSquare size={16} /> Message </button> </div> </div> </div> ))} </div> </div> { } <div className= > <div className= > <h3 className= >Saved (3)</h3> <div className= > {candidates.slice(0, 3).map(candidate => ( <div key={candidate.id} className= > <div className={`${getAvatarColor(candidate.id)} text-white w-8 h-8 rounded-full flex items-center justify-center font-bold text-xs flex-shrink-0`}> {getInitials(candidate.name)} </div> <div className= > <p className= >{candidate.name}</p> <p className= >{candidate.role}</p> </div> </div> ))} </div> <button className= > View All </button> </div> { } <button onClick={() => openModal('post-job')} className= interface Candidate id: number; name: string; role: string; company: string; location: string; experienceLevel: 'Entry' | 'Mid' | 'Senior' | 'Executive'; matchScore: number; skills: string[]; availability: 'Immediate' | '2 weeks' | '1 month'; salary: number; immediate?: boolean; img?: string; const TalentSearch: React.FC = () => const openModal = useModal(); const [searchName, setSearchName] = useState(''); const [searchLocation, setSearchLocation] = useState(''); const [experienceLevel, setExperienceLevel] = useState('All'); const [availability, setAvailability] = useState('All'); const [showAdvanced, setShowAdvanced] = useState(false); const [sortBy, setSortBy] = useState('Best Match'); const [searchSkills, setSearchSkills] = useState(''); const [minSalary, setMinSalary] = useState(''); const [preferredCompanies, setPreferredCompanies] = useState(''); const candidates: Candidate[] = [ id: 1, name: 'Alex Rivera', role: 'Senior React Engineer', company: 'Stripe', location: 'San Francisco, CA', experienceLevel: 'Senior', matchScore: 97, skills: ['React', 'TypeScript', 'GraphQL', 'AWS'], availability: 'Immediate', salary: 145000, immediate: true, img: avatarForIndex(0), , id: 2, name: 'Mia Chen', role: 'Product Designer', company: 'Figma', location: 'Remote', experienceLevel: 'Mid', matchScore: 94, skills: ['Figma', 'Sketch', 'UX Research'], availability: '2 weeks', salary: 115000, img: avatarForIndex(1), , id: 3, name: 'Jordan Park', role: 'Full Stack Dev', company: 'Netflix', location: 'Los Angeles, CA', experienceLevel: 'Senior', matchScore: 91, skills: ['Node.js', 'React', 'PostgreSQL'], availability: 'Immediate', salary: 135000, immediate: true, img: avatarForIndex(2), , id: 4, name: 'Sam Taylor', role: 'Data Scientist', company: 'Google', location: 'New York, NY', experienceLevel: 'Senior', matchScore: 89, skills: ['Python', 'ML', 'TensorFlow'], availability: '1 month', salary: 155000, img: avatarForIndex(3), , id: 5, name: 'Casey Morgan', role: 'DevOps Engineer"
    }
  ]
}